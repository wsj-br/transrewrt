<!-- DOCTOC SKIP -->

# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Use conventional types (Added, Changed, Fixed, etc.) and short descriptions.

## Unreleased

## [1.3.6] - 2026-06-02

- **Fixed**: Invalid or disabled OpenRouter API keys no longer show the cryptic "User not found." message; users see a clear OpenRouter API key error instead.
- **Fixed**: Electron translate/rewrite/transform — preload resolves LLM stream from the `llm:stream` invoke result (not `llm:end`), avoiding an IPC ordering race that dropped all streamed text; OpenRouter deltas use shared `streamChoiceToString` (multipart `content`, `delta.text`, `message.content`); empty successful runs show a clear message instead of staying on “Translating…”.
- **Fixed**: `scripts/upgrade-tools.ps1` — use forward-slash paths in inline `node -e` so Windows backslashes are not mangled as JS escapes when updating `packageManager`.
- **Changed**: `.github/workflows/pnpm-audit.yml` — bump actions to Node 24 runtimes (`checkout@v6`, `pnpm/action-setup@v5`, `setup-node@v6`, `create-pull-request@v8`).

## [1.3.5] - 2026-06-02

- **Changed**: `take-screenshots` — force dark theme in config/user prefs; load Noto web fonts (Bengali, Devanagari, KR, Gurmukhi, Telugu, Thai) globally so locales like bn, hi, ko, pa, te, th render correctly.
- **Changed**: i18n runtime aligned with ai-i18n-tools GETTING_STARTED — `setupKeyAsDefaultT`, `makeLocaleLoadersFromManifest`, `SOURCE_LOCALE` fallbacks, `flipUiArrowsForRtl` from runtime; added `i18n:dashboard` and `i18n:locales` scripts.
- **Removed**: Reverted unnecessary i18n complexity (`UiLanguageProvider`, `setUiLanguage`, `bindI18n: languageChanged loaded`, memo deps on locale); root cause of the reported issue was a stale dev-server process.
- **Fixed**: Settings → General → Presets catalog version and **Last check** timestamps use the browser/system timezone when available, with Docker `TZ` as fallback on web.
- **Fixed**: Settings → Models — **Deselect All** confirmation uses the in-app modal instead of the browser `confirm` dialog.
- **Changed**: Settings → Models — **Deselect All** uses outline button styling so its border stays visible.
- **Added**: Settings → Models — **Select from presets** on Selected Models adds preset main/fallback models per configured provider (with Load all).
- **Fixed**: Presets editor — show a connection-lost overlay when the server stops or becomes unreachable; health check every 10s (and on failed API calls) with auto-dismiss when the server returns.
- **Fixed**: Presets editor model picker — show “Pricing not available” instead of $0 when OpenRouter pricing is missing.
- **Changed**: OpenRouter provider routing — prefer lowest latency (`sort: "latency"`) instead of highest throughput.
- **Added**: Presets editor — OpenRouter models, pricing, and endpoint performance cached on disk (`presets-editor-openrouter-cache.json`, 6h TTL); model picker and Performance page read from cache (`?force=1` / Refresh to rebuild).
- **Added**: Presets editor — model picker (OpenRouter) shows best-provider latency P90 (seconds) and throughput P90 (tok/s) on the pricing row; loaded from the OpenRouter disk cache.
- **Changed**: `.github/workflows/release.yml` — bump artifact actions to Node 24 runtimes (`upload-artifact@v7`, `download-artifact@v8`, `action-gh-release@v3`); remove `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` (was triggering Node 20 deprecation warnings).
- **Fixed**: Dev presets editor OpenRouter disk cache — unused catch binding (eslint `no-unused-vars`).

## [1.3.4] - 2026-05-30

- **Changed**: `package.json` — duplicate `pnpm.overrides` from `pnpm-workspace.yaml` so Dependabot resolves override metadata from the manifest.
- **Changed**: Settings → General → Presets catalog — version shows catalog `updated_at` after the version (dash-separated); **Last check** shows when the app last looked for a remote update.
- **Fixed**: `pnpm 3p-notices` — `@jsonjoy.com/json-pointer@>=1.0.0 <2.0.0` clarification uses `licenseText` (v1.0.2 ships no `LICENSE` file; broken `licenseFile` path crashed license-checker).
- **Changed**: Settings → Transform prompts: row checkboxes (all selected by default), bulk delete toolbar, per-row save to `transrewrt_transform_[NAME].json`, and Export only includes selected prompts.
- **Fixed**: Unchecked checkboxes remain visible on muted table headers (dark and light theme).
- **Fixed**: Stacked workspace layout no longer forces the sidebar collapsed; collapse/expand follows the normal persisted preference.
- **Fixed**: Electron startup loading screen follows the OS dark/light theme before app config is loaded (main-process nativeTheme via IPC, early body class, CSS fallback).
- **Fixed**: Web app startup hang on "Loading…" — auth-first bootstrap, 12s safety timeout, non-blocking presets/locale load, and no renderer import of Node-only `multi-llm-ts`.
- **Fixed**: Cost Tracking maps OpenRouter auth failures (e.g. “Missing Authentication header”) to “API Key is invalid”.
- **Fixed**: `scripts/upgrade-dependencies.sh` keeps latest Electron when upstream ships a matching `better-sqlite3` prebuild; otherwise skips Electron in ncu (registry probe via `scripts/better-sqlite3-electron-compat.js`, no compile loop).
- **Fixed**: Pin `electron` to ^41.7.1 until `better-sqlite3` publishes Electron 42 (ABI 146) prebuilds ([issue #1474](https://github.com/WiseLibs/better-sqlite3/issues/1474)).
- **Fixed**: Docker build copies `pnpm-workspace.yaml` and `.npmrc` so `pnpm install` applies the same `minimumReleaseAgeExclude` / hoisted linker settings as local installs.

## [1.3.3] - 2026-05-27

- **Changed**: `README.md` and `USER-GUIDE.md` — Easy-mode preset names and nomenclature aligned with `easy-mode-config/presets.json` (**Standard** not Lite; preset vs skill wording).
- **Added**: `scripts/release.sh` and `pnpm run release:github` / `release:github:dry` — create GitHub releases from `release-notes/RELEASE_NOTES_<version>.md` (same flow as ai-i18n-tools).
- **Changed**: [dev/DEVELOPMENT.md](DEVELOPMENT.md) — document `release:github`, `release-notes/RELEASE_NOTES_<version>.md`, and pre-release checks.
- **Fixed**: Dev presets editor Reload re-reads only `easy-mode-config/presets.json` from disk (provider catalog cache still follows TTL, not refreshed on every Reload).
- **Fixed**: Dev presets editor header no longer leaves translation/suggestion model labels blank after load (sync display after catalog fetch).
- **Changed**: `presets.json` serialization keeps catalog metadata (`version`, `updated_at`, translation/suggestion models) at the top and `presets` last (presets editor save and presets-check).
- **Changed**: Presets-check no longer validates top-level `translation_model` / `suggestion_model` fields (preset `model_ids` only).
- **Fixed**: Presets-check NTFY notifications no longer fail when titles contain Unicode punctuation (em dash in HTTP headers).
- **Changed**: Presets-check logs start/finish per preset; model replacements still logged between those lines.
- **Changed**: Renamed Easy-mode skills to presets across catalog (`easy-mode-config/presets.json`), APIs (`/api/presets`), UI, dev tools (`presets-editor`, `preset-check`), and docs; screenshot script uses Easy mode with the Standard preset.
- **Changed**: Dev presets editor — internal helpers, UI strings, and `/api/meta` fields use preset nomenclature throughout.
- **Fixed**: Dev presets editor AI Suggestion setup — preset checkboxes use `ai-suggest-presets-checkboxes` id/classes matching the HTML (list was empty after the rename).
- **Fixed**: Webpack dev watch ignores `images/screenshots/` so `pnpm take-screenshots` no longer triggers a rebuild on every PNG write while `dev` / `dev:web` is running.
- **Changed:** Skill-check `run.sh` from install matches production: ssh-agent + deploy key, `source .env`, then checker; README documents `.env` and SSH setup.
- **Fixed:** Skill-check production install (`skill-check:install`) bundles `llm/estimateMaxTokens.js` so the runtime no longer fails with `Cannot find module './estimateMaxTokens'`.
- **Changed:** Behaviour setting label "Auto-translate on paste" renamed to "Auto-execute on paste".
- **Added:** Workspace footer toggles for auto-execute (input) and auto-copy (output) on translate, rewrite, and transform pages, synced with General Settings.
- **Added:** Hover tooltip on output run metrics (elapsed/TPS) showing last-run cost when inline cost is hidden.
- **Fixed:** Vertical center alignment of output metrics in the header row and pane footer controls (stats, switches, model id, buttons).
- **Changed:** Workspace auto-execute/auto-copy switches use footer action colours when on (muted on input row, mode accent on output row); rewrite grammar **Changes** switch always shown in grammar mode and uses mode accent when on.
- **Changed:** Toggle (`Switch`) styling follows a consistent simple on/off pattern in light and dark (neutral off track, white thumb); workspace footers use mode accent for the on track via `switch-accent` (translate / rewrite / transform colours).
- **Fixed:** Workspace `switch-accent` toggles no longer stay green in rewrite/transform — checked track uses `rgb(var(--mode-accent-rgb))` from `main[data-mode]`.
- **Fixed:** Auto-execute footer label and switch use the same mode accent styling as Auto-copy on each page.
- **Fixed:** Auto-copy after translate/rewrite/transform on Electron — clipboard write uses main process so it works without a click (renderer `writeText` after async often fails silently).
- **Fixed:** Output metrics cost tooltip only on hover over the Elapsed/TPS text (not the full header cell); tooltip shows above the text.
- **Fixed:** Output metrics cost tooltip uses the same cost fraction digit style as Settings (Appearance).
- **Changed:** Tooltips use theme background with a thin border (black in light mode, white in dark mode) instead of inverted foreground/background colours.
- **Changed:** Tooltips no longer show a pointer arrow (bordered panel only).
- **Removed**: Duplicate Clear (Esc) icon on the input pane stats row in translate, rewrite, and transform workspaces (Clear remains in the action bar; Esc still clears).
- **Added**: Skills editor AI Suggest review — per-provider select to keep the current primary/fallback model or apply the AI suggestion.
- **Fixed**: Skills editor AI Suggest — increased `max_tokens` from 4096 to 65536 so thinking-mode models don't exhaust the limit on reasoning tokens and return an empty response.
- **Fixed**: Translate, rewrite, and transform estimate `max_tokens` from input size and task type (translate 1.0×+300, rewrite 2.0×+500, transform 3.0×+500; untagged calls use transform; floor 500, cap 48k) so OpenRouter does not reserve the model default (e.g. 65536).
- **Changed**: Removed unconfigured `translate-svg` step from `i18n:translate`; `i18n:translate:svg` now prints an informational message. Updated `dev/DEVELOPMENT.md` with screenshot Pattern B workflow note.
- **Added**: Easy-mode skills now support per-provider fallback models via `skill.fallback_ids` with an automatic one-time retry on primary model failures; skills editor AI Suggest and `dev/skill-check` validate the fallback ids.
- **Fixed**: Dev skills editor — per-provider `fallback_ids` fields (type, Choose, Test) in the skill form; AI Suggestion review lets you change primary and fallback models separately.

## [1.3.2] - 2026-05-22

- **Changed**: `scripts/clean-workspace.sh` and `scripts/clean-workspace.ps1` remove all repository `.log` files (excluding `node_modules`, `.git`, `dist`, `release`, `documentation/node_modules`) plus `dev/skill-check/provider-catalogs-cache.json`, `dev/skill-check/skill-check.log`, and `skills-editor-provider-catalogs.json`.
- **Added**: Dev skill-check (`dev/skill-check/`) — cron-friendly CLI that validates Easy-mode `skills.json` model ids against live provider catalogs, fuzzy-replaces unavailable models, commits/pushes only `easy-mode-config/skills.json` to GitHub from an isolated runtime (`pnpm run skill-check:install`), logs changes, and sends NTFY notifications; shared provider catalog logic extracted to `src/shared/skillsProviderCatalog.js`.
- **Fixed**: Skill-check git push — rebase on `origin/main` before push so stale shallow clones do not fail with “fetch first” when SSH auth is fine.
- **Changed**: Skill-check install — never overwrites existing `config.json` or `run.sh`; skips copying `config.example.json` when `config.json` already exists.
- **Fixed**: Skill-check fuzzy model match — collapse duplicate dots in ids (e.g. `laguna-m..1` → `laguna-m.1`), prefer same model family (`laguna-m` vs `laguna-xs`), and tie-break on string similarity instead of capping bonus scores at 1.0 (which wrongly picked `laguna-xs.2` over `laguna-m.1`).
- **Added**: Dev skills editor — server console output written to `skills-editor.log` (repo root; previous run rotated to `skills-editor-<timestamp>.log` on startup); **Server log** button in the header; startup alert when the session log contains errors.
- **Changed**: Move `pnpm.overrides` and `pnpm.allowedDeprecatedVersions` from `package.json` to `pnpm-workspace.yaml` (pnpm 11 config location).
- **Changed**: Rename "Regular" skill to "Lite" with updated description ("Fast, lightweight, and cost-efficient…") to better reflect the lightweight model tier.
- **Fixed**: Duplicate export key `isEngineCatalogCached` in `src/shared/skillsProviderCatalog.js` (eslint `no-dupe-keys`).

## [1.3.1] - 2026-05-18

- **Fixed**: Windows NSIS assisted installer — "Run Transrewrt" on Finish launches the app via detached `cmd /c start` and hides the wizard first, avoiding a long "Not responding" state while Electron cold-starts.
- **Changed**: Easy-mode `skills.json` remote sync: Electron only in Easy mode (6 h throttle; manual refresh in Settings → General); web server still syncs shared `data/skills.json` every 6 h for all users, with `POST /api/skills/sync` for Easy-mode on-demand refresh.
- **Added**: Settings → General → AI experience — skills catalog version, `updated_at`, and Refresh button (second column beside provider).
- **Changed**: Easy-mode remote skills sync log (Electron + web) includes `updated_at` for the new and previous catalog.
- **Changed**: Dev skills editor — each catalog save bumps `easy-mode-config/skills.json` patch version and refreshes `updated_at`.
- **Changed**: `README.md` and `USER-GUIDE.md` — document Easy vs Advanced AI experience, skills/provider toolbar, simplified Dashboard tabs, web History scoping, and `HISTORY_DISABLED`.
- **Fixed**: Packaged Electron (AppImage/NSIS) configuration backup restore and SQLite — run `electron-rebuild` before `electron-builder` so `better-sqlite3` matches Electron’s Node ABI (avoids `NODE_MODULE_VERSION` mismatch after `dev:web` / `node-rebuild`); clearer error when the native module cannot load.
- **Changed**: Web Settings > API Config — provider status cards use a two-column grid.
- **Fixed**: Configuration backup export on web and Electron — use `archiver` v8 `ZipArchive` API (`require('archiver')` no longer provides a callable factory).
- **Fixed**: Linux taskbar icon (Electron dev and AppImage) — set `wayland-app-id` / `class` to `transrewrt`, align `StartupWMClass` in the built `.desktop`, resize PNG for `BrowserWindow`, and write `transrewrt-dev.desktop` on `pnpm dev` for Wayland shell icon lookup.
- **Changed**: Transform prompt editor modals (translate / improve / generate fields): Easy mode uses the same skill selector as Translate and Rewrite; Advanced mode keeps the model list.
- **Fixed**: `pnpm install` postinstall — pin `electron` to ^41.6.1 until `better-sqlite3` supports Electron 42 (V8 API break; upstream [issue #1474](https://github.com/WiseLibs/better-sqlite3/issues/1474)); add `pnpm-workspace.yaml` `allowBuilds` for pnpm 11 (replaces `onlyBuiltDependencies`); add `.nvmrc` (`24`) matching `engines.node`.
- **Security**: Removed unused `geist` dependency (pulled in vulnerable `next` transitively); Geist fonts remain self-hosted under `src/renderer/assets/fonts`.
- **Changed**: Easy mode is the default **AI experience** when `mode` is unset: `config_default.json` already uses `mode: "easy"`; on load the app now persists `mode: "easy"` for configs that omit it (legacy installs).
- **Added**: Dev skills editor AI Suggestion setup: checkbox list to choose which skills receive model suggestions (all selected by default; `skill_ids` in `POST /api/skills/suggest-models`).
- **Fixed**: Dev skills editor AI Suggestion: prompts and catalog filtering require chat-compatible models for Transrewrt translate/rewrite/transform (excludes completion-only endpoints such as `gpt-5.5-pro`).
- **Added**: Dev skills editor persists provider model catalogs to `skills-editor-provider-catalogs.json` at the repo root (gitignored, `lastUpdated` + 2h TTL); stale or missing cache refreshes from provider APIs on editor start.
- **Fixed**: Dev skills editor AI Suggestion **Continue** no longer refetches all provider model catalogs when boot prefetch already warmed the server session cache.
- **Fixed**: Dev skills editor AI Suggestion: `parseJsonFromModelText` now extracts JSON embedded in mixed reasoning text; `SUGGEST_SYSTEM` prompt enforces JSON-only final output; fallback attempt skips web search to avoid the same mixed-content failure and be faster.
- **Changed**: Dev skills editor AI Suggestion: prompts require catalog-only `model_id` values; server drops suggestions not present in the prefetched provider catalog.
- **Changed**: Dev skills editor: prefetch all provider model catalogs at startup (boot overlay) before showing the editor; skill form and AI Suggestion use the session cache.
- **Added**: Dev skills editor **AI Suggestion**: setup page for `suggestion_model` / `suggestion_model_fallback`, session-cached provider catalogs, run log with server `log` events, review UI (`POST /api/skills/suggest-models` uses suggestion models + OpenRouter web search).
- **Changed**: Easy mode skills: drop legacy per-skill `model_id`; app hides skills without `model_ids[provider]` (editor still lists all skills).
- **Added**: Dev skills editor: per-provider `model_ids` matrix (nine cloud engines); **Choose** opens the catalog when an API key is set; **No key** rows still accept typed model ids; per-row **Test**; `free-router` skill shows OpenRouter only.
- **Fixed**: Web Easy mode **Provider** list: use `configuredEngines` from `GET /api/status` (server env keys) instead of client `*_configured` flags that only exist on Electron.
- **Added**: Easy mode multi-provider: **Provider** control in Settings → General (hidden in Advanced); cloud skills use per-provider `model_ids` in `easy-mode-config/skills.json` (five skills: Free, Fast, Advanced, Technical, Legal); skills without a mapping for the selected provider are omitted; Easy + **Ollama** shows installed local models in the header instead of skills (`easy_ollama_model`).
- **Changed**: **AI experience** mode renamed **Regular** → **Easy** (`mode: "easy"`); skills catalog directory `regular-mode-config/` → `easy-mode-config/`.
- **Fixed**: Dev skills editor: **Test model** result is a fixed-height grid row below `model_id` so showing a message no longer shifts the detail layout (was nested inside the label, so `.form-grid` gap had no effect).
- **Changed**: Dev skills editor: clearing the selected skill or picking another in the list resets **Test model** output.
- **Fixed**: Dev skills editor: model picker modal list fills the modal height (removed inherited `max-height: 420px` from `.model-list` on the modal scroll area).
- **Fixed**: Web app: `App` no longer reads `settings` before `useAppContext()` (fixes “Cannot access 'settings' before initialization” on load).
- **Changed**: Dev skills editor: malformed `regular-mode-config/skills.json` is reported with plain-language **400** messages from `GET /api/skills` and in the status line (not **500** or `skills HTTP …` wording).
- **Changed**: Dev skills editor: name and description labels show the configured source locale in parentheses (e.g. `(en-GB)`) instead of the literal “(source)”.
- **Changed**: Dev skills editor: translations table shows `englishName` (column “English name”), narrow locale/name columns and a wide value column; translations stack scrolls in the remaining viewport height; removed the source-locale hint line; **Test model** output sits below `model_id`; `body` / `main.layout` / detail column use flex so the right panel can use full height.
- **Changed**: Dev skills editor: model picker modal keeps a fixed height (`min(90vh, 900px)`) so empty search results no longer shrink the dialog; the list area scrolls inside that frame.
- **Changed**: Dev skills editor: **Test model** sits beside **Change** on the `model_id` row; **Delete skill** stays in the detail header.
- **Changed**: Dev skills editor: catalog fields `translation_model` and optional `translation_model_fallback` (replaces `editor_translation_model_id`); header **Choose…** buttons open the model modal; inline skill list no longer shows the catalog picker. **Translate missing** uses `translation_model` from the catalog body; server retries each locale call with the fallback when the primary fails (`POST /api/skills/translate-missing`); NDJSON stream unchanged (`?stream=1`).
- **Changed**: Dev skills editor: **Change** next to per-skill `model_id` opens the same large modal picker (Escape / Close / backdrop to dismiss).
- **Added**: Development-only skills catalog editor: `pnpm run dev:skills-editor` serves `dev/skills-editor/` (edit `regular-mode-config/skills.json`, mirror to `data/skills.json`, OpenRouter model list, test model, translate missing, show translations). See `dev/skills-editor/README.md`.
- **Fixed**: Dev skills editor model list used only the free route when `OPENROUTER_API_KEY` was unset because `getAllModels` skips the OpenRouter engine without a key; the editor now loads the public OpenRouter `/v1/models` catalog (with optional key), matching Settings → Models coverage.
- **Changed**: Dev skills editor (`pnpm run dev:skills-editor`) opens the UI in the default browser on startup; set `SKILLS_EDITOR_NO_OPEN=1` to disable.
- **Changed**: Dev skills editor uses `OPENROUTER_API_KEY` from `process.env` only (does not read `.env`); see `dev/skills-editor/README.md`.
- **Fixed**: Dev skills editor model list: register API routes before static files, retry OpenRouter `GET /v1/models` via `https` when `fetch` fails, normalize `top_provider` from the API, and log catalog size; picker shows inner model ids (`displayId`) while `model_id` stays canonical `openrouter/...`.
- **Fixed**: Dev skills editor: if the HTTP port is already in use, Express 5 was still running the listen “success” callback (same `once()` handler as `error`), so the process printed startup lines then exited; startup now uses a `listening` handler plus explicit `error` / `EADDRINUSE` messaging, detached `spawn` for opening the browser, and a “Press Ctrl+C to stop” line when bound.
- **Changed**: Dev skills editor UI: detail area is two columns (form left, translations right); translations table is always visible for the selected skill (no toggle); model catalog picker lives in modals only; slightly smaller base font (`15px`).
- **Fixed**: Regular mode `skills.json`: **Advanced — Non-western** `model_id` `openrouter/google/gemini-3.1-pro` is not on OpenRouter; use `openrouter/google/gemini-3.1-pro-preview` (catalog match from `GET https://openrouter.ai/api/v1/models`).
- **Changed**: Regular mode skills catalog: canonical `regular-mode-config/skills.json` in this repo; remote URL is the same path on GitHub (`wsj-br/transrewrt`). Packaged Electron ships `config/skills.json` from that file; `updated_at` on disk newer than remote skips overwrite. First launch downloads the catalog into the app data directory (Electron main bootstrap; web server writes `data/skills.json` on startup if missing). Per-skill `translated_name` / `translated_description` locale maps; UI uses `ui_locale` then `source_locale` then fallback `name` / `description`. Default config adds `source_locale` (en-GB). Docker image copies `regular-mode-config/` for server defaults.
- **Added**: Regular mode (default): skills catalog from `skills.json` with `GET /api/skills` (web), Electron IPC `readSkills` / `updateSkillsFromRemote`, header `SkillSelector`, Settings → General **AI experience** (Regular vs Advanced), hidden Models tab in Regular mode, optional `prompt_hint` appended to translate/rewrite/transform system prompts; packaged `config/skills.json` copied from `regular-mode-config/skills.json`.
- **Removed**: `pnpm run update-skills` and `pnpm run build-skills-json` (edit `regular-mode-config/skills.json` in-repo or use `pnpm run dev:skills-editor`).
- **Fixed**: Default skills: **Fast** / **Fast — Non-western** use OpenRouter id `qwen/qwen3.5-flash-02-23`; older `qwen/qwen3.5-flash` / `openrouter/qwen/qwen3.5-flash` skill ids are aliased in the client to that id.
- **Fixed**: Regular mode: skill `model_id` values like `qwen/...` (OpenRouter slugs without the `openrouter/` namespace) are normalized before streaming so the server no longer returns 400 `Invalid model id` / `Unknown provider engine`.
- **Fixed**: Regular mode: 404/400 from a skill’s `model_id` no longer runs “remove from list + switch to free model” (that path is for Advanced `available_models` only); user sees a skill-specific error instead.
- **Fixed**: Release workflow: drop explicit `pnpm/action-setup` `version` so CI uses `package.json` `packageManager` (avoids mismatch with pnpm 10).

## 1.2.1 - 2026-05-03

- **Fixed**: Stop tracking `.translation-cache/cache.db.backup.*.sqlite` files (already ignored by `.gitignore`; remove from index with `git rm --cached` if they were added earlier).
- **Changed**: `.gitignore`: ignore everything under `.translation-cache/` except `cache.db` (same pattern as `translated-docs/.cache/`); doc translation backups live there by default, not under `translated-docs/.cache/`.
- **Changed**: `scripts/take-screenshots.js`: validate CLI then exit if `HISTORY_DISABLED` is true; validate `--screenshot=` / `--screen=` names before DB, config, server probe, or browser.
- **Changed**: `dev/DEVELOPMENT.md`: Related documentation section is a list instead of a wide table.
- **Changed**: Documentation: removed bold-wrapped inline code across Markdown docs; Cursor rule discourages that pattern in project documentation.
- **Fixed**: Settings (wide tab strip): selecting a section scrolls the horizontal strip so the active tab is fully visible.
- **Changed**: Translate, Rewrite, and Transform workspace footers: primary action button is horizontally centered in the main panel; Clear stays on the left.
- **Changed**: OpenRouter pricing lookup for non–OpenRouter providers: match model ids using normalized suffix comparison (spacing, hyphens, underscores) when exact suffix match fails, so estimates align across catalogs (e.g. `Qwen3 14B` vs `Qwen 3 14B`).
- **Added**: Environment variable `HISTORY_DISABLED` (`true`/`1`, case-insensitive): forces `keep_execution_history` off, disables execution-history storage and History settings controls, and shows “disabled by the administrator” on the History card (Electron main + web server).
- **Changed**: README (Configuration and environment), SYSTEM-OVERVIEW (execution history), and `.env.example`: document optional `HISTORY_DISABLED` for administrator lockdown.
- **Changed**: Settings → **General**: on wide viewports (`lg` and up), Appearance / Behaviour and History / Configuration Backup sit in a **2×2** grid with equal height within each row.
- **Changed**: History (web): removed the **User** filter; everyone including admins sees only their own execution history. `GET /api/calls/history` ignores `?username=` and scopes rows to the session user.
- **Fixed**: Web/Docker **Third‑party notices** modal: fetch the NOTICES URL using the same path prefix as the REST API (`getBasePath()`), not `new URL("NOTICES", document.baseURI)`, so hosts behind a reverse-proxy path (prefix without a trailing slash) load `/…/NOTICES` instead of `/NOTICES`.
- **Changed**: Standardised on **pnpm** only: removed `package-lock.json` and set `packageManager` in `package.json`.
- **Fixed**: Docker image: copy root `NOTICES` into the production stage so **Settings → About → Third-party notices** and `GET /NOTICES` work (previously only `dist/` was copied, so the file could be missing if the webpack copy did not run or match).
- **Fixed**: Translate / Rewrite / Transform header on narrow viewports: model selector and layout toggle sit on a second row below the title (Dashboard / History / Settings headers remain a single row).
- **Fixed**: Light theme: translate / rewrite / transform output panel text uses darker `--mode-output-text` overrides so body copy is readable; dark theme colors unchanged.
- **Changed**: Settings → **Models** (narrow inner tabs): hide the **Selected Models** panel title count badge; counts remain on the Available / Selected pill tabs.
- **Changed**: Settings → **Models** (wide layout): **Available Models** title row shows the total model count badge next to the title (same pattern as **Selected Models**).
- **Changed**: Settings → **Models** (wide layout): less padding on the scroll-end side of each column and a bit less list `padding-inline-end` so the scrollbar sits closer to the panel edge.
- **Changed**: Settings → **Models** (wide two-column layout): **Available Models** + search and **Selected Models** + **Deselect All** stay fixed at the top of each column; only the area below (filters, toolbar, lists) scrolls.
- **Fixed**: Settings → **Models** (wide split layout): removed duplicate vertical scrollbars by scrolling each pane (`overflow-y-auto` on left/right columns) instead of nested scrolling inside the model lists (`overflow-visible` list containers).
- **Added**: Settings → **Transform**: visible **Format:** label next to the JSON/CSV/XLSX export/import selector.
- **Fixed**: Dashboard **All Calls** and **By Model** expanded cards: **Model** full id aligns with other detail values (All Calls: Model is first row in `CallDetailsContent`; By Model: label + left-aligned value row with matching `gap`).
- **Fixed**: Dashboard, History, and Settings headers on narrow viewports: page title and interface language selector stay on one row (header uses a single horizontal flex row instead of stacking below `md`).
- **Fixed**: Settings → **Models** (narrow inner tabs): **Available** and **Selected** pill tabs stay on one row (`flex-nowrap`, equal `flex-1` tabs with truncation) instead of wrapping to two lines when horizontal space is available.
- **Fixed**: Settings → **Models** (narrow inner tabs): the scroll region is the area below the Available/Selected pills so filters, toolbar, and lists scroll together; removed nested list-only scrolling that hid filters and clipped cards.
- **Changed**: Settings → **Models**: below the `lg` breakpoint (1024px), inner pill tabs switch between **Available** and **Selected** instead of a vertically stacked split; wide layout remains side‑by‑side.
- **Fixed**: Settings **Users** tab: on narrow viewports the search field stacks above **Session Timeout** and **Add User** so the button is not cropped.
- **Changed**: Settings mobile section picker shows the same Lucide icons next to labels as the wide tab strip (trigger + menu items).
- **Fixed**: Settings mobile section picker: use Radix `Select` instead of a native `<select>` so the **open** options list uses themed popover styles (OS native option menus stayed light; closed state was already themed).
- **Changed**: Settings: below the `md` breakpoint, section navigation uses a full-width native select instead of a horizontal scrolling tab strip.
- **Changed**: History expanded card: metadata (`CallDetailsContent`) uses two columns from Tailwind `lg` up, one column below (mobile/narrow).
- **Changed**: History page uses the expandable card list at all viewport widths; removed the wide two-column layout (separate detail column with TextPanels).
- **Changed**: Dashboard Summary **#n Model** cards: model id uses the same color and weight as other KPI values (inherits summary value styling; no sky accent override).
- **Changed**: `scripts/take-screenshots.js` dashboard summary flow waits for `[data-testid="dashboard-tabpanel-summary"]` only; removed chart SVG / animation stability waits (dashboard no longer uses Recharts).
- **Changed**: Dashboard **By Model** expanded rows include the same **Model** + full model id header line as **All Calls** (above the translation/rewrite/transform breakdown).
- **Changed**: Dashboard sort by **Model** uses the compact **footer** id (`modelFooterDisplayId`: strip `openrouter/`, last `/` segment; `compareModelIdsByFooterDisplay` / matching SQLite expr), so ordering follows model tail names (e.g. gemma vs glm), not full path segments like `google` vs `z-ai`.
- **Added**: Dashboard **By Model** and **All Calls** desktop tables: clickable column headers toggle sort (arrow shows active column). **All Calls** uses server-side `ORDER BY` (whitelist); defaults remain **Total calls** descending and **ID** descending.
- **Fixed**: History card layout no longer clips horizontally: flex/grid min-width chain (`min-w-0`, `minmax(0,1fr)` columns) on main content wrappers and history list/detail columns; narrow accordion cards use full width within the viewport.
- **Changed**: Dashboard **By Model** default row order: **Total calls** descending (export matches); **All Calls** paginated list default: **ID** descending (was timestamp descending).
- **Changed**: Model selector trigger shows the current model's provider icon (`ProviderIcon`) instead of an orange status dot.
- **Changed**: Dashboard Summary KPI cards: column 1 is Total Cost, Models used, Translation, Rewrite, Transform; column 2 is Avg cost per call, Avg TPS, #1–#3 Model (paired row-by-row; empty top-model slots show a dash).
- **Changed**: Dashboard below `sm` (mobile card layout for **By Model** / **All Calls**): the main area under the app header scrolls as one page (filters, tabs, and cards) instead of only scrolling inside the tab panel. At `sm+` the previous nested scroll (tab panel; All Calls table body) is unchanged.
- **Changed**: History: the padded area under the header scrolls as one page (filters, download row, and history cards); the card list is the only layout at every width.
- **Changed**: Dashboard **By Model** and **All Calls** expanded detail panels use a distinct background (light sky tint; dark `#1a1f2e` with sky border) vs collapsed cards; desktop expandable rows use the same dark/light surfaces.
- **Changed**: Dashboard All Calls: collapsed Model uses compact id (`modelFooterDisplayId`); expanded block starts with a full-width **Model** + full id row (no rule under that line); mobile expanded metadata stays a single column (`CallDetailsContent` with `columnCount={1}`).
- **Changed**: Dashboard Summary KPI cards use a two-column grid (no three- or four-column breakpoints on wider viewports).
- **Changed**: Dashboard By Model uses expandable rows (All Calls pattern): collapsed row shows model, total calls, total cost, and avg TPS; expanded panel is a two-column table (`Translation Calls:` / `Translation avg. cost/call:` etc.). Mobile cards match the same collapse/expand behavior.
- **Changed**: Dashboard By Model lists the compact model label (`modelFooterDisplayId`, same as Summary `#n Model` cards); full model id is still available via tooltip (`title`).
- **Changed**: History: accordion cards (tap to expand/collapse); expanded body is one muted card with metadata, dividers, and plain input/output text (no duplicate type-summary header, no chars/words footer). Accordion body uses compact `11px` text, input/output in two columns under the metadata; outer history list scrolls. History list rows use `shrink-0` so sibling cards are not squashed when another row expands. CallDetailsContent uses `columnCount={1}` for history metadata.
- **Fixed**: Dashboard Summary Translation/Rewrite/Transform KPI values no longer show `[object Object]` for cost (`formatCost` returns JSX; template literals coerced it to a string). Percent share is right-aligned without a middle dot; top model cards show the last slash segment of the model id in sky-colored text with calls/share on the right.
- **Fixed**: Mobile nav hamburger vertical alignment with page headers (`size="icon-sm"` + `top-2` to match `AppHeader` `py-2.5` and title/icon row center).
- **Changed**: Dashboard sidebar and main header icon from bar chart to Activity (Lucide).
- **Added**: Dashboard Summary cards for the top three models by call count (`#1 Model` … `#3 Model`): model id, calls, and share of total calls (same filter as the rest of the dashboard).
- **Changed**: Dashboard Summary KPI cards for Translation, Rewrite, and Transform append each mode's share of total calls (by count) as a percentage after counts/cost.
- **Changed**: Dashboard simplified: removed "By Usage" and "By Day" tabs; Summary tab now shows only KPI cards (charts removed); By Model tab shows table only (chart removed) with compact column widths; By Model and All Calls tables transform to responsive card layout on narrow/mobile viewports; All Calls table is now full-width.
- **Changed**: Rewrite `StyleSelector` (underline trigger): horizontal `px-3` on the select so the current option text is not flush to the control edges (replaces `px-0`).
- **Fixed**: Dropdown menus (`DropdownMenuContent` / submenus) use `z-[10050]` so they stack above full-screen modals (`z-[10000]`), fixing model picker menus hidden behind Generate/Improve/Translate prompt dialogs.
- **Fixed**: Copy to clipboard when `navigator.clipboard` is missing or unusable (non-HTTPS, some Android browsers): centralized `copyTextToClipboard` tries `clipboard.writeText` then falls back to a temporary textarea + `execCommand("copy")`; wired through workspace copy actions, auto-copy, transform test panel, cost summary, and history-friendly `TextPanel` footer.
- **Fixed**: Web mobile layout: root shell and login use the **dynamic viewport** (`100dvh` / Tailwind `h-dvh` / `min-h-dvh`) instead of `100vh` / `h-screen` so the workspace action bar (Clear / Translate) stays in the visible area; `100vh` on mobile often extends under the browser chrome while `body` is non-scrollable, which hid the bottom bar. `main` content areas add `min-h-0` for a correct flex height chain.
- **Changed**: `dev/DEVELOPMENT.md` Linux prerequisites: inline shell/apt commands moved into fenced code blocks for copy-and-paste.
- **Changed**: `scripts/Load-DotEnv.ps1` prints one direnv-style summary line (`Load-DotEnv: export +VAR …`) listing variable names loaded from `.env` / `.env.local` (values are never shown).
- **Changed**: Local dev default port for Webpack (Electron `pnpm dev`) and the Express API in `pnpm dev:web` moved from **3030** to **4030** (avoids Windows excluded port ranges; proxy and scripts updated).
- **Fixed**: Punjabi UI language label showed `Panjabi&#160;/ Punjabi` literally — `&#160;` was an HTML entity stored in JSON; React renders plain text, so it never became a space. Source string and all locale keys are now `Panjabi / Punjabi` (see `ui-languages.json` `englishName`).
- **Changed**: Text selection: `body` defaults to selectable text (was `user-select: none` with only `p`/`span`/`textarea` enabled); form `Label` and Models provider headers no longer force `select-none`. Buttons, checkboxes/radios, dropdown/select items, and scrollbars stay non-selectable for clearer clicks.
- **Changed**: Settings About hero logo scaled down 15% (`h-24`/`sm:h-28` → `5.1rem` / `5.95rem`).
- **Changed**: Settings About and General: content column widened from `max-w-3xl` to `max-w-6xl` for more horizontal room when UI shows English plus a second locale; About app description drops `max-w-prose` so text uses the full card width.
- **Changed**: Settings About: unified `text-sm` body scale (`bodyTextCls`) for tagline, Legal, disclaimer, and repo link; section titles share one style; Source code is a flat section (heading + link row, no nested bordered panel); GitHub Invertocat SVG only on the section title (not repeated on the URL row).
- **Changed**: Settings About Legal card compacted (padding, gaps, merged license + rights line, smaller third‑party button); disclaimer and GitHub row slightly tighter.
- **Changed**: Settings About tab uses the same glass card stack as other settings (`settingsTabContent`, `settingsSection`), responsive hero (logo + title/version), About/Legal sections, disclaimer inset panel, and an interactive GitHub row; new strings extracted to `strings.json`.
- **Fixed**: `pnpm 3p-notices` on Windows: run `license-checker-rseidelsohn` via `node` and the package `bin/*.js` instead of `node_modules/.bin` (the Unix sh shim exists but is not executable by `execFileSync`).
- **Changed**: General settings layout: single centered column (`max-w-6xl`) — Appearance, Behaviour, History, and Configuration Backup are stacked full-width cards with uniform `gap-5`; “Delete history data” remains an inset panel inside History (no nested glass card).
- **Changed**: Settings UI aligned with workspace: `data-view="settings"` + emerald radial glow on `main`; glass header (`dark:bg-card/75`); pill-style tabs with emerald active state; section cards match workspace glass (`rounded-2xl`, `dark:border-white/10`); horizontal padding aligned with workspace grid (`md:px-6`); tables/stacked cards use matching borders.
- **Fixed**: Output header metrics (`workspaceOutputMetaClassName`): `min-w-[9rem]` + `flex-1` with `flex-wrap` on the row so metrics wrap to their own line instead of shrinking into a vertical character column; removed `overflow-wrap:anywhere`; row aligns `items-start` (Transform, Translate split/stack, Rewrite split/stack).
- **Fixed**: Transform prompt row (`TransformPromptSelector`): prompt dropdown uses `min-w-0` + truncated trigger text; action icons are grouped and wrap to their own row below `md` (`basis-full`) so they no longer paint over the select when the window or column is narrow.
- **Changed**: Stacked Rewrite layout moves **From:** language + elapsed/cost metadata into a row above the output panel (workspace top bar keeps Mode only); stacked Translate moves **To:** + metadata above the output panel so both match Transform’s output-column controls.
- **Fixed**: Stacked workspace layout (`WorkspaceGrid`) splits vertical space between input and output columns with `flex-1`/`min-h-0` instead of tall `min-h-*` constraints, avoiding a full-page scrollbar when both panes fit the viewport (Translate, Rewrite, Transform).
- **Changed**: Stacked translate workspace reuses `TextPanel` for input/output (matches split styling and borders); paste icon moved to the input stats row; duplicate clear removed from that row (clear remains in the bottom action bar); elapsed/cost/TPS meta uses the output card header like split mode (no accent “metrics strip”); single copy control in the stats row; circular bordered icon buttons aligned with split pane actions.
- **Fixed**: Collapsed sidebar expand strip uses the same Tailwind classes as the workspace action bar (`workspaceActionBarClassName`) so height and borders match; sidebar column uses `h-full` (not `100vh`) to track `MainContent`.
- **Added**: Sidebar collapsed/expanded preference persisted in `localStorage` (`transrewrt-sidebar-collapsed`).
- **Changed**: Stats row (Chars/Words/Paras + icon buttons) moved outside the card to sit directly below each `TextPanel`; added `hideFooter` prop to `TextPanel` to suppress the `CardFooter`.
- **Changed**: CTA button (Translate/Rewrite/Transform) and labelled "Clear" button moved to a dedicated action bar pinned at the bottom of the workspace (below both columns), spanning the full width.
- **Changed**: Output pane stats row shows model ID (muted, mode-accented) and an accent-coloured copy icon; rewrite "Changes" diff toggle moved from card footer into the output stats row.
- **Changed**: `WorkspaceGrid` accepts an `actionBar` slot rendered below the scrollable grid; `MainContent` and `App` thread the prop through all three workspace modes.

- **Added**: Per-mode CSS variables `--mode-output-bg`, `--mode-output-border`, and `--mode-output-text` for translate / rewrite / transform output panels; dark-mode `.output-tint` class on output `TextPanel`s.
- **Changed**: Workspace header (`AppHeader`) uses a single-row height on desktop (`md:min-h-14`).
- **Changed**: Model selector trigger is a compact pill (orange provider dot + monospace model id + caret); “Open Settings → Models” moved into the model dropdown menu.
- **Changed**: Interface language control uses a neutral pill style in the header (`rounded-full`, `border-white/10`, `bg-white/5`).
- **Changed**: Translate split layout shows elapsed / cost / TPS metadata beside the “To:” language row; output panel uses mode-tinted styling and no longer duplicates metadata in the card header.
- **Changed**: Pane footer actions (Clear / Paste / Copy) are icon-only with tooltips.
- **Changed**: Output footer model id line drops the “Model:” prefix and uses a muted mode-accent colour (`rgba(var(--mode-accent-rgb), 0.35)`).
- **Added**: Stacked translate workspace (`TranslateStackPanels.tsx`): unified From/To row with swap control; sidebar forces icon-only width while layout is stacked.
- **Added**: `swapLanguages` handler and `onSwapLanguages` option for translate workspace.
- **Fixed**: `swapLanguages` is declared after `sourceLanguage` / `targetLanguage` state so the app no longer throws on load (blank page from TDZ `ReferenceError`).
- **Fixed**: Docker/web production image build (`build-renderer`) by declaring `tailwindcss` as a devDependency so `@import 'tailwindcss'` resolves under pnpm’s strict `node_modules` layout.

- **Changed**: UI theme — border-radius increased to 16 px (`--radius: 1rem`) across all components for an ultra-modern look.
- **Changed**: Workspace action buttons (Translate / Rewrite / Transform) are now fully rounded (`rounded-full`) with a vibrant outer glow shadow matching the mode colour.
- **Changed**: Input/Output cards in dark mode now use glassmorphism (`backdrop-blur-xl`, semi-transparent `bg-card/75`, `border-white/10`).
- **Changed**: Sidebar now uses glassmorphism in dark mode (`backdrop-blur-xl`, `bg-card/80`).
- **Added**: Ambient radial glow behind the workspace shifts colour (green / blue / purple) based on the active mode via `data-mode` CSS cascade.
- **Added**: Blueprint grid background on the diff/Show-changes view in Rewrite mode.
- **Added**: Mode-coloured focus ring on text panels (uses `--mode-accent` CSS variable).
- **Added**: Layout Toggle (Split / Stack) segmented control in the workspace header; choice persists across sessions via `localStorage`.

- **Fixed**: Rewrite workspace top bar (`Mode` / `From` selectors) now wraps on mobile instead of overflowing horizontally.
- **Fixed**: Transform prompt selector no longer enforces a `200px` minimum width, preventing horizontal scroll on narrow screens.
- **Fixed**: Dashboard Summary and By Usage chart grids switch to a single column on mobile (`sm:grid-cols-2` breakpoint) so charts stack and are full-width instead of being squeezed into two tiny columns.
- **Fixed**: Dashboard chart cells gain mobile minimum heights (220–260 px) so charts remain legible before the two-column layout kicks in.

- **Changed**: Electron main-window size constraints now use shared constants in `src/main/main.js` so min/default dimensions are configured in a single place.

- **Fixed**: Sidebar navigation labels now force start/left alignment in expanded mode so multi-word translations (e.g., Greek/Ukrainian Dashboard) do not appear centered.

- **Fixed**: Theme parsing now normalizes legacy values like `System (Follow OS)` to `system`, so first-run Electron configs always resolve to OS-following theme behavior.

- **Fixed**: First-run default appearance theme now uses `system` (follow OS) with the correct lowercase value expected by renderer theme logic.

- **Fixed**: Restored missing build-time dependencies (`@babel/preset-typescript`, `postcss-loader`, `@tailwindcss/postcss`, `tw-animate-css`) so `pnpm build` resolves Babel/PostCSS/Tailwind modules correctly.

- **Fixed**: `scripts/take-screenshots.js` sidebar capture now slices top/bottom sections from the original image (240px each target), removes the middle safely, and avoids Sharp composite size mismatches during screenshot generation.

- **Changed**: Workspace language selectors (Translate/Rewrite/Transform From/To) and Settings > Languages list now display language names only in the selected UI language (e.g., "Espanhol" in pt-BR) instead of bilingual "Native / Translated" labels (e.g., "Español / Espanhol").

- **Fixed**: Language selector dropdowns now have a scrollbar when the list exceeds the viewport height (`max-h-80` with `overflow-y-auto`), allowing selection of languages that would otherwise be inaccessible.

- **Fixed**: Unified startup loading screen now uses theme-aligned background/foreground colors (light/dark) instead of a hardcoded dark gray, so refresh loaders match the app surface.

- **Fixed**: Hard-refresh startup now uses a unified loading screen in both `index.html` bootstrap and React `configLoading` state (same dark background, centered logo, message placement, and typography) to remove flashing between two different loader layouts.

- **Changed**: Renamed `THIRD-PARTY-LICENSES.txt` to `NOTICES`, renamed all components and functions from "Third-Party licenses" to "Third-Party Notices", and updated all references in code, scripts, and documentation.

- **Fixed**: `scripts/take-screenshots.js` UI language switching now uses real mouse clicks on visible header/menu element coordinates (instead of DOM `.click()`) and verifies the header locale label changed before capture.

- **Fixed**: `scripts/take-screenshots.js` now explicitly targets the header language trigger (`header [data-testid="language-selector-trigger"]`) and scopes locale option clicks to the language menu, avoiding accidental interactions with workspace language controls.

- **Fixed**: `scripts/take-screenshots.js` UI language switching now uses short bounded polling (fast-fail/skip) instead of long selector waits when the header language trigger is not visible, preventing repeated 10s+ stalls in screenshot runs.

- **Fixed**: `scripts/take-screenshots.js` now handles both legacy (`translate-to-option-pt-br`) and new UI (`translate-to-option-portuguese-br`) Translate target option test ids for stable screenshot capture after UI refactors.

- **Fixed**: ESLint now ignores `dist-main/` generated bundles so `pnpm lint` only checks source files.

- **Fixed**: Added graceful SQLite shutdown in Electron and web server to checkpoint WAL (`PRAGMA wal_checkpoint(TRUNCATE)`) and close DB handles on app/server exit.
- **Changed**: Enabled SQLite WAL mode and `synchronous = NORMAL` on app database open in both Electron and server runtimes to improve concurrent read/write performance.
- **Security**: Added pnpm overrides for `@xmldom/xmldom` (>=0.8.13), `uuid` (>=14.0.0), and `postcss` (>=8.5.10) to resolve 6 audit vulnerabilities (4 high, 2 moderate).

- **Fixed**: Settings → General → Appearance — font family and font size use separate non-wrapping rows so each label stays on the same line as its control on narrow screens.
- **Changed**: `dev/SYSTEM-OVERVIEW.md` tech-stack table and directory tree updated to reflect Tailwind v4 + shadcn/Radix (removed Fluent UI 9 / FluentProvider references); `dev/DEVELOPMENT.md` 3p-notices examples updated to use current packages.

- **Fixed**: Settings → General — Behaviour and History blocks were missing from the tab; restored. Sections order is Appearance, Behaviour, History, then Configuration Backup; one column on small screens, two columns from `md` up.

- **Changed**: Translate / Rewrite / Transform workspace header on small screens — UI language control (globe) sits on the same row as the mode title; model selector stays on the row below.

- **Changed**: Mobile navigation sheet (hamburger menu) closes automatically after choosing a sidebar destination or a user-menu action.

- **Fixed**: Settings page responsive layout on small screens — API key inputs and provider grid no longer force horizontal overflow; Models tab panes and header stack better; Cost, Users, and Transform prompts tabs use card layouts below `sm` instead of wide tables; Cost tab indents reduced on mobile.

- **Changed**: Removed the sidebar brand header bottom border and the separator line above the sidebar dashboard/navigation section; main panel `AppHeader` keeps a bottom divider.

- **Fixed**: `AppHeader` desktop height now matches the expanded sidebar brand header (`84px`) so the top divider aligns cleanly when the sidebar is open.

- **Changed**: Login and About pages now use the same shared app-name gradient text effect as the sidebar via `.app-name-gradient` in `styles/main.css`.

- **Changed**: Sidebar brand wordmark styles moved from inline `React.CSSProperties` in `Sidebar.tsx` to global stylesheet class `.sidebar-app-name` in `styles/main.css`.

- **Changed**: Sidebar brand block sizing/position restored to the legacy layout (`18px` gradient app name, centered logo stack, `24px` collapsed logo, original spacing).

- **Changed**: Sidebar app name (`Transrewrt`) restored to the previous multi-stop green→orange text gradient style in expanded mode.

- **Added**: Open **sidebar** logo/app name now toggles collapse on click; same brand area acts as expand/collapse toggle in both sidebar states.

- **Added**: Collapsed **sidebar** logo is now clickable; clicking the Transrewrt icon expands the sidebar (same behavior as the bottom expand button).

- **Fixed**: Collapsed **sidebar** logo — `img` is no longer squashed by flex (`shrink-0`, `object-contain`); logo row uses `px-2` when collapsed to match nav padding and free horizontal space.

- **Changed**: **StyleSelector** (rewrite **Mode:**) — `Palette` icon matches header `PenTool`: `h-5 w-5`, `text-blue-400`, `strokeWidth` 1.6 (replaces `size={18}` / default stroke).

- **Changed**: **LanguageSelector** — optional `iconClassName` / `iconStrokeWidth` so the leading Languages icon matches each workspace header accent (translate emerald, rewrite blue, transform purple; `strokeWidth` 1.6 like `MainContent` mode icons).

- **Changed**: Transform workspace — **From** language selector omits the visible **From:** label (`hideLabel`), matching rewrite; trigger `aria-label` unchanged.

- **Changed**: Workspace **output** panel footer — character stats on the first line, **Model:** on the second; model label uses `modelFooterDisplayId` (e.g. `openrouter/qwen/qwen3-235b-a22b-2507` → `qwen3-235b-a22b-2507`). Full id remains on hover (`title`).

- **Added**: **Rewrite** system prompt includes the workspace **From** language when it is not “Detect Language” (`prompts.json` `rewrite.withSourceLanguageLine`); API logging records `source_lang`.

- **Changed**: **Transform** — run UI uses **From:** (replaces **Target:**); optional per-prompt language is merged with workspace **From** into a single **stated From** value for `shared.transform.withSourceLanguageLine` (removed `translateToTargetLang` / output translation step). The From control matches translate/rewrite (`detectLanguage`, **Detect Language**). Prompt editor / Settings → Transform prompts table labels updated; DB column stays `target_language` (boolean). `apiService.transform` / `AppContext.transform` now take `(…, signal, statedFromLang)`; history transform summary reads `source_lang` for the bracket label.

- **Changed**: **HeaderLanguageSelector** — wider menu (`min-w-80`, up to `28rem`, capped by `calc(100vw - 2rem)`); language trigger uses `size="sm"` instead of `icon-sm` so width follows icon + locale text (no fixed `32px` square).

- **Fixed**: **ModelPickerRow** — when the provider icon is hidden (header trigger), the name sat in the first grid track (`auto`) so long IDs never ellipsized; use `grid-cols-[minmax(0,1fr)_auto]` in that case, plus `min-w-0 truncate` and `overflow-hidden` on the trigger chain.

- **Changed**: **AppHeader** — below `md`, title and controls stack in two rows (`flex-col`) so the model strip and language control do not overlap; from `md` up, single `h-14` row with `justify-between`. Header actions use `gap-x-8` / `gap-y-2` (space between model selector and language control). **ModelSelector** root uses `min-w-0`, `flex-wrap`, and a shrinkable model trigger on small widths (`sm:min-w-[200px]`).

- **Changed**: **TextPanel** footer stats use `11px` (`text-[11px] leading-snug`) and wrap (`break-words`, no `truncate` / `whitespace-nowrap`) in narrow columns; **Show changes** label stays `whitespace-nowrap`.

- **Fixed**: Workspace translate/rewrite/transform — left/right **TextPanel** cards share row height (`MainContent` grid `items-stretch`, `h-full` columns, `minmax(0,1fr)`); run-button band uses `workspaceCtaRowClassName` (`60px`); **TextPanel** footers use fixed `h-[60px]` + `flex-nowrap` (`workspacePanelFooterLayoutClassName`); stats + model string uses a `·` separator and wraps within the footer band instead of truncating with an ellipsis.

- **Added**: Rewrite workspace — **From** language control (Detect Language + list) on the same row as **Mode:**; full-width toolbar above the grid; content-sized selects (`hugSelectWidth`); visible **From:** text hidden in that bar only (`LanguageSelector` `hideLabel`, trigger `aria-label` unchanged). Shared `source_language` with translate.

- **Changed**: Model selector — current model label (`ModelPickerRow`) uses `text-sm` and tighter line-height so long model IDs match header typography.

- **Fixed**: Light theme — `index.html` splash CSS set `color` / `background` on `html, body` outside any `@layer`, which overrode Tailwind `@layer base` and left inherited text white on light `bg-background` (settings and other views). Scoped splash colours to `.app-loading` only; improved light `--muted-foreground`, switch thumb contrast, and General → font preview sample text colour.

- **Changed**: CSS consolidation — shared settings table/layout class modules under `components/settings/`; Settings → Models uses `settingsModelsLayoutClasses.ts` instead of global BEM in `main.css` (global stylesheet reduced to app shell only); dashboard styles live in `components/dashboard/dashboardPageStyles.ts` with `DashboardExportToolbar` for repeated export controls; removed `DashboardPage-styles.ts` and `useStyles` shim.

- **Fixed**: Light theme readability — replaced hardcoded dark greys and invalid `hsl(var(--…))` on oklch tokens in `main.css`, settings tables, web loading frame, prompt selector buttons, and dashboard scrollbar styling.

- **Added**: Settings → General → Appearance — **Theme** selector (Light / Dark / System). "System" follows the OS `prefers-color-scheme` and updates live when the user switches the OS theme.

- **Changed**: Full UI migration — replaced Fluent UI with Tailwind v4 + shadcn/Radix primitives across all renderer components; added TypeScript support (tsconfig, ts-loader, `@/` alias); self-hosted Geist font; responsive shell with collapsible desktop sidebar and mobile Sheet; responsive `lg:grid-cols-2` workspace panels; all settings, dashboard, history, login, and modal views converted.
- **Removed**: `@fluentui/react-components`, `@fluentui/react-icons` dependencies and `useAppStyles` hook; dead CSS rules from `main.css` (Fluent token references, legacy panel/modal/sidebar classes); `ResizablePanels.js` replaced by CSS grid.
- **Fixed**: `costUtils.ts` renamed to `costUtils.tsx` — babel-loader was failing with "Unterminated regular expression" because the file contains JSX but had a `.ts` extension.

- **Changed**: Windows NSIS - removed `preInit` silent uninstall of any existing “Transrewrt” listing; upgrades replace files in place under the same install directory (electron-builder’s default behaviour).
- **Fixed**: Windows NSIS - `customCheckAppRunning` uses `Function` vs `Function un.TR_CheckAppRunningImpl` per `makensis` pass (`GetProcessInfo` / `un._GetProcessInfo`), and the running-app logic uses only `StrCmp` / `IntCmp` (no `${if}` / LogicLib) so it compiles in electron-builder’s sharedHeader, fixing `ERR_ELECTRON_BUILDER_CANNOT_EXECUTE` / `Invalid command: "${if}"`.
- **Fixed**: Windows NSIS - `updated` / upgrade detection uses a `$CMDLINE` substring check instead of `StdUtils::TestParameter` so the uninstaller `makensis` pass succeeds (StdUtils is not loaded there); matching `un.TR_TR_HasUpdatedCmdline` / `TR_TR_HasUpdatedCmdline` for valid `Call` targets; `DetailPrint` avoids `$(PRODUCT_NAME)` (warning 6040 under `-WX`).
- **Fixed**: Windows NSIS - `installer.nsh` helper `!macro`s use `$$` for runtime vars inside `nsExec` strings, `Pop $R6` / `$R7` (never `$0`–`$9` inside user macros), and global `$PsChk` for the PowerShell probe so makensis does not hit warning 6000 under `-WX`.
- **Changed**: Settings → Models - **Expand All** / **Collapse All** toolbar buttons use Lucide **UnfoldVertical** and **FoldVertical** icons (provider sort mode).
- **Fixed**: Windows NSIS installer - `customCheckAppRunning` in `build/installer.nsh` detects only `Transrewrt.exe` whose `ExecutablePath` matches the install directory (not every process under `$INSTDIR`, and not other copies of the same exe elsewhere), avoiding false “app is running” loops from electron-builder’s stock `Path.StartsWith($INSTDIR)` / per-user `tasklist` fallback.
- **Changed**: Settings → About - **Apache 2.0** link opens the repository `LICENSE` on GitHub (`…/blob/main/LICENSE`) instead of apache.org.
- **Changed**: Settings → About - **Third‑party licenses** link shows a Lucide `ScrollText` icon beside the label.
- **Changed**: Web configuration backup export - `files/config.json` in the ZIP merges preferences from the `admin` user (username match, else `role = admin` with most recent `last_login`) so Electron import receives models, top languages, and other per-user settings; `web_session` / `web_session_expires_at` are omitted from the export.
- **Fixed**: Web backup `files/config.json` - per-user keys such as `available_models` no longer get replaced by the on-disk server `config.json` defaults; only server-global keys (API keys, `web_session_timeout`) keep values from the file/env layer.
- **Changed**: Configuration backup download filename timestamp uses **local** wall-clock time (browser when saving from web; server or desktop machine timezone for `Content-Disposition` / Electron save dialog) instead of UTC.
- **Added**: `dev/RELEASE-NOTES-v1.2.1.md` with user-facing notes for the 1.2.1 GitHub release.

## 1.1.1 - 2026-04-01

- **Fixed**: Electron packaged builds (including Linux AppImage) - **Third‑party licenses** resolves `NOTICE` from the app content root (`extraFiles` location, sibling of `resources/`) and from `dist/` inside the asar; the previous paths only checked `resources/` and `app.asar/…` at the archive root, so the file was never found when shipped via `extraFiles`.
- **Fixed**: Settings → About - repository URL link uses `openExternalUrl` (system browser in Electron, new tab in web) like the Apache 2.0 license link.
- **Changed**: `dev/DEVELOPMENT.md` - document `pnpm clean-logs` (translation session logs); `scripts/clean-workspace.sh` / `scripts/clean-workspace.ps1` run `scripts/clean-translation-logs.js` before removing build artifacts. Fixed missing comma in `clean-workspace.ps1` artifact list.
- **Changed**: Renamed `scripts/clear-translation-logs.js` to `scripts/clean-translation-logs.js` (`pnpm clean-logs` unchanged).
- **Added**: `scripts/sync-i18n-md-script-sources.js` and `pnpm run i18n:sync-md-annex` - refresh the `extract-strings.js` / `generate-translations.js` source annex in `dev/i18n.md` from the repo scripts.
- **Changed**: `dev/DEVELOPMENT.md` - document `pnpm run i18n:sync-md-annex` in the UI i18n sections.
- **Changed**: `dev/DEVELOPMENT.md` - added **Document translation (`translate:docs`)** section: `translate.config.json`, `glossary-user.csv`, UI glossary from `strings.json`, CLI flags (`--locale`, `--force`, `--force-update`), and cleanup (`translate:cleanup` / `cache-cleanup.ts`).
- **Changed**: `dev/DEVELOPMENT.md` - align with current scripts: Windows/PowerShell upgrade commands; `pnpm translate:docs` → `scripts/translate/index.ts` vs legacy `translate-docs.js`; Docker (`transrewrt` container name); remove `docker:deploy` / missing `docker-deploy.sh` and broken `devel_cross_compile_docker_deploy.md` link; GHCR/Raspberry Pi deploy notes; `translate.config.json` in key files.
- **Changed**: Electron `appDb` - log database path only when `NODE_ENV === "development"` (quiet production startup).
- **Changed**: `dist-main/` is gitignored and no longer tracked - Electron main/preload webpack output from `pnpm run build:main` only; CI and `pnpm package` / `package-arm64` already run that step before electron-builder.
- **Changed**: Renderer production build - `NOTICE` is copied to `dist/` via `CopyWebpackPlugin` in `webpack.config.js` instead of a post-build `copyFileSync` one-liner in `build` / `build-renderer` scripts.
- **Added**: `pnpm run package-arm64` - production build and Linux **arm64** AppImage (same steps as CI) via `build/electron-builder.linux-arm64.cjs`.
- **Fixed**: Linux **arm64** AppImage crashed at startup (`Cannot find module 'brace-expansion'`, `minimatch` via `multi-llm-ts`): `--config build/electron-builder.linux-arm64.json` did not merge `package.json` `build`, so `extraMetadata.main` was missing and the app ran `src/main/main.js` instead of the webpack main bundle. Replaced with `electron-builder.linux-arm64.cjs` that spreads `package.json` `build` and overrides `linux.target` to arm64; release Linux jobs run `pnpm run build:main` like Windows packaging.
- **Changed**: Electron Linux packaged builds - suppress Node deprecation warnings when `NODE_ENV` is not `development` (quieter AppImage/console startup from transitive `punycode` use).
- **Added**: `TRANSREWRT_DISABLE_GPU=1` - disables hardware acceleration so systems with broken GLES/EGL avoid noisy Chromium GPU-init errors (optional; all platforms).
- **Changed**: README - Linux AppImage quick start and installation notes on console deprecation suppression and `TRANSREWRT_DISABLE_GPU=1`.
- **Fixed**: README / `dev/DEVELOPMENT.md` - Linux Electron/AppImage dependency line uses `libnotify4` (runtime) instead of `libnotify-dev`; notes on minimal `.so` deps, FUSE/AppImage extract-and-run.
- **Fixed**: Transform workspace - empty prompt list: **Load sample prompts** is placed after the prompt row icon buttons (24px gap) so it no longer overlaps the dropdown on narrow layouts.
- **Fixed**: Web/Docker third-party notices - fetch uses a URL relative to `document.baseURI` (matches production `./` public path and reverse-proxy path prefixes); production webpack copies `NOTICES` into `dist/` and the server resolves that path so the file ships with the static bundle (Docker no longer needs a separate root copy).
- **Changed**: `scripts/translate/index.ts` - no longer prints per-locale `Files: … processed … skipped` and `Segments: … cached … translated` lines (aggregate summary unchanged).
- **Changed**: Settings → Appearance - default (Windows/macOS) monospace presets use **Courier New** instead of **Menlo** so the third choice is visually distinct in the browser (Menlo often substituted to the same face as Consolas / `ui-monospace` on Windows web). Saved **Menlo** still resolves with a `Courier New` fallback stack.
- **Fixed**: `license-clarifications.json` - `@epic-web/invariant` ships no `LICENSE` on npm (checker used `README.md`); clarification supplies standard MIT text so `NOTICE` no longer embeds the full readme.
- **Fixed**: `license-clarifications.json` - more packages that resolved to `README.md` or a missing `LICENSE` (nested `@jsonjoy.com/json-pointer@1.x` → root `LICENSE`; `esrecurse`, `glob-to-regex.js`, `glob-to-regexp`, `html-parse-stringify`; SPDY-related `spdy`, `spdy-transport`, `handle-thing`, `hpack.js`, `http-deceiver`, `select-hose`, `wbuf`; `embla-carousel`, `embla-carousel-autoplay`, `embla-carousel-fade`) now emit plain license text in `NOTICE`.
- **Fixed**: Linux clients - Settings → Appearance **Font family** uses a Linux‑oriented preset list in **Electron on Linux** and in the **web app when the browser reports desktop Linux** (userAgent / `navigator.platform`), so presets match typical fontconfig fonts instead of missing Segoe UI, Consolas, Menlo, etc.; default effective font is `system-ui` on Linux. Windows/macOS (Electron or web) keep the original list. Panel text uses CSS stacks with `sans-serif` / `serif` / `monospace` fallbacks (`resolveAppearanceFontFamilyCss`).
- **Added**: Preload `electronAPI.getRuntimePlatform` for renderer platform detection.
- **Added**: Settings → About - **Third‑party licenses** opens a modal with file contents (scrollable; monospace `pre-wrap` text with long lines wrapped, vertical scroll only); desktop loads via main-process read, web/Docker via `GET /NOTICE`; webpack dev serves the repo file on that path.
- **Added**: Settings → About - when the app license is Apache-2.0, **Apache 2.0** links to `http://www.apache.org/licenses/LICENSE-2.0` and opens in the system browser (Electron `openExternalUrl`) or a new tab (web).
- **Changed**: Third‑party notices modal - after each `---` separator, the next four lines (blank, package + version, license type, first body/copyright line) use blue text (`colorPaletteBlueForeground2`).
- **Changed**: Renamed `scripts/write-third-party-licenses.js` to `scripts/write-third-party-notices.js` (`pnpm run 3p-notices`).
- **Changed**: `pnpm run 3p-notices` output prefixes each dependency block in `NOTICES` with `---` and a blank line.
- **Changed**: Renamed `THIRD-PARTY-NOTICES.txt` to `NOTICES` (`3p-notices` output, Docker `/app/NOTICES`, Electron `extraFiles`, docs).
- **Changed**: Settings > General: turning off **Keep execution history** opens the data-deletion confirmation only when at least one execution-history row exists; otherwise the setting turns off immediately. `GET /api/calls/history` and desktop `getExecutionHistory` accept optional `limit` (capped at 500) for lightweight checks.
- **Fixed**: Docker image - run `pnpm rebuild better-sqlite3 argon2` after `pnpm prune --prod` so native SQLite/argon2 bindings are present in the copied `node_modules` (avoids "Could not locate the bindings file" on Linux/arm64 Alpine).
- **Added**: `pnpm clean-logs` - `scripts/clean-translation-logs.js` removes translation session logs under `dev/` and `paths.log-folder` (`generate-translations-*.log`, `translate-docs_*.log`, `translate-docs-blocks_*.log`) and cache-backed artifacts under `paths.cache` (`cache-*.db` backups, `cleanup_*.log`, `debug-traffic-*.log`); does not delete `cache.db` or `dev/translations.log`.
- **Changed**: `.gitignore` - `translated-docs/.cache/cache.db` is tracked; other files under `translated-docs/.cache/` (logs, backup DBs, etc.) stay ignored.
- **Changed**: `scripts/translate/config.ts` - default OpenRouter config uses `translationModels` (ordered fallback chain) aligned with `translate.config.json`; default `maxBatchChars` 4096; removed unused `paths.staticImg` default and type field.
- **Fixed**: `scripts/translate/index.ts` - `rewriteDocLinksForI18nOutput` was producing `]](url)` for every markdown link (double `]`) due to `textPart` already including the closing `]` while the return template also prepended `](`.
- **Fixed**: `scripts/translate/index.ts` - `maybeSyncLanguageListInSource` locates the language-list block in the markdown body after frontmatter only (same scope as translated post-process), not raw file lines that include YAML.
- **Fixed**: `scripts/translate/index.ts` - language-list block is located line-by-line (`language-list-block.start` / `.end`) and replaced in place (no cross-line regex); then `rewriteDocLinksForI18nOutput` runs on the full body.
- **Added**: `scripts/translate/additional-adjustments.ts` - shared `additional-adjustments` helpers used by doc post-process.
- **Changed**: `scripts/translate/index.ts`, `config.ts`, `types.ts` - doc post-process uses config-driven link rewriting (`paths.docs` / `paths.i18n` depth, `paths.source-files` basenames): strip `paths.i18n`-relative prefix, point other configured source docs at `<stem>.<locale>.<ext>`, prefix remaining relative paths with `../` × depth (handles `paths.i18n` same as `paths.docs` as depth 0). Language switcher block is generated from `locales.targets` (`ui-languages.json`) + `language-list-block` in `translate.config.json` (custom `start` / `end` / `separator`); source files are updated only when the block text changes (preserves mtime); translated output replaces the block on the **full** reassembled markdown body before `rewriteDocLinksForI18nOutput`. Removed hardcoded README/USER-GUIDE and per-locale screenshot path swapping.
- **Added**: `translate.config.json` / `types.ts` / `config.ts` - optional `additional-adjustments`: named rules with regex `search` and `replace` on the **full** translated markdown body (frontmatter content only), before link-depth rewriting; supports `${sourceLocale}`, `${translatedLocale}`, `${sourceFullPath}`, `${sourceFilename}`, `${sourceBaseName}`, `${sourceExtension}`, `${translatedFullPath}`, `${translatedFilename}`, `${translatedBaseName}`, `${translatedExtension}`, `${sourceBasedir}`, `${translatedBasedir}`. Default config includes screenshot folder normalization via `sourceLocale`.
- **Fixed**: `scripts/translate/splitter.ts` - fenced code blocks delimited with `~~~` (as well as ` ``` `) are non-translatable, matching CommonMark-style fences (3+ backticks or tildes).
- **Changed**: `scripts/translate/index.ts` - OpenRouter debug traffic log is written only when `--debug-traffic` is passed (no longer default-on; removed `--no-debug-traffic`).
- **Added**: `scripts/translate` - URL count mismatch validation triggers single-segment re-translation from the next configured OpenRouter model(s) until counts match or models are exhausted (`validator` structured warnings, `Translator.translate` `startModelIndex`, `segmentMarkdownUrlCountsMatch`).
- **Fixed**: `scripts/translate/cache-cleanup.ts` - aligned with Transrewrt: reads `paths.source-files`, `paths.i18n`, `paths.cache`, and `locales.targets` from `translate.config.json`; builds segment snapshot via `DocumentSplitter` like `translate:docs`; removes orphaned `stem.<locale>.md(x)` under `paths.i18n` (skips `.cache/`); no `OPENROUTER_API_KEY` required; pre-run hint uses `pnpm translate:docs --force-update` for `last_hit_at`.
- **Changed**: `scripts/translate/file-utils.ts` - shared `expandSourceFilePatterns` / `expandDocSourceFiles` / `buildExistingSegmentsSnapshot` for CLI and cache cleanup.
- **Changed**: `package.json` - `translate:clean` / `translate:cleanup` run `translate:docs --force-update` before `cache-cleanup.ts` (refresh segment hits before stale-row deletion).
- **Added**: `scripts/translate/index.ts` - `--force-update` re-processes docs when `file_tracking` matches the source hash, while still using the segment translation cache; `--force` and `--force-update` cannot be used together.
- **Changed**: `scripts/translate/translator.ts` - system prompts include explicit markdown/GFM preservation (emphasis, inline code, links/images, tables); no instructions for fenced code blocks (splitter keeps ``` segments non-translatable, so they are not sent to the model).
- **Fixed**: `scripts/translate/index.ts` - post-process repairs lines that open with `` but have no closing `` (common doctoc TOC title glitches after translation).
- **Changed**: `scripts/translate/translator.ts` - OpenRouter model fallback warnings use the same indent as other doc-translate lines, include `locale` and filename, name the next model, and use a shorter batch-mismatch message (no misleading “single-segment” wording when retrying another model).
- **Changed**: `scripts/node-rebuild.js` - quiet on success (exit 0); stderr only on failure (exit 1). Skips `pnpm rebuild` when `better-sqlite3/build/Release/.transrewrt-system-node.json` (written after a successful run) matches `NODE_MODULE_VERSION` and the SHA-256 of `better_sqlite3.node`, so an Electron rebuild changes the binary and forces a rebuild without `require()`-ing the addon.
- **Fixed**: `scripts/translate` - `--locale` accepts multiple codes separated by commas and/or spaces (`pt-BR,es,fr`); previously the whole string was treated as one locale and `normalizeLocale` mangled values after the first `-`.
- **Changed**: `scripts/translate/index.ts` - summary prints wall-clock **Total elapsed time** as `MM:SS`; per-locale time uses `MM:SS` instead of decimal seconds.
- **Changed**: `scripts/translate/index.ts` - progress uses line-by-line logs (📦 cache hit, ✔️ segment/batch done with token counts) like `translate-docs.js`, instead of a single `\r` status line that stayed on “translating” until the file finished.
- **Added**: `scripts/translate` - `-j` / `--concurrency` (max parallel target locales, default 3) and `-b` / `--batch-concurrency` (max parallel translation API calls per locale per file: one call = a segment batch, or one segment with `--no-batch`, default 4); optional `concurrency` / `batchConcurrency` in `translate.config.json`. Helpers in `scripts/translate/concurrency.ts` (`runMapWithConcurrency`, `AsyncSemaphore`).
- **Fixed**: `scripts/translate/index.ts` - `--force` skips segment cache reads (not only `file_tracking`); previously identical segments still hit `translations` and the file looked “unchanged” with no API calls.
- **Fixed**: `scripts/translate/index.ts` - `--help` / `--version` no longer throw `CommanderError` after printing; `exitOverride` exits with Commander’s code so `pnpm translate:docs --help` returns 0.
- **Added**: `pnpm run rebuild:node-native` - runs `scripts/node-rebuild.js` to rebuild `better-sqlite3` for **system** Node (needed for `pnpm run translate:docs` / `tsx` after `postinstall` targeted Electron’s Node ABI).
- **Added**: `scripts/translate/anchor-placeholders.ts` - protects `<a id="…"></a>` and Docusaurus `{#…}` heading ids from translation; wired into `index.ts` after admonitions, before URL placeholders; restore after URLs, before admonitions.
- **Changed**: `scripts/translate/index.ts` - segment pipeline protects Docusaurus/GFM admonitions before markdown URLs and restores URL placeholders before admonition lines after translation (batch, fallback, and single-segment paths).
- **Changed**: `scripts/translate` - CLI session log (`setupLogOutput`) uses `paths.log-folder` / `paths.logFolder` from `translate.config.json` (default `.translation-cache` when unset).
- **Changed**: `scripts/translate/translator.ts` - OpenRouter chat completions include `provider: { sort: "throughput", allow_fallbacks: true }` (same routing as `openrouter-script-models.js`) for faster provider selection.
- **Changed**: `scripts/translate` - `paths.ui-glossary` may point at `strings.json` (same format as `i18n:extract` / `i18n:translate`); user glossary stays CSV; `openrouter.translationModels` is an ordered fallback chain (replaces primary+single fallback). `Translator` and `Glossary` (parent package) updated accordingly. User-glossary `locale: *` expands only over `locales.targets` (no hardcoded locale list).
- **Added**: `scripts/translate/index.ts` - segment-based markdown translation from `translate.config.json` `paths.source-files` (globs supported) to `paths.i18n` as `<name>.<locale>.md`, with cross-doc link fixes and lang-list injection from the legacy `translate-docs.js` pipeline; dev script `pnpm run translate:docs-md`. Dev dependencies: `chalk`, `gray-matter`, `glob`.
- **Changed**: `scripts/translate/config.ts` - resolves `locales.targets` from a JSON file path (e.g. `ui-languages.json`), maps `paths.ui-glossary` / `paths.user-glossary` and `paths.source-files`, exports `normalizeLocale`; optional `paths.jsonSource` validation only when set.
- **Added**: Translate admonition placeholders (`scripts/translate/admonition-placeholders.ts`) - protect [GitHub GFM alert](https://docs.github.com/en/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax#alerts) lines (`> [!NOTE]`, `> [!TIP]`, etc.) from translation alongside Docusaurus `:::` directives.
- **Removed**: `scripts/translate/duplistatus-translate/` - doc translation is standalone; `ignore.ts`, `file-utils.ts`, and `log-output.ts` live under `scripts/translate/`. Locale display names for prompts come from `ui-languages.json` via `locales.displayNames` (no hardcoded locale lists in defaults).
- **Added**: `scripts/upgrade-dependencies.ps1` - PowerShell counterpart to `upgrade-dependencies.sh` (ESLint peer check + `ncu`, `pnpm install` / audit); toolchain steps live in `upgrade-tools.ps1`.
- **Changed**: `scripts/upgrade-dependencies.sh` and `scripts/upgrade-dependencies.ps1` source `upgrade-tools` (`upgrade-tools.sh` / dot-source `upgrade-tools.ps1`) for nvm/Node/globals; `TRANSREWRT_UPGRADE_TOOLS_SUPPRESS_DONE` skips the tools script’s trailing `Done.` until project steps finish. `upgrade-tools.ps1` restores prior `$ErrorActionPreference`, supports optional nvm git-dir upgrade and `nvm use lts` fallback (aligned with the bash flow), and `TRANSREWRT_UPGRADE_TOOLS_EMBEDDED` adjusts the nvm tip when run from dependencies.
- **Fixed**: `scripts/translate-docs.js` - `--block-size` sub-split no longer breaks GFM pipe tables mid-row or fenced code blocks (``` / ~~~); whole tables and fences stay in one block so rejoined output does not corrupt structure.
- **Added**: Docker image - `tzdata` in the Alpine production stage, default `TZ=Europe/London`; Compose files pass `TZ=${TZ:-Europe/London}`; README documents overriding `TZ` for server-side time.
- **Changed**: `scripts/take-screenshots.js` - before inserting a history sample translate row, reads execution history (same filters/order as the History page) and **skips insert** when the first **translate** row already has the canonical sample `input_text`, avoiding duplicate noise; otherwise inserts with the current timestamp so that entry is selected first.
- **Changed**: `scripts/take-screenshots.js` - `--screenshot=` / `--screen=` accept **multiple** set names (comma- or space-separated); help lists all configured sets and counts them; log line **Screenshot set:** includes **N/total** (e.g. `translate (1/12)`).
- **Changed**: `scripts/take-screenshots.js` - transform screenshots use **Dictionary Entry** everywhere: **transform** / **transform-generate** prompt selection, DB seed (`ensureDictionaryEntryPrompt` from `transform-prompts.json`), replacing the former Rewrite with Synonyms ensure and slugs.
- **Fixed**: Windows `pnpm dev` / `electron-rebuild` failing with `EBUSY` on `better-sqlite3\\build`: `scripts/electron-rebuild.js` removes that folder first (retries with delay) and prints a clear hint if the directory stays locked.
- **Changed**: Removed unused `pnpm.overrides` entries (`lodash-es`, `serialize-javascript`) that did not affect the lockfile.
- **Changed**: Rewrite → Check Spelling & Grammar - output footer **Show changes** is a switch (on/off) instead of a button.
- **Fixed**: That switch uses Fluent `size="small"` and footer-sized label text so it aligns with **Paste** / **Copy** (`TextPanel.js`).
- **Changed**: `scripts/translate-docs.js` splits source at markdown headings (ignoring `#` inside fenced code blocks), sub-splits long sections at paragraph boundaries (`--block-size`, default 1024), caches per-block translations under `translated-docs/.cache/<locale>.json` (pretty-printed JSON, entries per doc sorted by block `id`; each entry includes `id`; cache hits refresh `id` on save), prunes stale cache entries after each run, and writes front matter `translated_at` / `model` from the newest block time and comma-separated models used.
- **Added**: Settings → General → Configuration Backup - optional **Include usage data in the backup** (serializes `api_calls` and `action_content` into the ZIP). Restore modal adds **Restore the usage data** and renames the clear option to **Clear the old usage data before restoring** (web + desktop).
- **Fixed**: Packaged Electron app crashed at startup with `Cannot find module 'event-target-shim'` (and previously `balanced-match`/`brace-expansion`). Root cause: electron-builder's pnpm node-module collector misses some transitive dependencies. Fix: bundle the entire main process with webpack (`webpack.main.config.js`), embedding all JS dependencies into `dist-main/main.js`; only native addons (`better-sqlite3`) remain external. Removed workaround direct deps (`balanced-match`, `brace-expansion`). Added `.npmrc` with `node-linker=hoisted` as defense-in-depth.
- **Changed**: Main process IPC files use static `require("../../shared/llm")` instead of dynamic `require(path.join(__dirname, ...))` for webpack compatibility.
- **Changed**: Icon paths in `main.js` use `app.getAppPath()` instead of `__dirname`-relative paths, working correctly in both dev and bundled production.
- **Fixed**: Windows NSIS welcome/finish sidebar: `images/installerSidebar.bmp` is **164×314** and uses a classic **40-byte** `BITMAPINFOHEADER` (24-bit uncompressed). It had been **162×314** and GIMP/export had written a **124-byte** BITMAPV5 header; wrong size and/or extended DIB headers often make `MUI_WELCOMEFINISHPAGE_BITMAP` show as blank in the wizard.
- **Fixed**: Web configuration restore never writes LLM provider keys into `config.json` (web and Electron-origin ZIPs), so environment-based keys keep working and backups that embed keys do not lock the server to stale file values.
- **Changed**: Web configuration backup (`GET /api/config/backup`) embeds resolved LLM provider keys in `config.json` (file values plus environment fallbacks via `mergeKeys`), matching desktop backups that include keys from disk.
- **Changed**: Settings → General - restore backup opens a confirmation modal first; ZIP is chosen inside the modal (browse + filename), then **Restore** runs. Desktop uses `getPathForFile` + existing IPC `filePath` path instead of a second native open dialog after confirm.
- **Changed**: OpenRouter chat completions use `provider` routing (`sort: throughput`, `allow_fallbacks: true`): app via `src/shared/openRouterProviderRouting.js`; CLI scripts via `OPENROUTER_PROVIDER` in `scripts/openrouter-script-models.js`.
- **Changed**: `scripts/generate-translations.js` - CLI flag `--retranslate`/`-r` renamed to `--force`/`-f` (translate all strings, ignoring existing entries).
- **Changed**: `scripts/generate-translations.js` - unique source strings pending translation are logged to the **console** (and session transcript) after `done`, before the totalizer (not to `dev/translations.log`). `dev/translations.log` still receives one cost/summary line per run when work was done. Full console output (log/warn/err) is copied to `dev/generate-translations-YYYYMMDD-HHMMSS.log` per run.
- **Changed**: Settings → General - after configuration backup, the success line shows the archive file name (`Backup generated: …`) using `{{filename}}` in i18n.
- **Changed**: Configuration backup default download filename includes UTC time (`transrewrt-config-backup-YYYY-MM-DD_HHMMSS.zip`) so same-day exports do not collide.
- **Fixed**: Merge import of transform prompts (file + load sample): duplicate names are resolved case-insensitively (`Name (1)`, …); failed `getAll` before import no longer falls back to an empty list (which could trigger merge UPDATE and overwrite existing rows).
- **Added**: Settings → Transform - **Load sample prompts** on the export/import row (after format dropdown), always visible; same confirmation and merge import as the Transform workspace button.
- **Fixed**: Transform mode delete-prompt confirmation in the main workspace (`App.js`) substitutes `{{name}}` with `interpolateTemplate` (key-as-default i18n does not apply i18next interpolation).
- **Fixed**: Transform (and other) text panels - long `headerMeta` text (e.g. prompt instructions beside **Input**) truncates with an ellipsis within the panel instead of overflowing into the adjacent column; full string shown on hover when meta is plain text (`TextPanel.js`).
- **Changed**: Default transform prompts in `transform-prompts.json` - clearer Summarise, Dictionary Entry, and Markdown-to-CSV instructions (typos fixed, RFC 4180 CSV rules, hallucination guards for IPA and examples).
- **Fixed**: After saving a **new** transform prompt from the Prompt editor, the Prompt dropdown now keeps the new prompt selected (avoid a one-frame mismatch between selection and list that reset to the first prompt).
- **Changed**: Rewrite mode dropdown order: after **Check Spelling & Grammar**, **Improve Clarity** then **Alternative versions**, then length (Shorten / Expand), register (Formal / Informal), **Make Technical** (`constants.js`).
- **Added**: Rewrite mode **Alternative versions** - several structurally distinct reformulations separated by `----`, same meaning and register (`prompts.json`).
- **Fixed**: Web login, session re-login, and change-password forms use a real `action` URL, `autocomplete="on"` on password-change flows, standard `name` attributes, and a visually hidden `username` field where needed so browsers and password managers can save and update credentials reliably.
- **Added**: Configuration backup (ZIP) - **Settings → General → Configuration Backup**: export/import a single `transrewrt-config-backup` archive (`config.json`, `state.json`, optional `transrewrt.key`, users, `user_preferences`, `custom_prompts`). Web: admin-only `GET/POST /api/config/backup` (+ restore). Desktop: IPC save/open dialogs. Cross-restore between Electron and web (adapters + optional clear of `api_calls` / execution history). Dependencies: `archiver`, `multer`, `adm-zip`.
- **Removed**: Web server one-time migration from global `config.json`/`state.json` into `user_preferences`, and the `app_meta` SQLite table (dropped on DB init if it still exists). Per-user prefs are seeded for new admins only (`seedDefaultAdmin`).
- **Changed**: Dashboard > Summary: charts **Usage over time** (was “Cost over time”) and **Usage by model** (was “Cost by model”) show **call counts**-stacked cumulative calls by translate / rewrite / transform over dates, and total calls per model (including transform). New UI strings are in `locales/strings.json`; run `pnpm run i18n:translate` with `OPENROUTER_API_KEY` to refresh per-locale JSON files.
- **Fixed**: Web server resolves `build_timestamp` for Docker (`/app/build_timestamp`) vs dev (`repo root`); Settings About build time was empty in containers because the path pointed at filesystem root.
- **Changed**: Unavailable-model message in `useModelManagement` uses `t()` and `interpolateTemplate` for `{{freeModelId}}` so it can be translated.
- **Fixed**: `license-clarifications.json` sets `multi-llm-ts` to `Apache-2.0` (package has no `package.json` license; checker had inferred `MIT*` from the README while `LICENSE` is Apache 2.0). Range `>=5.1.0-beta4 <7.0.0-0` is used because `*` does not match prerelease versions in the checker’s semver check.
- **Changed**: `license-clarifications.json` uses semver ranges (`^2.0.0`, `^9.0.0`) for Fluent UI entries so patch/minor bumps do not require key updates.
- **Changed**: [dev/DEVELOPMENT.md](dev/DEVELOPMENT.md) documents `pnpm run 3p-notices`, clarifications, and related files under **Build** and **Useful Commands**.
- **Changed**: `license-clarifications.json` - full MIT text for `@fluentui/react-icons`; `@fluentui/react-components` matches published `LICENSE` (including assets note) instead of a truncated placeholder.
- **Changed**: `pnpm run 3p-notices` runs `scripts/write-third-party-notices.js` with `license-checker-rseidelsohn` `--json`, `--customPath`, and `license-clarifications.json`, so `licenseText` overrides (e.g. `@fluentui/react-icons` without a `LICENSE` file) appear in `NOTICES` instead of the package README.

## 1.0.15 - 2026-03-27

- **Changed**: `scripts/take-screenshots.js` logs wall-clock elapsed time (HH:MM:SS) on its own line before the session log path at the end of a successful run.
- **Added**: `scripts/translate-docs.js` `--debug` writes each locale/doc’s split source blocks to `dev/translate-docs-blocks_<run>.log` (same run id as the session log).
- **Changed**: `scripts/translate-docs.js` per-block “trying model” / fallback-failure lines include the locale code (e.g. `ms README block 1: …`).
- **Changed**: `scripts/translate-docs.js` and `scripts/generate-translations.js` pass OpenRouter `provider.sort: "throughput"` (and `allow_fallbacks: true`) so requests prefer the fastest provider for the model ([provider routing](https://openrouter.ai/docs/guides/routing/provider-selection#provider-sorting)).
- **Added**: `scripts/translate-docs.js` `--block-size <n>` to override the default max characters per split block (default 4096).
- **Added**: `scripts/translate-docs.js` prints the session log path at the end of a successful run (`session log file: dev/translate-docs_….log`).
- **Added**: [RELEASE-NOTES-v1.0.15.md](RELEASE-NOTES-v1.0.15.md) - generic release notes for the initial official release (v1.0.15).
- **Changed**: Windows NSIS assisted installer uses [images/installerSidebar.bmp](images/installerSidebar.bmp) for the wizard sidebar (`build.nsis.installerSidebar` / `uninstallerSidebar`).
- **Fixed**: [build/electron-builder.linux-arm64.json](build/electron-builder.linux-arm64.json) sets `directories.buildResources` to `images` and `directories.output` to `release`. A standalone `--config` file does not inherit `package.json` `build`, so defaults used `build/` and missed the Linux PNG; electron-builder fell back to the default Electron icon and wrote AppImages under `dist/` instead of `release/`.
- **Changed**: Linux AppImage/desktop packaging uses [images/transrewrt_logo_512x512.png](images/transrewrt_logo_512x512.png) (`build.linux.icon`) so the Transrewrt logo is embedded instead of the default Electron icon.
- **Added**: [`.github/workflows/appimage-x64.yml`](.github/workflows/appimage-x64.yml) - `workflow_dispatch` that builds only the Linux **x64** AppImage on `ubuntu-latest` (same packaging steps as release), for quick validation without arm64, Windows, or Docker.
- **Added**: [`.github/workflows/appimage-arm64.yml`](.github/workflows/appimage-arm64.yml) - workflow_dispatch and path-filtered PR/push runs that build only the Linux **arm64** AppImage (same steps as the release workflow) for quick validation without Windows, Docker, or x64 jobs.
- **Changed**: [`.github/workflows/release.yml`](.github/workflows/release.yml) Linux x64 AppImage step passes `--publish never` (aligned with arm64).
- **Changed**: Linux AppImage `electron-builder` target lists only `x64` in `package.json`; CI x64 uses `electron-builder --linux AppImage --x64` directly; arm64 merges [build/electron-builder.linux-arm64.json](build/electron-builder.linux-arm64.json) so `@electron/rebuild` targets arm64 (avoids x64 `argon2` compile on arm64 runners and duplicate AppImages on x64).
- **Fixed**: Linux arm64 AppImage CI (`--config build/electron-builder.linux-arm64.json`) sets `--publish never` and `publish: null` in that config so `electron-builder` does not require `GH_TOKEN` (“GitHub Personal Access Token is not set”).
- **Changed**: Release workflow artifact names include the app version: `Transrewrt-<version>-windows-installer-x64`, `Transrewrt-AppImage-<version>-x64` / `-arm64`; `build-windows` / `build-linux` depend on `prepare` for the version.
- **Added**: `publish-release-assets` job (on `release` / `published` only) uploads Windows `.exe` and Linux `.AppImage` files to the GitHub Release via `softprops/action-gh-release`.
- **Changed**: Comment on Docker `build-push` step: GHCR `unknown/unknown` manifest rows are BuildKit provenance attestations, not missing `linux/amd64` / `linux/arm64` images (attestations kept).
- **Added**: [dev/DEVELOPMENT.md](dev/DEVELOPMENT.md) **Releasing** section: version bump, changelog, `pnpm run update-version`, tag `vX.Y.Z`, GitHub Release notes, CI behavior.
- **Fixed**: `electron-builder` no longer attempts GitHub Release publishing during `pnpm run package` (`build.publish` is `null`). CI builds only produce installers/AppImages; release assets are uploaded by the workflow, so a `GH_TOKEN` was not required and was missing (“GitHub Personal Access Token is not set”).
- **Fixed**: [`.github/workflows/release.yml`](.github/workflows/release.yml) grants `packages: write` on Docker build/merge jobs so `GITHUB_TOKEN` can push to `ghcr.io` under an organization (avoids “installation not allowed to Create organization package”).
- **Changed**: [`.github/workflows/release.yml`](.github/workflows/release.yml) bumps `actions/checkout` to v6, `actions/setup-node` to v6, `pnpm/action-setup` to v5, `actions/upload-artifact` / `download-artifact` to v6; `docker/setup-buildx-action` to v4 and `docker/build-push-action` to v7 ([Node 24 runtimes](https://github.com/docker/build-push-action/releases/tag/v7.0.0)); sets `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` for any remaining actions still on Node 20 ([deprecation notice](https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/)).
- **Added**: [scripts/eslint-react-peers-allow-eslint10.js](scripts/eslint-react-peers-allow-eslint10.js) queries `pnpm view … peerDependencies` for `eslint-plugin-react` and `eslint-plugin-react-hooks` and uses semver to test ESLint 10; [scripts/upgrade-dependencies.sh](scripts/upgrade-dependencies.sh) runs it before `ncu` and only passes `-x eslint,…` when peers still exclude ESLint 10 (or the check fails).
- **Changed**: `scripts/upgrade-tools.sh` and `scripts/upgrade-dependencies.sh` update nvm-sh when `NVM_DIR` is a git clone (quiet fetch/checkout), resolve LTS Node via `scripts/nvm-lts-resolve-version.sh`, must be run with `source` in bash ([nvm-sh#2124](https://github.com/nvm-sh/nvm/issues/2124)); running as `./scripts/…` aborts unless `CI=1` or `TRANSREWRT_UPGRADE_ALLOW_EXEC=1`. [dev/DEVELOPMENT.md](dev/DEVELOPMENT.md) documents the upgrade flow.
- **Fixed**: LTS Node version detection no longer treats the **npm** semver in `nvm install --lts` output (e.g. `(npm v11.11.0)`) as the Node version; resolution uses `Now using node v…`, then `nvm list` lines such as `default -> lts/* (-> vX.Y.Z)`, via `scripts/nvm-lts-resolve-version.sh`.
- **Changed**: `scripts/upgrade-dependencies.sh` runs `ncu` with `--reject` for `eslint`, `@eslint/js`, and the React ESLint plugins so bulk upgrades do not jump to ESLint 10 before those plugins support it.
- **Fixed**: Release workflow Docker merge step only tags `latest` when the published release is the newest repo tag (or manual `workflow_dispatch` with `tag_as_latest`); per-arch digest builds now set `name=` on push-by-digest output; removed stray `--help` prefix from workflow comment.
- **Added**: Root `production.yml` Compose file (optional `.env` via `env_file` with `required: false`) plus a commented environment-variable reference for new-machine Docker runs.
- **Changed**: DEVELOPMENT.md Prerequisites describe installing and using direnv (`.envrc`, shell hooks, `direnv allow`); Linux direnv step defers to that section.
- **Changed**: `scripts/translate-docs.js` front matter field `source_mtime` is written as an ISO 8601 timestamp instead of raw filesystem `mtimeMs`.
- **Changed**: renamed the api keys environment variables names (`*_KEY` → `*_API_KEY`) across source files, Markdown docs, and `.env*` files
- **Changed**: API provider test success messages (`Ollama configuration is working.`, `{{provider}} credentials are valid.`) are translated in the UI via i18n; `testProviderAuth` returns `successI18n` metadata for the renderer (Electron IPC and web `/api/provider-test`).
- **Fixed**: `scripts/translate-docs.js` no longer logs a false “missing lang-list” warning when the canonical block matches the translated text already (the old check compared strings before/after replace).
- **Changed**: `scripts/translate-docs.js` translates document blocks in parallel within each locale (`--block-concurrency` / `-b`, default **2**); with default locale concurrency (**4**), up to **8** simultaneous block translations.
- **Changed**: `scripts/translate-docs.js` replaces the translated `<small id="lang-list">…</small>` block with the British English source block (canonical language names and links; English (UK) → `../README.md` / `../USER-GUIDE.md`); keeps the English (UK) link fix for any other bare `README.md` / `USER-GUIDE.md` links after locale rewriting.
- **Added**: Release workflow builds Linux **AppImage** for **x64** and **arm64** (native runners); Linux artifact names include the arch (e.g. `Transrewrt-1.0.0-arm64.AppImage`).
- **Changed**: `scripts/take-screenshots.js` applies fixed `available_models` and `top_languages` to `data/config.json` and web `user_preferences` for `ADMIN_USERNAME` before capture; default `last_used_model`, picker selection, and history sample use `openrouter/qwen/qwen3-235b-a22b-2507`.
- **Fixed**: Webpack dev server (`pnpm dev:web` / `watch:web`) sets `devServer.allowedHosts: "all"` so reverse proxies (nginx, Tailscale hostname, etc.) no longer trigger **Invalid Host header**. Development `output.publicPath` defaults to `/` again so **GET /** works (relative `./` broke webpack-dev-server’s index route). For a reverse-proxy **subpath** (e.g. nginx `/translator/`), run with `WEBPACK_PUBLIC_PATH=/translator` (trailing slash optional).
- **Fixed**: Settings models provider filter: `SettingsPanel` derives `effectiveFilterEngine` during render when the stored engine is no longer in `engineFilterOptions`, removing a disallowed `setState` in `useEffect` (eslint `react-hooks/set-state-in-effect`).
- **Fixed**: `scripts/upgrade-tools.ps1` now runs `nvm use` on the LTS version from `nvm install lts` output instead of the previously active (`*`) version in `nvm list`.
- **Added**: USER-GUIDE and README note that OpenRouter **Body Builder** (`openrouter/bodybuilder`) outputs JSON API request bodies, not plain text-avoid it for Translate, Rewrite, and Transform.
- **Added**: Settings → General → Appearance: **Show cost information on the actions** (default on); when off, translate/rewrite/transform output meta and transform prompt test output hide run/total cost lines while tracking and persistence stay unchanged.
- **Changed**: Output meta last-run cost line in `App.js` uses two clear branches (`amount` / `free`) instead of a nested ternary and empty fallback for `unknown` / `none`.
- **Changed**: README and USER-GUIDE now warn that Ollama must be configured to allow external/network access when used from other devices, containers, or services.
- **Changed**: Translate/Rewrite/Transform output meta now prefixes run cost with `~` only when the amount is estimated (non-OpenRouter direct-provider runs); exact OpenRouter amounts remain without `~`.
- **Changed**: Transform > Prompt Edit test usage line now also prefixes estimated cost amounts with `~`, matching the main run output meta behavior.
- **Changed**: Settings > Models now appends `(estimated)` to pricing lines when direct-provider model costs are inferred from OpenRouter pricing matches.
- **Changed**: Settings → Models provider filter list is now sorted alphabetically (by provider label in the current UI language), with "All providers" kept first.
- **Fixed**: Electron external-link actions in Settings → API now open provider/Ollama URLs in the OS default browser via `shell.openExternal` (preload IPC bridge) instead of opening inside a new Electron window.
- **Fixed**: Electron Settings → API Config provider external-link icon now appears in both field states (configured view and edit/input view); previously it was rendered only in edit/input mode.
- **Changed**: Electron API-provider link lookup now matches `provider_id` case-insensitively (normalized compare) when resolving URLs from `icons_with_files.json`.
- **Changed**: Electron Settings → API Config now shows an external-link icon beside provider API-key labels (and beside Ollama base URL) when a provider URL exists in `icons_with_files.json`; clicking opens the provider website in the browser.
- **Changed**: Provider icon mappings for `cerebras`, `google` (now `google.ico`), and `google-vertex` are now explicit in `icons_with_files.json`; removed redundant `ProviderIcon` manual override map.
- **Changed**: Sorted `src/renderer/assets/icons_with_files.json` entries by `provider_id` for consistent maintenance and diffs.
- **Changed**: Cleaned new provider icon catalog entries (`icons_with_files.json`) with normalized provider names (brand casing) and populated known official URLs.
- **Changed**: Added missing provider icon entries in `src/renderer/assets/icons_with_files.json` so existing `.ico` assets resolve in `ProviderIcon` instead of falling back to the generic icon.
- **Changed**: `scripts/trim-ico-sizes.sh` now processes icon files from `./src/renderer/assets` when run from the repository root.
- **Changed**: `scripts/trim-ico-sizes.sh` now exits early with an install hint (`sudo apt install imagemagick`) when ImageMagick tools are missing.
- **Changed**: Provider icons now also resolve from `provider/model` identifiers by trying the first segment as the provider key, and Google provider icon mapping now uses `google.ico`.
- **Changed**: Successful **Ollama** API test message is now “Ollama configuration is working.” instead of “Ollama credentials are valid.”
- **Fixed**: API provider test for **Ollama** no longer always reported “Ollama base URL is required” - `buildProviderTestRequest` incorrectly attached `missingMessage` on the successful branch so `testProviderAuth` never ran the `/api/tags` check.
- **Fixed**: Settings > API Config inputs now keep per-provider draft values so unconfigured API key fields are editable; Ollama base URL editing is buffered locally and saved on blur/Enter to prevent cursor jumps while typing.
- **Changed**: History list input preview: newlines are replaced with spaces (single-line preview); full `input_text` unchanged in the detail panel.
- **Fixed**: **ProviderIcon** maps native `cerebras` engine to `cerebras.ico` (vendor absent from OpenRouter `icons_with_files.json` catalog).
- **Changed**: LLM provider env vars: missing or blank/whitespace-only values are ignored (`readEnvNonBlank` in shared LLM `mergeKeys`, Electron env sync, `listLlmEnvVarsPresent`, web `/api/provider-keys` env flags).
- **Changed**: `scripts/translate-docs.js` writes its session log under `dev/translate-docs_YYYYMMDD_HHMMSS.log` (was `translated-docs/`); appends one summary line per processed doc (`README`, `USER-GUIDE`) to `dev/translations.log` (timestamp, primary `--model`, summed per-locale doc elapsed time, tokens, cost).
- **Added**: Cerebras provider support (`CEREBRAS_API_KEY`; desktop **Settings → API** and **Settings → Models** provider filter; `docker-compose.yml`). README, USER-GUIDE, and SYSTEM-OVERVIEW updated.
- **Changed**: Settings → Models provider filter lists only engines that have at least one model in the current catalog (`allModels`, same source as `/api/llm/models` / Electron `fetchModels`).
- **Removed**: SambaNova env/UI/compose wiring (not a native **multi-llm-ts** engine in the pinned version).
- **Changed**: `scripts/take-screenshots.js` session log file is written to `dev/take-screenshots-YYYYMMDD-HHMMSS.log` (was `images/screenshots/`); successful runs log the resolved log path at the end.
- **Changed**: [DEVELOPMENT.md](DEVELOPMENT.md): aligned with **SYSTEM-OVERVIEW** - intro + **Related documentation** table; Docker `/app/data` describes `transrewrt.db` / `user_preferences`; key files add `shared/llm`, `llmIpc`, `apiLlm`, `config` route, `webConfigKeys`, `encryption`; closing pointer defers architecture to SYSTEM-OVERVIEW.
- **Changed**: [SYSTEM-OVERVIEW.md](SYSTEM-OVERVIEW.md): new sections **Multi-llm-ts and provider support** (engines, namespaced ids, `mergeKeys`, pricing cache) and **Security and encryption** (Electron AES-256-CBC + `transrewrt.key`, sanitized IPC / `getSecretsForRequest`, web Argon2id / session cookies / admin config); removed **Transrewrt proxy**; documents web `user_preferences`, `state.json` migration, mermaid + folder tree fixes.
- **Changed**: [DEVELOPMENT.md](DEVELOPMENT.md): expanded **Useful Commands Summary** (grouped subsections; extra scripts and `docker:*`); **UI translations** and **Key configuration files** document OpenRouter model list in `scripts/openrouter-script-models.js` for `i18n:translate`, `translate-docs`, `generate-test-data`.
- **Changed**: `scripts/take-screenshots.js` `--locale=` accepts comma- or space-separated codes (PowerShell turns unquoted `a,b,c` into `a b c` before Node); help text notes quoting commas in PowerShell.
- **Changed**: `scripts/translate-docs.js` now accepts `--locale=` (and `-l`) and splits locale codes on commas or whitespace inside the provided locale value, matching `take-screenshots.js` behavior.
- **Changed**: `scripts/generate-translations.js` now accepts `--locale=` (and `-l`) and splits locale codes on commas or whitespace inside the provided locale value, matching `take-screenshots.js` / `translate-docs.js` locale filtering.
- **Changed**: `scripts/take-screenshots.js` loads and validates UI locales from `ui-languages.json` (and `--locale` filter) at startup, before admin/env checks and browser launch, so bad or empty locale selection fails fast.
- **Changed**: README: note that non–English (UK) UI strings are AI-translated and may be imprecise or wrong.
- **Changed**: Dev docs [DEVELOPMENT.md](DEVELOPMENT.md), [SYSTEM-OVERVIEW.md](SYSTEM-OVERVIEW.md), and [i18n.md](i18n.md): aligned with Electron 41, execution **History**, **Settings** tabs and role-gated visibility, REST `calls` routes, `ui-languages.json` workflow, RTL (`getTextDirection`, `useDirection`, `FluentProvider` `dir`, CSS/`dir="auto"` / arrow flipping); fixed i18n annex typo and noted scripts are snapshots.
- **Changed**: Dependencies: `diff` 8.0.4, `i18next` 25.10.5, `react-i18next` 16.6.2, `lucide-react` 1.0.1. ESLint remains on 9.39.x (`eslint` / `@eslint/js`); ESLint 10 is blocked until `eslint-plugin-react` / `eslint-plugin-react-hooks` support it (ESLint 10 removes `context.getFilename()`, which current plugin releases still use).
- **Changed**: RTL: user-visible strings containing U+2192 (→) are shown with U+2190 (←) via `flipUiArrowsForRtl` in Settings → Models sort labels, Cost Tracking key-usage message, model selector tooltip, transform prompt temperature label, and History translate language pair.
- **Changed**: RTL UI languages: `FluentProvider` receives reactive `dir`; CSS and component styles use logical properties (`margin-inline-*`, `text-align: start/end`, etc.); user text fields use `dir="auto"` where appropriate; directional icons (chevrons, back arrow) use `.rtl-icon-mirror` in RTL; sort-select dropdown chevron position fixed for RTL. Exported `getTextDirection()` from `i18n.js`; `useDirection` hook + `AppRoot` wrapper.
- **Added**: USER-GUIDE troubleshooting: six new entries covering dashboard empty charts, cost "not available"/estimate accuracy, total vs provider bill, missing History sidebar, web session expiry, and admin-only user filter.
- **Changed**: USER-GUIDE **General settings**: documented **History** (**Keep execution history**, **Delete history data**) and clarified vs **Cost Tracking** deletion.
- **Fixed**: `scripts/take-screenshots.js` `CUSTOM_PROMPTS_INSERT` now passes `user_id` (`null`, same as Electron) so the statement matches `appSchema.js` (10 placeholders).
- **Changed**: `scripts/reset-web-password.js` enables `PRAGMA foreign_keys = ON` after opening the DB. `scripts/generate-test-data.js` help/comments note parity with `appSchema.js` for `api_calls` / `action_content` DDL.
- **Changed**: `action_content.api_call_id` → `api_calls(id)` foreign key with `ON DELETE CASCADE`; `PRAGMA foreign_keys = ON` in `applyAppSchema`. Existing databases migrate by rebuilding `action_content` (orphan rows removed). Cost/model deletes only `DELETE` from `api_calls`; linked history text is removed by cascade.
- **Changed**: `scripts/generate-test-data.js` forces the latest translate row to **Detect Language** → **Portuguese (BR)** after random generation (sorted by time).
- **Changed**: `scripts/openrouter-script-models.js` exports shared `TRANSLATION_MODELS` for `scripts/generate-translations.js`, `scripts/generate-test-data.js`, and `scripts/translate-docs.js` (not app `config.json`). `buildModelsToTry` orders primary then fallbacks.
- **Changed**: `scripts/generate-test-data.js` aligns `api_calls` with `src/shared/db/appSchema.js` (text stats columns, `sql.INSERT_API_CALL`).
- **Changed**: `scripts/take-screenshots.js` sidebar capture (`sidebar.png`) saves at **70%** of the clipped dimensions (sharp resize after capture).
- **Changed**: `scripts/translate-docs.js` rewrites `[README](README.md)` / `[User Guide](USER-GUIDE.md)` (and optional `#` fragments) to the locale files `README.<locale>.md` / `USER-GUIDE.<locale>.md` in `translated-docs/`, instead of linking to the English files in the repo root.
- **Fixed**: README.md and USER-GUIDE.md: updated documentation to reflect multi-provider support (not OpenRouter-only), added History section/screenshot to README, fixed typos (Transrewt, Openrouter, teste), grammar errors, duplicate anchors, en-GB spelling consistency, missing/broken `<a id>` anchors for translated-doc linking, and clarified API Config instructions for both desktop and web.
- **Added**: History page filter row: **Download** (JSON / CSV / XLSX) for the current filtered execution history, aligned with Dashboard’s All Calls export (plus `input_text` / `output_text` columns), `32px` margin after the User filter when shown.
- **Added**: `scripts/generate-transrewrt-banner.js` (and `pnpm run generate-banner`) writes `images/transrewrt_banner.svg` and `images/transrewrt_banner.png` (512×128, transparent) with the logo and gradient “Transrewrt” text matching the sidebar styling.
- **Changed**: `scripts/take-screenshots.js` exits at startup if `ADMIN_USERNAME` or `ADMIN_PASSWORD` is missing or blank (after parsing CLI args, before opening the log file).
- **Added**: `scripts/take-screenshots.js` captures a full-window **history** shot (`history.png`): opens sidebar History, selects the first translate entry, seeds a sample translate row when the DB has no execution history yet; `HistoryPage` list rows expose `data-testid="history-list-item"` and `data-history-type` for automation.
- **Changed**: Settings → API Config (Electron): Edit button now matches Save/Test button styling - uses `appearance="secondary"` with `size="small"`, removed pencil icon, for consistent alignment and visual weight.
- **Changed**: Web factory defaults for new accounts: `user_preferences` is seeded from [`src/config-defaults/config_default.json`](src/config-defaults/config_default.json) merged with app `DEFAULT_STATE` (same basis as GET `/api/config`) for the first-time admin user, migration from legacy shared config, and every user created via **Settings → Users**.
- **Changed**: User guide and API Settings UI explicitly state that users can use free OpenRouter models (no credit card required) or install Ollama for local, keyless models, providing clear alternatives for users without an API key.
- **Changed**: Web/Docker: workspace settings (models, languages, general options, session fields like `last_used_model`), `total_cost`, and transform prompts are **per user** (SQLite `user_preferences` + `custom_prompts.user_id`); global `config.json` keeps only server keys (API provider secrets, `web_session_timeout`). One-time migration copies prior shared config/state into each account and strips those keys from the global file. **Settings → Cost Tracking** is **admin-only** in web mode; non-admins cannot read or write provider keys via `/api/config`; `GET /api/calls/*` username filters are enforced server-side for non-admins.
- **Changed**: Settings > API Config: web mode now shows an admin-only API tab with environment-configured provider rows and per-provider auth test buttons; Electron API tab now uses a 2-column provider layout with per-provider test buttons (removed standalone OpenRouter test button). Electron startup now syncs missing provider keys from env into config, and runtime key precedence is config-over-env so configured status and runtime usage stay consistent.
- **Changed**: Renamed settings tab component nomenclature from `SettingsDialog*` to `Settings*` across renderer files, imports/usages, and active docs/comments.
- **Fixed**: History detail view shows **Input** and **Output** side-by-side (like the main workspace), fills the pane height without a page-level vertical scrollbar; `ResizablePanels` flex items use `min-height`/`min-width` 0 so nested splits shrink correctly.
- **Changed**: Settings → Models tab: **Available models** header row fits **search** (centered); **Free only** sits on the **Refresh / Expand / Collapse / sort** toolbar row (right); tighter **section** spacing (panel headers and pane gaps).
- **Changed**: Model route badge label **openrouter** is fixed English (`"openrouter"`); **direct** stays translated via `t("direct")` in `modelRouteBadgeProps`.
- **Changed**: Header model picker shows `vendor/model` (strips the `openrouter/` prefix for OpenRouter ids), the same **direct** / **openrouter** / **local** route badges as Settings → Models, and **provider icons** from the inner vendor for OpenRouter (OpenRouter icon only for `openrouter/...` rows such as `openrouter/openrouter/free`). Implemented via popover + `modelHeaderDisplayId` / shared `modelRouteBadgeProps`.
- **Fixed**: Header model picker list: route badges align in a **single right column** (Fluent `MenuItem` defaults to `max-width: 290px` and `align-items: start`, which prevented full-width rows); rows use a **CSS grid** for icon / name / badge.
- **Fixed**: Direct-provider cost (e.g. **Google** `gemini-2.5-flash`): match OpenRouter rows by **trailing slug** (`google/gemini-2.5-flash` ↔ `gemini-2.5-flash`); tie-break when several vendors share a slug. **Pricing cache** now loads from **public** `GET /api/v1/models` when no OpenRouter key is set (previously refresh was skipped, so estimates never matched), and `streamCompletion` refreshes before lookup so translate works without opening the model list first. Stream `usage` sets `cost_known`; unknown match → **Cost not available**; real **$0** with known pricing or **openrouter/** → **free**. Debug: `scripts/debug-openrouter-vs-native-model-ids.js`.
- **Fixed**: Web translation via `POST /api/llm/stream` aborted immediately - the route listened on `req.on("close"/"aborted")` to cancel the LLM; with a JSON body, the request stream can emit `close` after `express.json()` finishes while the client is still reading SSE, so the abort fired before any tokens (Electron IPC path was unaffected).
- **Fixed**: Translate/rewrite/transform showed **Translation stopped by user** when the user did not cancel - React 18 Strict Mode (dev) ran the `useProcessing` unmount cleanup that called `abort()` on the shared `AbortController`, and **Failed to fetch** / network errors were misclassified as user aborts. Cleanup no longer aborts LLM requests; `isAbortError` and catch handling only treat real `AbortError` / explicit abort wording as user cancel; unexpected `cancelled` results show **The request ended before completion**. Webpack `devServer.compress` is **false** so proxied `/api/llm/stream` SSE is not buffered by gzip.
- **Fixed**: Direct OpenAI (and other multi-llm-ts) chat streams could return no assistant text - multi-llm-ts injects tools for models marked tool-capable (e.g. `gpt-3.5-turbo-1106`), so the model often streamed tool calls only; `streamCompletion` now passes `tools: false` for translate/rewrite/transform. Web SSE client also treats `delta.content` as string or text-part array and rethrows any stream `error` event when the body is empty (not only HTTP 404).
- **Fixed**: `POST /api/llm/stream` called `setSessionRefreshCookie` after `flushHeaders()`, causing `ERR_HTTP_HEADERS_SENT`; session refresh now runs before SSE headers are flushed.
- **Changed**: Settings → Models route badges show **openrouter** (grey / informative) or **direct** (blue / brand) instead of “Vendor (OpenRouter)” / “Vendor (direct)”; Ollama rows use **local** (informative).
- **Changed**: Model list API includes `pricingKnown`; providers without list pricing (or OpenRouter rows without `meta.pricing`) no longer show as free, show **Cost not available** instead of `$0.00/1M`, and are excluded from **Free Only**; cost sort puts unknown pricing last. Helpers in `src/renderer/utils/misc/modelPricingUtils.js`.
- **Fixed**: When Ollama is configured (`OLLAMA_URL` / `ollama_base_url`) but the daemon is not reachable, `multi-llm-ts` logged `Error listing models: fetch failed` / `ECONNREFUSED` on every model list; `getAllModels` / `ensureCatalogForEngine` now probe `GET …/api/tags` first and skip Ollama if unreachable (no change to HTTP 200 model response).
- **Fixed**: pnpm install warning `Failed to create bin … multi-llm-ts … tools/cli.js` - upstream publishes `bin.cli` pointing at `tools/cli.js` but npm `files` only includes `dist`, so the file is missing; added `patches/multi-llm-ts@5.1.0-beta4.patch` (remove `bin`) and `pnpm.patchedDependencies`.
- **Changed**: Web server startup logs which LLM-related environment variables are set (`OPENROUTER_API_KEY`, `OPENAI_API_KEY`, …) via `listLlmEnvVarsPresent()` in `src/shared/llm/index.js`.
- **Changed**: Dev scripts `scripts/generate-translations.js`, `scripts/translate-docs.js`, and `scripts/check-api-key.js` read the OpenRouter key only from `OPENROUTER_API_KEY` (no `API_KEY` / `OPENROUTER_API_KEY` fallbacks). `.env.example`, `.cursor/rules/minimal-context.mdc`, `dev/DEVELOPMENT.md`, and `dev/i18n.md` updated. Web missing-key modal and locale strings refer to `OPENROUTER_API_KEY`; `SettingsDialogApiTab` field list constant renamed to `PROVIDER_SECRET_FIELDS`.
- **Added**: Multi-provider LLM layer via `multi-llm-ts` on the Node side: Electron IPC (`llm:stream`, `llm:abort`, `llm:models`) and web routes `POST /api/llm/stream` (SSE), `GET /api/llm/models`, `GET /api/llm/generation`; nine providers (OpenRouter, OpenAI, Anthropic, Google, DeepSeek, Groq, Mistral, Ollama URL, xAI) with namespaced model ids; OpenRouter streaming uses direct fetch for generation-id cost; native engines use pricing estimates when an OpenRouter key can populate the cache.
- **Changed**: Removed legacy `API_KEY` / `API_URL`, Transrewrt proxy rolling key, and transparent `/api/proxy`; desktop Settings → API is per-provider keys + Ollama URL; Settings → Models adds provider filter chips and inner-vendor grouping for OpenRouter rows; `FREE_MODEL_ID` is `openrouter/openrouter/free`; Docker/README/USER-GUIDE document new env vars (`OPENROUTER_API_KEY`, …).
- **Changed**: `scripts/generate-translations.js` parallel batch header (`🚀 Running in parallel:`) appends run-wide progress `done/total` languages (e.g. `32/36`).
- **Changed**: History detail summary card: translate rows show source/target via `t()` on language keys (UI locale only) / `t("auto")`; rewrite mode via `t()`; transform row keeps prompt text as stored but translates target language only.
- **Changed**: Electron desktop: sidebar no longer shows the user block (username / user menu); web app unchanged.
- **Changed**: History detail top summary: timestamp and username use neutral foreground and bold weight instead of the type accent colour.
- **Changed**: Execution history list (`getExecutionHistory` / `GET /api/calls/history`) omits cancelled or incomplete rows: requires `completion_tokens > 0` and `tps > 0` (shared `buildExecutionHistoryWhere` in `appSchema.js`).

- **Changed**: History detail header: first card shows type-specific summary (translate: auto → target, rewrite: mode, transform: prompt with target in brackets); second card uses a two-column label/value grid for ID, model, cost, TPS, tokens, duration, etc.; redundant Input/Output stats rows removed from that card (shown under the text panels). `CallDetailsContent` supports `prependFields`, `excludeFieldKeys`, `costFractionStyle`, and `columnCount` (Dashboard expanded rows stay three columns).
- **Changed**: History Input/Output `TextPanel` footers use a compact row (chars · words · paragraphs) with a **Copy** button on the right; `footerMinimal` + `readOnly` defaults copy to `navigator.clipboard.writeText` when `onCopy` is omitted.
- **Changed**: Settings > General Settings: main content uses a two-column layout (Behaviour and History on the left, Appearance on the right), stacking to one column below 800px width.
- **Added**: Execution history: optional storage of translate/rewrite/transform input and output in SQLite `action_content`, History view (Dashboard-style) with filters and detail panels, Settings > General controls (`keep_execution_history`, delete by age); APIs `GET`/`DELETE` `/api/calls/history` and Electron IPC `getExecutionHistory` / `deleteExecutionHistory`.
- **Fixed**: Windows `installer.nsh` desktop shortcut UI wrapped in `!ifndef BUILD_UNINSTALLER` so the NSIS uninstaller pass does not compile unused page functions (avoids warning 6010 / `-WX` failure).
- **Fixed**: Windows `installer.nsh` desktop page no longer calls `StdUtils::TestParameter` (StdUtils plugin is not loaded for the NSIS uninstaller build pass, so `makensis` failed with "Plugin not found").
- **Changed**: Windows NSIS installer uses assisted mode (`oneClick: false`) with a "Create a desktop shortcut" checkbox; built-in desktop shortcut creation is disabled so the choice matches user selection (silent installs skip the page and keep default: create shortcut).
- **Fixed**: Electron packaged app: include `src/shared/**/*.js` in electron-builder `files` so `src/main/appDb.js` can require `../shared/db/appSchema.js` inside `app.asar` (was missing, causing main-process crash after install).
- **Changed**: `scripts/electron-rebuild.js` runs the `@electron/rebuild` CLI via `spawnSync(process.execPath, [cli.js, ...])` so rebuild works without relying on `electron-rebuild` being on `PATH` (e.g. when invoking the script with plain `node`).
- **Changed**: DevDependencies: keep `eslint` and `@eslint/js` on 9.x until `eslint-plugin-react` / `eslint-plugin-react-hooks` declare ESLint 10 support (ESLint 10 breaks `react/display-name` with current plugins); Electron 41 and `sharp` 0.34.x remain upgraded.
- **Fixed**: `scripts/upgrade-tools.ps1`: Node version in the success message shows the semver (e.g. 24.14.0), not `System.Collections.Hashtable[1]` (use a variable or `$($Matches[1])` inside double quotes).
- **Fixed**: Dashboard > All Calls expanded row: label column now grows to fit translated labels (e.g. "Prompt de transformation") instead of truncating; uses `minmax(110px, max-content)`.
- **Added**: Call details "Input"/"Output" stats line (chars · words · paragraphs) is now translatable via i18n and `interpolateTemplate`.
- **Changed**: README and USER-GUIDE: GitHub-style alerts (`[!NOTE]`, `[!WARNING]`) replaced with standard markdown blockquotes using emojis (ℹ️ NOTE, ⚠️ WARNING) and `<br/>` for compatibility.
- **Changed**: USER-GUIDE.md copy-edited for clarity and consistency; removed duplicate anchor IDs, tightened wording, and standardised UK English throughout.
- **Changed**: README now links to the User Guide after the feature summary and at the end of the Quick Start section.
- **Removed**: `--dry-run` / `-d` option from `translate-docs` and `generate-translations` scripts.
- **Changed**: `translate-docs` now logs and totals `reasoning_tokens` (from `usage.completion_tokens_details.reasoning_tokens`) in per-block, per-locale, and summary lines when a thinking model reports them.
- **Fixed**: Settings > Models: ordering by model name or provider now uses case-insensitive sort in both Available and Selected lists.
- **Changed**: translate-docs now runs documents (README, USER-GUIDE) in parallel within each locale to reduce per-language translation time.
- **Added**: Translated docs (translate-docs) now include YAML frontmatter: `translated_at`, `source_hash`, `source_mtime`, `model`; translation is skipped when existing file's `source_hash` matches the current source (no API calls, no write). `--force` / `-f` forces translation even when the hash matches.
- **Changed**: README and USER-GUIDE screenshot paths now use `images/screenshots/en-GB/`; translate-docs substitutes `../images/screenshots/<locale>/` (and `../images/` for other image paths) in translated outputs so images resolve correctly from the `translated-docs/` folder. Doc links are rewritten: en-GB source links (`README.md`, `USER-GUIDE.md`) become `../README.md`, `../USER-GUIDE.md`; other-language links (`translated-docs/README.xx.md`) become same-folder links (`README.xx.md`) so all links work from `translated-docs/`.
- **Added**: README and USER-GUIDE use stable fragment IDs via `<a id="slug"></a>` above headings so internal links work after translation; translate-docs prompt instructs to keep fragment and anchor ids unchanged.
- **Changed**: Settings > Models now shows input and output cost per 1M tokens separately (Input: $X / 1M · Output: $X / 1M tokens) instead of a single average.
- **Changed**: `translate-docs` simplified: sends the whole document per API call; if document exceeds 16k chars, splits at nearest markdown heading into blocks of at most 16k. Section hash cache and batch-JSON logic removed; `--retranslate` option removed.
- **Changed**: All scripts that accept CLI arguments now reject unknown options: they print an error message in red and exit with code 1. Scripts with no supported options (extract-strings, update-version, electron-rebuild, write-build-timestamp, check-node-version, node-rebuild) reject any argument; scripts with options (take-screenshots, generate-translations, translate-docs, generate-test-data, check-api-key, reset-web-password) reject only options they do not support.
- **Added**: All scripts now support `--help` and `-h` to print usage and exit: check-api-key, reset-web-password, extract-strings, update-version, electron-rebuild, write-build-timestamp, check-node-version, and node-rebuild (take-screenshots, generate-translations, translate-docs, generate-test-data already had help).
- **Changed**: `take-screenshots` transform-prompt-edit now runs a final teardown that clicks "Back to Run", leaving the app in Transform run view (known state); important when using `--screenshot=transform-prompt-edit` or other subsets.
- **Fixed**: `take-screenshots` transform-generate: when run after transform-prompt-edit, the app stays in the transform editor (no prompt selector). Initial prepare now clicks "Back to Run" when the editor is open so the prompt selector is visible before selecting "Rewrite with Synonyms" and opening the editor again.
- **Changed**: `generate-translations.js` now translates up to 4 languages in parallel (configurable via `PARALLEL_LANGUAGES`) to reduce total run time; `strings.json` is written once after each batch to avoid write races.
- **Changed**: LanguageSelector no longer accepts `topLanguages` or `allLanguages` props; both are derived inside the component (and in TransformPromptEditor) via new `useContentLanguageLists()` hook from AppContext and config. Removed these props from Translate/Transform panels, TransformTranslateModal, and App options.
- **Changed**: Content language list display now uses "englishName / t(englishName)" when the translation differs from the English name (locale-based), otherwise shows only the English name; implemented in `getUILanguageLabel` using existing locale keys for language names. Header UI language selector keeps the previous format (englishName / native label) via `getUILanguageLabelNative`.
- **Added**: After `setModelToOpenRouterFree()`, `take-screenshots` now sets Translate From: Detect Language and To: Portuguese (BR) (navigate to Translate, then select in From/To dropdowns). Added `dataTestId` support to `LanguageSelector` and `translate-from` / `translate-to` in Translate workspace for automation.
- **Changed**: `take-screenshots` transform-generate flow: open Transform → click "Edit prompt" (after selecting "Rewrite with Synonyms") once, then for each locale set language → click "Generate Prompt" → capture → Cancel; after all locales, click "Back to Run" so Transform stays in run view. Added `data-testid="edit-prompt-button"` on Edit prompt button.
- **Changed**: `take-screenshots` now saves files per language in subfolders `images/screenshots/LANGCODE/` (e.g. `images/screenshots/pt-BR/translate.png`) instead of `name.code.png` in a single folder.
- **Added**: `take-screenshots` optional args `--screenshot=NAME` (or `--screen=NAME`) to run only one set (e.g. `translate`), and `--locale=CODE` or `--locale=CODE,CODE,...` to run only for selected locale(s).
- **Added**: `take-screenshots` logs "Setting UI language N/T to LANGCODE (ENGLISHNAME)..." with counter, and prints an ASCII summary table at the end (one row per language, columns per screenshot set, ✓/× for success/fail).
- **Changed**: `take-screenshots` retries up to 3 times when waiting for the language selector or when a per-language capture fails, with a 2.5s delay between attempts, then skips that screenshot and continues.
- **Changed**: `take-screenshots` log lines now start with a timestamp (HH:MM:SS -) and are written to both console and a log file `take-screenshots-YYYYMMDD-HHMMSS.log` in `images/screenshots/`.
- **Changed**: `take-screenshots` now captures each screenshot in every UI language from `ui-languages.json`, saving as `{name}.{code}.png` (e.g. `rewrite.pt-BR.png`); navigate/setup runs once per screenshot type in en-GB, then the UI language is cycled for each capture.
- **Added**: `data-testid` attributes on sidebar nav, language selector, model/prompt selectors, Generate prompt modal, dashboard filter/tabs, and settings tabs so the screenshot script can find elements without relying on translated labels.
- **Added**: `translate-docs` script to translate README and USER-GUIDE to all UI languages via OpenRouter into `./translated-docs`; language selector in README and USER-GUIDE for translated versions; section hash cache in `translated-docs/.section-cache/` (gitignored); translated .md files are committed.
- **Fixed**: `take-screenshots` model selection: wait for model listbox to have options before matching, and match option by display text "free (openrouter)" (and value attribute if present) instead of raw id "openrouter/free", so "openrouter/free" is selected correctly.
- **Fixed**: UK English spelling in UI strings and config defaults: Behavior→Behaviour, gray→grey, english→English, Summarize→Summarise, summarized→summarised.
- **Fixed**: `USER-GUIDE.md` grammar/spelling (UK English), broken internal link, and added cross-reference links between sections for easier navigation.
- **Changed**: `take-screenshots` at start: ensure "Rewrite with Synonyms" exists in `custom_prompts` (import from `src/config-defaults/transform-prompts.json` if missing), then after setting UI language to en-GB select model "openrouter/free" for predictable behaviour.
- **Changed**: `take-screenshots` transform.png: after opening the Transform page, select the prompt "Rewrite with Synonyms" from the dropdown before capturing.
- **Changed**: `take-screenshots` dashboard-summary: after all 3 charts have content, wait for chart fingerprint (path/rect/circle attributes) to stay stable for 500ms before capturing, so Recharts animations are finished; 4s stability timeout then screenshot anyway.
- **Changed**: `generate-test-data` now randomizes timestamps within the last 2 months instead of the last 2 years.
- **Changed**: `generate-test-data` now sums generated row costs, writes the result to `config.json` as `total_cost`, and logs the updated total after each run.
- **Changed**: Dashboard Summary "Cost by model" chart excludes free models (zero cost); shows "No data" when all models have zero cost.
- **Changed**: Dashboard By Model "Cost by model (stacked)" chart excludes free models (zero cost). Table shows a Free badge (same as Settings > Models) for models with zero cost.
- **Changed**: Dashboard By Day "Daily call volume" chart shows total calls per day only (single area), no longer broken down by type.
- **Changed**: Web server startup logs now print the resolved SQLite database path (`transrewrt.db`) alongside the data directory, so it is immediately clear where the web app stores its database.
- **Changed**: `generate-test-data` now logs the resolved `transrewrt.db` path at startup so it is clear which database file is being used in `--web` and `--app` mode.
- **Changed**: `generate-test-data` now clears existing `api_calls` rows and resets their autoincrement before inserting a fresh test-data batch, so reruns replace old generated call data instead of appending to it.
- **Changed**: `generate-test-data` model selection is fully random again for each generated row instead of rotating through shuffled model batches.
- **Changed**: Rewrote `USER-GUIDE.md` with a clearer first-run flow, plain-English explanations for AI terms, a self-contained desktop API-key walkthrough, consistent UI labels, and a new Common Issues section for non-technical users.
- **Fixed**: `take-screenshots` model-selector.png: include the model icon (Bot/Provider icon) to the left of the dropdown in the clip.
- **Changed**: `take-screenshots` language-selector: no longer load Noto from Google Fonts; only apply font-family so system-installed Noto (e.g. fonts-noto-cjk, fonts-noto-core) is used.
- **Fixed**: `take-screenshots` language-selector.png: apply fonts before opening the menu and wait for `[role="menu"]` to be visible before capturing, so the language list is included (popover was closing during the previous font-load step).
- **Changed**: `take-screenshots` dashboard-summary: wait for charts to be drawn (Summary tabpanel not loading + Recharts SVG with content) instead of fixed 25s wait; 30s timeout then screenshot anyway.
- **Changed**: `take-screenshots` clears browser cache (CDP Network.clearBrowserCache) before navigating so cached pages do not affect screenshots.
- **Fixed**: `take-screenshots` after capturing transform-generate.png, click the modal's "Cancel" button (fallback Escape) so the UI is closed and subsequent screenshots work.
- **Added**: `take-screenshots` transform-generate.png: after transform-prompt-edit, click "Generate prompt" and capture the Generate prompt configuration modal (cropped to modal only).
- **Fixed**: `take-screenshots` sidebar.png: temporarily set sidebar height to auto and nav flex to 0 0 auto so the screenshot crops to content only (no empty gap between Transform and Dashboard); clip height trimmed to end at the last footer button (user menu or Settings) to remove bottom empty space.
- **Changed**: User guide moved from `docs/user-guide.md` to repo root as `USER-GUIDE.md` (name uppercase, extension lowercase).
- **Changed**: `take-screenshots` script: language-selector screenshot is saved at half width/height (via sharp); added model-selector (dropdown + options crop), sidebar (sidebar area crop), and dashboard-filter (filter row + user selector crop) screenshots.
- **Fixed**: `take-screenshots` now waits for the app shell (nav) after login from main(), with a 30s timeout and a clearer error when the main app does not appear (e.g. login failed or credentials missing).
- **Fixed**: `take-screenshots` language-selector font loading: reject with an Error so the catch logs a clear message (e.g. "Font stylesheet failed to load") instead of "[object Event]" when the stylesheet fails to load; log message uses (e && e.message) || String(e) for robustness.
- **Changed**: `take-screenshots` is more verbose: logs server check, navigation, login/shell/locale steps, each screenshot step (button clicks, crops, resize), and saved files with a [take-screenshots] prefix.
- **Changed**: `take-screenshots` language-selector: always apply Noto font-family so system-installed Noto fonts are used when Google Fonts cannot be loaded; log whether web fonts loaded or suggest installing fonts-noto-cjk and fonts-noto-core (e.g. on Debian/Raspbian) for best results.
- **Changed**: DEVELOPMENT.md lists Chromium and Noto fonts (fonts-noto-cjk, fonts-noto-core) as prerequisites; Linux section documents apt install and PUPPETEER_EXECUTABLE_PATH for take-screenshots (e.g. on Raspberry Pi).
- **Added**: User guide (`docs/user-guide.md`) for end users: interface overview, Translate/Rewrite/Transform, Dashboard, and Settings (General, Models, Languages, Cost Tracking, Transform Prompts, Users, API Config, About), with screenshots and Electron vs Web / admin-only notes.
- **Added**: Script `scripts/check-api-key.js` and `pnpm run check-api-key` to verify OpenRouter API key and print masked key and limit/usage (like Settings > Cost tab). Uses API_KEY or OPENROUTER_API_KEY env, or --key / first arg; optional OPENROUTER_API_URL or --url.
- **Changed**: Renamed "Rewrite style" to "Rewrite mode" in UI, config key, DB column (with migration), API, and variable names; labels "Style" → "Mode", constants and options use REWRITE_MODE_KEYS / getRewriteModeOptions; prompt config key `rewrite.styles` → `rewrite.modes`.
- **Added**: README screenshots section using images from `pnpm run take-screenshots` (Translate, Rewrite, Transform, new prompt, dashboard, settings-models, language selector).
- **Fixed**: Reverted Electron to ^40.8.0 so `pnpm install` postinstall (electron-rebuild) succeeds. better-sqlite3 12.6.2 does not build against Electron 41 (V8 14.6 removed `PropertyCallbackInfo::This()`/`Holder()`; fix is in better-sqlite3 PR #1459, not yet released). Re-upgrade to Electron 41 once better-sqlite3 ships a release with the HolderV2 fix.
- **Added**: Puppeteer script (`pnpm run take-screenshots`) to capture screenshots of Translate, Rewrite, Transform, Transform New Prompt, Dashboard Summary, Settings Models, and header language selector; output in `images/screenshots/`. Uses `ADMIN_USERNAME`/`ADMIN_PASSWORD` for web login and sets UI to en-GB before capturing.
- **Changed**: README badge: Electron version updated from 40 to 41 to match package.json.
- **Changed**: README polished: added license badge; tightened intro; standardized API key placeholder to `sk-or-…`; broke long Docker commands into multi-line; converted notes/warnings to GitHub alert blockquotes; broke dense Web-auth paragraph into a list; removed redundant platform summary table; added macOS not-supported note; added Disclaimer to ToC; improved Contributing with links to issues and dev docs; cleaned up extra blank lines.
- **Changed**: README restructured: quick start (Docker/Windows/Linux) first with copy-paste commands; installation by platform (Windows, Linux, Docker) with API key note for Docker (env only); new “Getting an OpenRouter API key” section; configuration and environment in one section; links to dev/DEVELOPMENT.md and dev/SYSTEM-OVERVIEW.md; releases and tags; contributing and issue reporting. Development link fixed to dev/DEVELOPMENT.md.
- **Changed**: Upgraded dependencies: `diff` ^6.0.0 → ^8.0.3 (fixes low DoS vuln), `electron` ^40.8.0 → ^41.0.2, `@fluentui/react-icons` ^2.0.320 → ^2.0.321, `webpack-cli` ^6.0.1 → ^7.0.0. Added pnpm override `yauzl` >=3.2.1 to fix moderate vulnerability in `electron` > `extract-zip` > `yauzl`. `pnpm audit` now reports no known vulnerabilities.
- **Changed**: dev/i18n.md annex now includes full source of both extract-strings.js and generate-translations.js so developers can reuse them as a base; intro text updated accordingly.
- **Changed**: Dev docs updated: DEVELOPMENT.md (troubleshooting link fixed to DEVELOPMENT.md); i18n.md (i18next-scanner documented as Parser used by extract script, dependencies list and annex updated).
- **Fixed**: String extraction no longer mangles `\n` (and other JS escape sequences) inside `t()` calls: `scripts/extract-strings.js` now uses `i18next-scanner`'s AST-based parser instead of regex, so multi-line confirmation messages (e.g. "Revoke all sessions…?\n\nThey will need to log in again.") are stored correctly in `strings.json` and locale files.
- **Changed**: All confirmation dialogs now use `\n\n` to separate the action question from the consequence/warning ("This cannot be undone.", "They will need to log in again.", etc.), making destructive warnings more visually distinct.
- **Fixed**: Settings > Transform prompts: delete confirmation message now replaces `{{name}}` with the prompt name using `interpolateTemplate`.
- **Fixed**: Resolved all ESLint errors and warnings (unused imports, useless try/catch wrappers, unnecessary hook dependencies).
- **Changed**: Dashboard export (By Model, By Day, All Calls): CSV and XLSX headers now use translated labels via t() so exported files match the UI language.
- **Changed**: Dashboard > By Model tab: chart first, then download row directly above the table.
- **Changed**: Dashboard > By Model table and export: removed Avg translation and Avg rewrite columns (table and JSON/CSV/XLSX).
- **Fixed**: Dashboard > By Model table: Model column no longer overflows into Translation calls; first column constrained with ellipsis and full name on hover.
- **Changed**: Transform “translate output to target language” instruction moved from hardcoded string in apiService to `prompts.json` (`shared.transform.translateToTargetLang`) with `{{targetLang}}` placeholder.
- **Fixed**: Transform prompt temperature from the slider is now rounded to 2 decimal places when saving, so JSON no longer shows spurious digits (e.g. `0.7000000000000001`).
- **Fixed**: When the user aborts translation/rewrite/transform, the output box now shows the localized "Translation/Rewrite/Transform stopped by user." instead of the raw "Error: signal is aborted without reason". The API layer (apiService) now treats any abort-like error (AbortError or message containing "aborted" / "failed to fetch") as cancellation and returns `{ cancelled: true }` so the UI always receives `result.cancelled` and displays the friendly message regardless of how the environment reports the abort (e.g. DOMException with "signal is aborted without reason"). Error and partial-result messages use `interpolateTemplate()` for i18n placeholder substitution.
- **Changed**: Output panel header (Cost and Total) now uses the same Cost fraction digits style (Settings > General) as Cost tracking and Dashboard; value through cents uses success green (like Dashboard/Cost tracking) so it stands out from the muted header; fraction digits keep the chosen style.
- **Changed**: Rewrite diff now uses jsdiff (`diffWordsWithSpace`) for the main diff instead of custom line/word alignment; consecutive removed+added pairs use `diffChars` for in-word granularity.
- **Fixed**: Browser console: Griffel no longer warns on unsupported `borderColor` (replaced with longhand border-*-color in Dashboard filter button); invalid CSS selector `& $table` replaced with `& table` in tab table content; Fluent Option in ModelSelector given `text` prop when children include non-string nodes; Recharts ResponsiveContainer width/height -1 warnings addressed by giving chart containers a minimum height (summaryChartContainer, byUsageChartContainer, and By Model chart wrapper).
- **Changed**: Model selector in header updates immediately (optimistic) and persists in background; no loading feedback.
- **Changed**: Provider icons use in-memory URL cache and are preloaded at startup for faster, cached display. Provider .ico files (from renderer/assets) are inlined in webpack (asset/inline) in both dev and production so they load with the bundle and require no network requests; only the favicon (images/) remains an emitted asset. CSP updated with img-src 'self' data: so inlined data-URL icons are allowed.
- **Changed**: Initial config load runs locale and (web) auth/API status in parallel to reduce startup time.
- **Added**: api_calls stores input and output character, word, and paragraph counts for translate/rewrite/transform calls; Dashboard > All Calls expanded row shows these stats (Input/Output chars · words · paragraphs); export (JSON/CSV/XLSX) includes the new columns.
- **Changed**: Dashboard > Summary: KPI cards use horizontal layout (label beside value) when viewport height is small (max-height 480px), with minimal padding to avoid text cropping; KPI grid has 4 equal rows so Avg TPS card sizes like the others; Summary tab allows vertical scroll when content exceeds the panel so all information is visible without cropping.
- **Added**: Dashboard > By Day and By Model tabs: Download block (JSON, CSV, XLSX) with same styling and strategy as All Calls - export columns use key/labelKey, headers use `t(labelKey)`, XLSX uses header fill BDD7EE, cost columns 6 decimals, TPS 1 decimal (By Model), auto column widths; filenames transrewrt-byday and transrewrt-bymodel. Shared export helpers in `exportUtils.js`.
- **Changed**: Removed `total_cost` column from api_calls: no longer in schema, INSERT, or SELECT; migration drops it on existing DBs (SQLite 3.35+). GET /api/calls/total-cost still returns SUM(cost) for settings. Dashboard > All Calls export and table no longer reference it.
- **Changed**: Dashboard > All Calls export: CSV and XLSX use table labels in the header; JSON uses field names. XLSX: cost column 6 decimals, TPS 1 decimal.
- **Changed**: Dashboard > All Calls XLSX export now uses the same formatting as Settings > Transform: header row with light-blue fill (BDD7EE) and bold, top-aligned cells, and auto-adjusted column widths (min 10, max 80 chars).
- **Added**: Dashboard > All Calls table shows 7 columns (ID, Timestamp, Type, Username, Model, Cost, TPS); row click expands the row inline to show full call details in a two-column label+value layout below the selected row; side drawer and call details modal removed.
- **Added**: Dashboard All Calls: download full table as JSON, CSV, or XLS from the pagination row (icon + Download: JSON/CSV/XLS buttons, blue-tone styling); files named transrewrt-calls.json / .csv / .xlsx.
- **Changed**: Dashboard > All Calls table uses smaller font (12px) for header and body; header labels wrap at word level instead of truncating.
- **Changed**: api_calls now stores `prompt_tokens` and `completion_tokens` (from API `usage`) instead of `request_bytes`/`response_bytes`; existing rows migrated with 4 bytes = 1 token.
- **Fixed**: Translate/rewrite/transform language dropdowns (From, To, Target): selection now updates to show the chosen language. The "Detect Language" option now uses a constant value so it matches `selectedOptions` in all locales; the Dropdown is keyed by the current selection so the trigger display stays in sync. On the Translate page, sync-from-settings effects no longer list sourceLanguage/targetLanguage in their dependency arrays, so they do not overwrite the user's dropdown selection when it changes (updateSettings is async; the effect was previously re-running with stale settings and reverting the selection).
- **Changed**: RTL language set in `src/renderer/i18n.js` (RTL_LANGS) expanded to cover all common RTL scripts: Arabic-script (ps, sd, ug), Thaana (dv), Adlam (ff), Syriac (syr, aii, cld), N'Ko (nqo), Hanifi Rohingya (rhg), in addition to existing ar, he, fa, ur, yi.
- **Added**: UI language selector (header globe): language code badge (e.g. pt-BR) to the right of the globe, styled like the Transform "Generate prompt" button.
- **Added**: dev/i18n.md - documentation for i18n (multi-language support): tools (i18next, react-i18next), usage in source code, translation workflow, pnpm scripts, file layout, RTL, and annex with full source of extract-strings.js and generate-translations.js.
- **Fixed**: Web mode config write loop after login: `updateSettings` and `setSetting` in AppContext are now memoized with `useCallback` so App.js sync effects do not re-run every render and flood POST /api/config.
- **Changed**: Web login is now a full-screen login page instead of a modal; page includes app logo, login box, top-right language selector (Languages icon + dropdown), and a first-time login message (default admin credentials) when the admin account has never been used.
- **Fixed**: React/ESLint: enabled Babel automatic JSX runtime (`.babelrc` preset `runtime: "automatic"`) so React no longer needs to be in scope for JSX; removed redundant `import React` from renderer components and hooks. Replaced `React.useRef` with `useRef` in SettingsDialogTransformPromptsTab. Aligns build with ESLint’s `react/react-in-jsx-scope: off` and resolves `no-unused-vars` for React.
- **Fixed**: ESLint: disabled `react/react-in-jsx-scope` and `react/jsx-uses-react` for React 17+ new JSX transform; removed unused `import React` from 7 files. Fixed remaining 3 errors: moved ref updates out of render in `useDebouncedProcess.js` (set `settingsRef.current` inside effect, added `realTimeDelay` to deps) and `useKeyboardShortcuts.js` (removed ref, use `currentView` in effect deps); fixed `Number(selected.temperature) ?? 0.4` to `|| 0.4` in `useProcessing.js` (no-constant-binary-expression). `pnpm lint` now passes with zero errors.
- **Fixed**: Lint Pass 4: resolved all `react-hooks/exhaustive-deps` warnings by adding missing dependencies (settings, targetLanguage, sourceLanguage, updateSettings, setSetting, locale, t, shouldAutoProcessRef) where correct, and adding targeted eslint-disable comments with reasons where deps are intentionally omitted (fetchKeyInfo/costApi in SettingsDialogCostTrackingTab); removed unnecessary `isWeb` from dependency arrays where the rule flagged it as invalid.
- **Fixed**: Lint Pass 3: resolved all `react-hooks/set-state-in-effect` errors by deferring setState calls in useEffect with `queueMicrotask()` in App.js (sync from settings: target/source language, mode, view, rewrite style, config loaded), DashboardPage.js (loadSummaries, loadAllCalls, loadByDayPaginated), SettingsPanel.js (local settings/tab restore, activeTab correction, models loading), and Transform modals (TransformGenerateModal, TransformImproveModal, TransformTranslateModal reset when open).
- **Fixed**: Lint Pass 2: resolved all `react/prop-types` errors by enabling the rule in ESLint (merge React recommended rules with project rules), adding `prop-types` devDependency, and defining `propTypes` (and removing unused `React` imports) on renderer components: modals (ApiKey, Confirm, ChangePassword, Login, Transform*), selectors (Language, Model, Style, TransformPrompt), panels (ResizablePanels, TextPanel, MainContent, Sidebar), Dashboard tabs, Settings panel and tabs (Api, General, Languages, Models, CostTracking), TransformPromptEditor, TransformTestPanel, AppContext AppProvider, and inline LoadingLogoSvg in App.
- **Fixed**: Lint Pass 1: added `@eslint/js` devDependency so `pnpm lint` runs; declared webpack DefinePlugin globals in ESLint (`__REPO_URL__`, `__APP_VERSION__`, `__APP_DESCRIPTION__`, `__APP_AUTHOR__`, `__APP_LICENSE__`, `__DEV__`); fixed `no-undef` in main (requestPath in app protocol catch); fixed `no-empty` and unused catch params (scripts, main, renderer, server); removed or omitted unused vars in targeted files; fixed `no-constant-binary-expression` for `Number(...) ?? 0.4` (use `|| 0.4`).
- **Fixed**: `scripts/check-node-version.js` no longer passes a string to `process.exit()` when the server exits due to a signal (e.g. SIGSEGV); exit code is now always numeric.
- **Changed**: Renamed transform modals: GeneratePromptConfigModal → TransformGenerateModal, TranslatePromptFieldsModal → TransformTranslateModal, ImprovePromptConfigModal → TransformImproveModal (files and component names).
- **Added**: Transform Prompt Editor: "Generate prompt with AI" button and modal to generate prompt config (name, instructions, role, output description, temperature) from a short user description; Cancel closes or aborts generation.
- **Changed**: Sidebar and app-wide Transform mode icon updated from Sparkles to WandSparkles.
- **Changed**: "Generate prompt with AI" button styled like "Load sample prompts" (green tint); layout: second row between "Back to Run" and "Prompt name", right-aligned, minimal spacing.
- **Fixed**: Transform Prompt Editor modals (Generate prompt, Translate prompt fields, Improve prompt): modal width capped; long error messages wrap and modal grows vertically instead of horizontally.
- **Fixed**: Web mode: margin around the app no longer shown on login (or when unset); margin only when Settings > "show a margin around the app" is explicitly enabled; default is no margin.
- **Changed**: Bumped dependencies to latest: eslint-plugin-react ^7.37.5, eslint-plugin-react-hooks ^7.0.1, globals ^17.4.0, i18next ^25.8.17, react-i18next ^16.5.6, recharts ^3.8.0, lucide-react ^0.577.0. ESLint kept at ^9.15.0 (ESLint 10 not yet supported by eslint-plugin-react). React-hooks config uses flat format for compatibility.
- **Fixed**: Sidebar user menu now closes when any option is selected (User management, Change password, Sign out); popover is controlled so it closes on item click.
- **Fixed**: Sidebar user menu "User management" now switches to Settings > Users when Settings is already open on another tab (e.g. About); previously the Users tab was only applied on initial Settings open.
- **Fixed**: Docker/web: avoid Fluent UI positioning error "Cannot read properties of undefined (reading 'onPositioningEnd')" by passing explicit positioning objects with `onPositioningEnd` to Popover in Sidebar and HeaderLanguageSelector.
- **Added**: ESLint with React and React Hooks (flat config); `lint` and `lint:fix` scripts.
- **Changed**: Config and code refactor: `available_languages` renamed to `top_languages`; list state/prop `languages` renamed to `topLanguages` in context and components. Default config key in `config_default.json` is `top_languages`. Existing configs with `available_languages` are migrated to `top_languages` on load.
- **Fixed**: Translate/rewrite/transform language selectors now show "English Name / local name" for all options; most-used (top) languages were shown in a different format when stored or resolved by code (e.g. "fr") - lookup now supports both English name and BCP 47 code via `findUILanguageEntry`.
- **Security**: Removed unused `xlsx` dependency to resolve `pnpm audit` high vulnerabilities (SheetJS prototype pollution and ReDoS); XLSX export uses `xlsx-js-style` only.
- **Fixed**: Docker web image: server now resolves `dist` as `/app/dist` instead of `/dist` by using one level up from `__dirname` (Docker layout is `/app/server`; dev layout is `src/server`). Added `dist` to `.dockerignore` so host build output is not copied into the image.
- **Added**: Improve prompt configuration modal: label "Select the model to improve the prompt" above the model selector; Translate prompt fields modal: label "Model to translate" beside the model selector.
- **Added**: Transform prompt editor: "Improve prompt configuration" button to get model-suggested improvements for role, instructions, and temperature; improved JSON is applied to the form.
- **Added**: Model selector in both "Improve prompt configuration" and "Translate prompt fields" modals (reuses header ModelSelector); the model chosen in the modal is used for the API call.
- **Added**: Cancel in both modals aborts the in-flight translation or improvement request (AbortController).
- **Changed**: Translate prompt fields and Improve prompt (button) now log to api_calls with `transform_prompt` "Translate prompt (button)" and "Improve prompt (button)" respectively; dashboard Usage split and cost-by-transform-prompt show them under those names.
- **Changed**: generate-test-data.js assigns each generated api_calls row a random username from the existing users table when present; otherwise username is null.
- **Changed**: Transform prompt editor: Temperature is now a slider (0–1, step 0.05) with value shown above and labels "Deterministic/Consistent output" (left) and "Creative/Varied output" (right).
- **Added**: Transform prompt edit mode: button to translate prompt name, instructions, model role, model instructions, and output description to a selected language (same list as Translate destination).
- **Changed**: Transform "Translate prompt fields" now sends a single JSON object to the model (same system/user prompt style as generate-translations) and applies the returned object in one step instead of translating field by field.
- **Changed**: Translate prompt fields system prompt moved from apiService.js to prompts.json (`translate_prompt_fields.system`).
- **Changed**: generate-translations.js: final step prunes from strings.json any translation keys for locales not listed in ui-languages.json (e.g. after removing a UI language).
- **Changed**: generate-translations.js: on chunk failure (non-JSON or API error), retries with alternative models in order; if all fail, skips the chunk and continues.
- **Changed**: i18n locale loaders are built dynamically from `ui-languages.json` (excluding en/en-GB) instead of a hardcoded map.
- **Added**: Numbers, dates, and relative time are localized by UI language: decimal separator (e.g. comma in pt-BR), date/time format (e.g. 09.03.2026 in de, 2026/03/09 in ja), and relative time (e.g. "há 5 min", "vor 5 Min.") use `Intl` with `i18n.language`; "Just now" is translatable; `formatDateTime`, `formatRelativeTime`, `formatDecimal`, and locale-aware cost/integer formatters in formatUtils and costUtils.
- **Fixed**: Login, Add user, and other form inputs no longer get a white background when Chrome/Google password manager autofills; `color-scheme: dark` / `light` is set on body so the browser uses theme-appropriate native form and autofill styling.
- **Fixed**: Add User (and other) modal input borders no longer show broken corners when Chrome/Edge autofill styles apply; autofill override now uses `border-radius: inherit` so the inset shadow follows the input wrapper's rounded corners.
- **Fixed**: extract-strings.js now correctly handles quotes inside t() strings: apostrophe in double-quoted strings (e.g. `t("Don't show the changes")`) and double quotes in single-quoted strings (e.g. `t('show the "changes"')`) are treated as literal characters by using one regex per delimiter.
- **Removed**: Rewrite "From" / input language selector and `withSourceLanguageLine` from rewrite prompt; rewrite no longer takes a source language (model infers from content).
- **Fixed**: Rewrite page "Show changes" diff view now preserves empty lines; diff is computed line-by-line and the diff container uses white-space: pre-wrap so newlines render.
- **Changed**: Translate and transform: "To" / "Target" language list now matches the "From" (origin) list (selected + remaining languages), without "Detect Language"; transform keeps "No target language / model decides." as first option.
- **Changed**: Rewrite: added "From" language selector to the right of the Style selector with 32px gap; reuses the same source language state as translate.
- **Changed**: Rewrite prompt now instructs the model to use the selected input language when the user selects any language other than "Detect Language".
- **Changed**: Translation uses a single prompt in prompts.json; optional source-language line (`withSourceLanguageLine`) is in JSON and included when user selects a language other than "Detect Language". Rewrite optional line (`withSourceLanguageLine`) moved from code into prompts.json for consistency.
- **Security**: Translation, rewrite, and transform prompts now instruct the model not to follow any instructions embedded in the content between `<translate>`, `<rewrite>`, or `<transform>` tags to reduce prompt-injection risk.
- **Added**: Stats line (Chars / Words / Paragraphs) in workspace panels is translatable via `getInputStats`/`getOutputStats(text, t)`.
- **Added**: Settings > Models tab: sort dropdown options are translatable (Cost ↑/↓, Model A→Z/Z→A, Provider A→Z/Z→A).
- **Fixed**: After deleting the current user and logging in as another admin (Settings > Users tab open), the app no longer shows "failed to fetch" or stale error; post-login state is refreshed (global error cleared, API status and users list refetched).
- **Added**: Settings > Cost tracking: translatable "(reset {{when}})" for API key usage limit reset text.
- **Fixed**: i18n strings ending (or starting) with a space: extract stores keys trimmed; runtime `t()` now normalizes the lookup key with trim so `t("Label ")` resolves to the same translation as `t("Label")`.
- **Added**: generate-translations.js logs per-language cost (tokens and USD) and running total so far after each locale.
- **Changed**: i18n: en-GB is the source language (key-as-default); generate-translations no longer translates to en-GB, does not write en-GB.json, strips en-GB from strings.json when run, and exits cleanly for `--locale en-GB`. Removed unused en-GB.json; en-GB remains in ui-languages so users can select "English (UK)".
- **Changed**: Settings > Users tab: delete button hidden for the last remaining admin (at least one admin required).
- **Changed**: `reset-web-password` accepts optional username (`[username] <new-password>`; default username "admin"); creates user with admin role if missing; always clears "must change password on next login" for the reset user.
- **Fixed**: Sidebar user menu "User management" now opens Settings with the Users tab selected (pass explicit openToTab instead of relying on async setSetting).
- **Changed**: Edit User modal: same layout and field order as Add User modal (instruction text, username with placeholder Generate button, new password with Generate button, Admin user checkbox, Must change password checkbox); role shown as checkbox instead of dropdown.
- **Changed**: generate-translations.js: each console log line is prefixed with timestamp (HH:MM:SS).
- **Added**: generate-translations.js: `--locale` / `-l <code>` to translate only one locale (e.g. `-l pt-BR`).
- **Added**: generate-translations.js: end-of-run totalizer with time elapsed (HH:MM:SS), total strings translated, total tokens (prompt + completion); cost shown when reported by API.
- **Changed**: Edit User modal: username field is editable for all users (including admin).
- **Changed**: Settings > About: license and disclaimer lines show English + translation when UI language is not English (e.g. "Licensed under ▪ …"); when UI language is English (en, en-GB, en-US, etc.) only the translation is shown to avoid duplication.
- **Changed**: Settings > Users tab: search and Add user on one row; session timeout moved from Authentication to Users with tooltip "Applies to new logins"; action icons larger and not muted; search icon inside input on the right; section label and description with gap to content.
- **Changed**: All app tables (Dashboard By Day, By Model, All Calls; Settings Cost Tracking, Transform Prompts) use the same header and footer style as Settings > Users tab (neutral background, 1px border); tables with a total row use the same style for the footer.
- **Removed**: Settings > Authentication tab (deprecated).
- **Added**: "Change password" modal opened from sidebar (web); replaces opening Settings to Auth tab.
- **Changed**: Sidebar user menu "Change password" icon from Key to KeyRound.
- **Changed**: Sidebar User row: initials avatar replaced with User icon (same size as other sidebar icons); role badge removed; username label larger and bold; separation line between nav and user row removed.
- **Changed**: Session remaining no longer shown in UI; session expiry still enforced server-side; `web_session_expires_at` removed from config API response.
- **Fixed**: Set password and Revoke/Delete user modals: "{{name}}" is now replaced with the actual username (use interpolateTemplate when key-as-default bypasses i18next interpolation).
- **Added**: Edit User modal: optional "New password" field to change the user's password when saving; leave blank to keep current password.
- **Fixed**: Add User modal: username input now matches the password input width; Generate button aligned vertically with the password input at the same height.
- **Fixed**: Modal input white background: all modal Inputs (Login, Add User, Edit User, Change password, Set password) now use Fluent v9 `appearance="filled-darker"` which correctly applies a dark fill on the wrapper element. Removed all CSS hacks (`.app-modal` rules, `& input` makeStyles rules) that targeted the wrong DOM element. Added CSS override for Chrome/Edge `-webkit-autofill` so autofilled fields match the dark theme instead of showing a white/light background.
- **Changed**: PasswordInput refactored to Fluent v9 only: uses `Field` + `Input` with `appearance="filled-darker"` and `contentAfter` (inline icon toggle with EyeRegular/EyeOffRegular from `@fluentui/react-icons`). Eye icon uses a plain `<span>` with `background: none` so it inherits the input's fill color. Removed lucide-react and Fluent Button from this component.
- **Changed**: Modal forms (LoginModal, Add User, Edit User) use Fluent `Field` for label + control consistently.
- **Added**: Shared `PasswordInput` component (`src/renderer/components/PasswordInput.js`) with show/hide eye button, theme-aligned background and text (tokens), optional label, and optional controlled visibility for "Generate" flows. Reused in Login modal, Add User and Set password modals, and Settings → Authentication.
- **Changed**: Log in and Add User modals: input and password fields use the same background as other app inputs; fixed white inner input by targeting the native `input` with `colorNeutralBackground2` via a wrapper `& input` selector so Fluent Input’s inner element is styled. (`tokens.colorNeutralBackground1` / `colorNeutralForeground1`) instead of defaulting to white.
- **Changed**: Add User modal: form layout aligned with Login modal (labels on top, instruction line); password input uses same style as Settings → API (relative wrapper, show-password button with white icon and same background as input); Role replaced by "Admin user" checkbox (off by default); "Must change password on next login" checkbox on by default; Generate button includes RectangleEllipsis icon.
- **Changed**: Login modal: title always "Log in"; instruction "Enter your credentials to access your account" (or session-expired message); username and password inputs use same style as Settings → API (label on top, password with show/hide button, white icon, button background matches input). Change-password step uses same input style for new and confirm password fields.
- **Added**: Add User form: "Generate" button next to the password field creates a random 10-character password (mixed case letters and numbers) and shows the password so the user can copy it; show/hide password (eye) button remains in the input.
- **Changed**: Change password flow no longer asks for current password: only new password and confirmation (login forced-change, Settings → Authentication, and API). Show/hide password button (eye icon) added to all password inputs (LoginModal, Auth tab, Users tab Add/Set password); icon styled in white.
- **Fixed**: Add User form: username and other Fluent Input values no longer show "[object Object]" (use `data.value` from Input onChange). Add User form now includes "Must change password on next login" checkbox; server and API accept `must_change_password` on user creation.
- **Added**: Multi-user support for the web app: users table and per-user sessions; login with username and password; admin and user roles; Settings → Users tab (admin only) to add, edit, delete users, set passwords, and revoke sessions; sidebar user menu (sign out, change password, User management for admins); Dashboard user filter (admin: all users or specific user); optional “must change password” flag; admin user cannot be deleted. Electron app shows OS username in sidebar (no login). Removed single shared password and `web_password_hash` from config.
- **Added**: `scripts/reset-web-password.js` and npm script `reset-web-password` to reset the admin user's password (updates users table; creates admin if missing). Documented in README and included in Docker image.
- **Changed**: Extracted Dashboard page makeStyles to `DashboardPage-styles.js` and shared chart constants to `DashboardPage-constants.js`; components import from these files. Nomenclature uses hyphens (DashboardPage-styles, DashboardPage-constants) for consistency.
- **Changed**: Moved default config and prompt files from `config/` to `src/config-defaults/` (config_default.json, prompts.json, transform-prompts.json). Updated main, server, renderer, scripts, and docs.
- **Changed**: `generate-test-data` uses transform prompt names from `src/config-defaults/transform-prompts.json` only (no hardcoded list); loads file when `--transforms N` > 0.
- **Added**: `generate-test-data` script generates random transform transactions (type `transform`, `transform_prompt`, optional `target_lang`); default 150 transforms, overridable with `--transforms N` / `-f`. Table and INSERT include `transform_prompt` column.
- **Changed**: `generate-test-data` script requires one of `--web` or `--app`: `--web` writes to repo `./data`, `--app` uses app data dir (CONFIG_PATH / APPDATA / repo data). No mode shows usage; `--help` shows usage and exits.
- **Fixed**: Dashboard By Usage: Rewrite style chart tooltip now uses the same default formatter/labelFormatter pattern as Translation target language and Transform prompt (consistent layout and styling).
- **Fixed**: Bar charts: tooltip no longer shows white background on hover (cursor set to transparent); hovered bar now uses a distinct color via `activeBar` (single-series: `barFillHover`; stacked: translation/rewrite/transform hover colors in `DashboardConstants`).
- **Fixed**: Dashboard Summary "Cost over time" chart tooltip now shows correct labels (Translation / Rewrite / Transform cost) instead of all "Transform cost"; formatter uses payload `dataKey` because Recharts passes the series display name as `name`.
- **Changed**: Moved `formatInteger`, `formatDurationMs`, and `interpolateTemplate` from `DashboardConstants.js` to `src/renderer/utils/misc/formatUtils.js`; dashboard and tab components now import them from formatUtils.
- **Changed**: Split dashboard tab content into separate components: `DashboardTabSummary.js`, `DashboardTabByUsage.js`, `DashboardTabByModel.js`, `DashboardTabByDay.js`, `DashboardTabAllCalls.js` (tab label = component name suffix). Shared chart/formatter helpers moved to `DashboardConstants.js`. `DashboardPage.js` keeps layout, filter row, tab list, state, data loading, and ConfirmModal.
- **Changed**: Moved `SettingsDialogCostTrackingTab.js` and `DashboardPage.js` from `src/renderer/features/dashboard/components/` to `src/renderer/components/`; `useCostTracking.js` from `src/renderer/features/dashboard/hooks/` to `src/renderer/hooks/`; `costUtils.js` from `src/renderer/features/dashboard/utils/` to `src/renderer/utils/misc/`. Dashboard barrel (`features/dashboard/index.js`) re-exports from new paths.
- **Changed**: Dashboard > All Calls: default rows per page is 10 (was 50). Table height fits content with a maximum of the viewable area to remove the gap below the last row when showing 10 rows.
- **Changed**: `generate-translations.js` now sends `max_tokens: 8192` by default; add `--max-tokens` / `-t` to override. Reduces cost and avoids 402 when account limit is below model default.
- **Security**: API key and Key Seed are never sent to the renderer: main sanitizes `config:get` (omits `api_key`/`key_seed`, adds `api_key_configured`/`key_seed_configured`), and `config:setAll` never merges secrets from the payload. Renderer obtains credentials only via `getSecretsForRequest` IPC for building requests; Test API runs in main via `api:testConfiguration` IPC.
- **Changed**: Settings > API Config tab: when a key/seed is configured, show "API key is configured" (or key seed) with an Edit button (pencil icon) instead of the value; in edit mode show an empty input with Eye/EyeOff password visibility toggle and Save/Cancel. OpenRouter API Key block moved above API URL. Removed custom Show checkbox; use standard password visibility control.
- **Added**: GitHub Actions release workflow (`.github/workflows/release.yml`): builds Windows x64 NSIS installer, Linux x64 AppImage, and multi-arch Docker image (linux/amd64, linux/arm64) pushed to GHCR; triggered on `v*` tags and workflow_dispatch; uses native runners (no QEMU).
- **Changed**: Electron-builder win target set to explicit `x64`; added linux `AppImage` target for x64.
- **Removed**: `scripts/docker-deploy.sh` and `docker:deploy` npm script; superseded by GitHub Actions workflow.
- **Fixed**: Settings > General font sample label "Sample:" was invisible (white on light background); use theme token and Label. i18n: `t('Sample:')` returned empty because i18next treats `:` as namespace separator; set `nsSeparator: false` in i18n init so keys containing colons (e.g. "Sample:") resolve correctly.
- **Removed**: Settings > General "Application" section (Interface language dropdown); language is now selected via the header globe on all pages.
- **Added**: Diff toggle on Rewrite output panel to show input vs output differences (same / removed / added styling). "Show diffs" button (File / FileDiff icon) to the left of Copy; diff view uses position-based word alignment and character-level diff for changed words (white unchanged, muted strikethrough removed, blue added).
- **Removed**: Input and output text color support (config keys and UI); single font/size sample kept in Settings > General.
- **Changed**: Moved `server/` and `shared/` under `src/` so all application source lives in `src/` (src/server, src/shared). Updated scripts/check-node-version.js, Dockerfile (COPY src/server and src/shared), and docs.
- **Changed**: Shared DB schema and queries: added `src/shared/db/appSchema.js` as single source of truth for api_calls and custom_prompts schema (applyAppSchema) and all shared SQL (sql.*, buildWhereFromTo, promptTargetLanguageToDb, replaceWhere). Electron `src/main/appDb.js` and server `src/server/db/appDb.js` + routes (calls, customPrompts) now use it; server keeps server-only sessions table in appDb. Schema or query changes are made in one place.
- **Changed**: Codebase refactor (plan codebase_refactor_plan_00ed772b): split `src/main/main.js` into `configPath.js`, `encryption.js`, and `ipc/` (configIpc, apiIpc, windowIpc); split the server (src/server) into `db/appDb.js`, `utils/configFile.js`, and `routes/` (config, auth, apiProxy, calls, customPrompts, status); reorganized `src/renderer/utils/` into domain subfolders `config/`, `api/`, `security/`, `misc/`; introduced `src/renderer/features/dashboard/` (DashboardPage, SettingsDialogCostTrackingTab, useCostTracking, costUtils) with barrel `index.js`. Transform and settings features remain in components/ for a follow-up.
- **Changed**: SYSTEM-OVERVIEW.md updated to match current codebase: i18n (react-i18next, locales, ui_locale) and renderer folder structure (locales/, index.js i18n entry).
- **Changed**: Extract script no longer adds UI_LANGUAGES `label` to strings.json (label is already in the local language; only `englishName` is extracted for translation).
- **Added**: `generate-translations.js`: `--dry-run` / `-d` to show what would be translated per language (no API calls or file writes); `--show-strings` / `-s` to list source strings that need translation (key + text) per language; `--help` / `-h` documented in script and help output.
- **Changed**: Dashboard "All Calls" table refactored: new layout with separate header and body tables, shared colgroup (flexible column widths, min 1090px); vertical scrollbar overlays last column (no extra width), horizontal scrollbar always visible; columns truncate with ellipsis; header fixed, body scrolls from header bottom to table bottom; paging and cell content unchanged.
- **Added**: All Calls tab rows-per-page selection is persisted in state (`all_calls_page_size`); value is restored on next load (Electron: state.json; web: config blob).
- **Fixed**: API key modal no longer flashes on load when the API key is already configured; modal is shown only after initial config has been applied (`configLoaded`).
- **Changed**: Dashboard summary tab "By type" renamed to "By Usage" (tab value `byusage`); label and i18n key "By Usage" with locale translations.
- **Changed**: Removed `MODEL_DECIDES` constant; transform target "no target / model decides" now uses literal value `"auto"` and displays "No target language / model decides." in the UI (source language still shows "Detect Language").
- **Changed**: Settings tab labels use left/right chevrons to slide the tab strip instead of a horizontal scrollbar; scrollbar is hidden and chevrons are disabled when there is nothing to scroll.
- **Fixed**: Settings > Languages tab grid no longer overlaps at some window widths; column count is now derived from the grid container width (ResizeObserver) with a minimum column width so long labels fit without overlap.
- **Changed**: Settings > Languages tab checkbox list now shows predefined languages via `getUILanguageLabel`: "label / englishName" when they differ, else "label"; custom languages unchanged.
- **Changed**: LanguageSelector dropdown now shows UI_LANGUAGES entries as "label / englishName" when they differ, or just "label" when equal; uses existing `getUILanguageLabel` from `utils/languageDisplay.js`. Non-UI_LANGUAGES options and special options (separators, Detect Language, Auto) unchanged.
- **Changed**: Workspace panel helpers (`getTranslatePanels`, `getRewritePanels`, `getTransformPanels`) now accept consolidated arguments: `{ common, input, output, options }` instead of long flat parameter lists. `App.js` builds `common` (t, styles, settings, isProcessing, handleRunAction, lastRunModel, outputMeta), `input`/`output` (text, setText, getStats, clear/copy, paste handlers), and mode-specific `options`.
- **Changed**: Consolidated language lists to a single source of truth: `src/renderer/locales/ui-languages.json` holds the full list; `constants.js` imports it as `UI_LANGUAGES`. Removed `ALL_AVAILABLE_LANGUAGES`, `CONTENT_LANGUAGE_TO_BCP47`, `LEGACY_LANGUAGE_NAMES`, and `normalizeLanguageName` from `languageConstants.js`. Content languages and scripts now derive from `UI_LANGUAGES` via `ALL_CONTENT_LANGUAGE_NAMES`, `isPredefinedContentLanguage()`, and `codeFromEnglishName()`. `extract-strings.js` and `generate-translations.js` read `ui-languages.json` for language names and locale generation. Config/persisted `top_languages` are used as-is (no legacy normalization).
- **Changed**: UI language options are now a single array `UI_LANGUAGES` with `code`, `label`, and `englishName`; `UI_LANGUAGE_ENGLISH_NAMES` removed. `getUILanguageLabel` uses `lang.englishName` from the same array.
- **Removed**: `getContentLanguageLabel()` function from `languageDisplay.js`; language selectors now consistently display simple names like "English (UK)" without bilingual labels.
- **Changed**: `LanguageSelector.js` simplified: now displays simple language names like "English (UK)" (matching UI style) instead of bilingual labels "British English / English (UK)"; removed `getContentLanguageLabel()` import and uses plain language names from props.
- **Changed**: `SettingsDialogLanguagesTab.js` updated: language labels now display simple names like "English (UK)" for all languages, consistent with other language selectors.
- **Fixed**: Content language labels (Settings > Languages, Translate/Transform dropdowns) now show "Native / English" even when locale files lack translated language names: fallback to `Intl.DisplayNames` so e.g. Portuguese UI shows "português (Brasil) / Portuguese (BR)" without running `i18n:translate`. Restored correct `LEGACY_LANGUAGE_NAMES` entry for "Brazilian Portuguese".
- **Changed**: Bilingual language labels now use slash separator (e.g. "Português (BR) / Portuguese (BR)") instead of parentheses to avoid nested "))" when the native name already contains parens.
- **Added**: Language selectors and Settings tabs now show language names in both UI language and English (format: native/translated name plus English, separated by " / " when different). Header UI language popover uses a two-column layout. Extract script extended to include `ALL_AVAILABLE_LANGUAGES` from `languageConstants.js` in `strings.json` so content language names can be translated. Regional variants use code form (e.g. "Portuguese (BR)", "Portuguese (PT)") with legacy migration for "Portuguese (BR)" → "Portuguese (BR)" and "Mandarin Chinese" → "Chinese (CN)".
- **Changed**: Unchecking "Use Transrewrt Proxy" in Settings > API now resets the API URL to the default OpenRouter URL (`https://openrouter.ai/api/v1`).
- **Fixed**: Cost Tracking tab no longer overwrites Total Cost from the database when returning to the tab; value set via "Sync with API key usage" (persisted in config) is now preserved in Electron as in web/Docker.
- **Fixed**: API key usage in Cost Tracking tab now loads correctly when using the Transrewrt proxy: main process builds key URL as `{proxyBase}/{rollingKey}/api/v1/key` when proxy is enabled; Cost Tracking tab treats proxy mode as OpenRouter so it still fetches key info.
- **Added**: Page headers now show the same icon as the sidebar for each view: Languages (Translate), PenTool (Rewrite), Sparkles (Transform), BarChart3 (Dashboard), Settings (Settings).
- **Changed**: Settings header now matches Translate/Rewrite/Transform/Dashboard: same padding (tokens), background, title font size (18px), title color, minHeight (52px), and title lineHeight (1.25) so all page headers have the same height.
- **Changed**: Dashboard page now uses the same header layout as Translate, Rewrite, Transform and Settings: toolbar with page title on the left and UI language selector on the right; in-page duplicate header and icon removed.
- **Fixed**: Dashboard pagination and confirm modal messages now show interpolated values (count, page, total, error, model) instead of literal `{{...}}` placeholders when using key-as-default (en-GB); added `interpolateTemplate()` helper for t() strings that are returned un-interpolated by i18next.
- **Fixed**: Loading screen no longer flashes "Loading…" text without logo: React loading screen now uses the same inline SVG logo as index.html instead of a PNG image, so the logo is visible immediately when React mounts (no async image load).
- **Added**: Electron IPC `getOpenRouterKeyInfo`: main process fetches OpenRouter key usage (same as server GET /api/key in web mode). Cost Tracking tab uses it in Electron so API key cost is shown via IPC instead of renderer fetch.
- **Added**: Loading screen with logo and "Loading…" while app/config is loading (web and Electron); pure HTML loading page in index.html with inlined SVG logo and dark background. Electron main/settings windows use `backgroundColor: "#1a1a1a"` to prevent white flash.
- **Changed**: Default UI language is UK English (en-GB): i18n `lng`/`fallbackLng`, `ui_locale` default, and first option in language list; keys are treated as UK English (no en-GB.json). en-US remains for American spellings via generated locale file.
- **Added**: UI languages: Arabic, Bengali, Dutch, English (UK), English (US), Hindi, Italian, Japanese, Javanese, Malay, Mandarin Chinese (zh-CN), Polish, Portuguese (PT), Punjabi, Romanian, Russian, Telugu, Ukrainian, Wu Chinese. Locale loaders in `i18n.js`, options in `constants.js` (`UI_LANGUAGES`), and `LANGUAGES` in `scripts/generate-translations.js`. Run `pnpm run i18n:extract` then `pnpm run i18n:translate` to generate new locale JSONs.
- **Added**: Globe icon in the right-panel header on all workspace pages (Translate, Rewrite, Transform run and edit); clicking it opens a dropdown to select the UI language. Only the icon is shown (no selected language label). Uses shared `UI_LANGUAGES` from constants; Settings > General now imports it from there.
- **Fixed**: Dashboard summary and all tabs: chart and UI labels are now translated (pie usage types, area/bar/line series names, chart titles “Cost over time”, “Cost by model”, “Usage split”, “Daily call volume”, “By day (paginated)”, “Cost by model (stacked)”, “All API calls (raw data)”, “Translation target language”, “Rewrite style”, “Transform prompt”; “No data”, “Total”, “Calls”, “Total cost”, “Prev”, “Next”, pagination text). Added `getUsageTypeLabel()` for translate/rewrite/transform in pie and tooltips.
- **Added**: Sidebar collapse: toggle (PanelLeftClose/PanelLeftOpen) on the same row as logo and app name, right-aligned and muted; collapsed state shows only logo and nav/footer icons.
- **Fixed**: Dropdown option strings (session timeout, cost fraction style, dashboard filters, cost delete range) now use `t()` at definition so they appear in `strings.json` and can be translated: session timeout and delete-range options use `useMemo` with literal `t("1h")`, `t("7 days")`, `t("> 1 month")` etc.; `getCostFractionStyleOptions(t)` and `getFilters(t)` in costUtils use literal `t("...")` for each option label.
- **Changed**: Hardcoded dropdown option labels are now translated: Cost tracking (delete range, Delete data/Deleting…), Settings > General (interface language, font section headers, cost fraction style), Settings > Auth (session timeout). Dynamically generated options (e.g. language list in Languages tab, transform prompt names) are left untranslated as requested.
- **Changed**: Documentation updated for current i18n implementation: plan (RTL, locale loaders, extract package.json description, generate-translations API_KEY/model/options/cost/errors), README (UI language feature, i18n subsection, commands), DEVELOPMENT.md (i18n section, commands, key files, plan link).
- **Added**: RTL support in i18n: when the UI language is a right-to-left language (ar, he, fa, ur, yi), `document.documentElement.dir` is set to `rtl`; otherwise `ltr`. Enables proper layout for Arabic, Hebrew, etc.
- **Changed**: About tab app description (from package.json) is now translated via `t(APP_DESCRIPTION)`. `scripts/extract-strings.js` includes `package.json` "description" in the extracted strings so it is not overwritten on extract and can be translated by `i18n:translate`.
- **Added**: `scripts/generate-translations.js` supports `--help` / `-h` to print usage and options.
- **Fixed**: `scripts/generate-translations.js` reads cost from `usage.cost` and `data.cost` (OpenRouter format) as well as `total_cost`; normalizes numeric/string values so total cost is calculated and displayed correctly.
- **Fixed**: `scripts/generate-translations.js` no longer uses assistant message prefill (conversation now ends with user message) so it works with providers that reject prefill (e.g. Anthropic); response is parsed as JSON array with optional markdown fence stripping.
- **Changed**: `scripts/generate-translations.js` on API or response error aborts with exit code 1 and prints all available debug info (HTTP status, response body, error object, parse errors, and failed content).
- **Changed**: `scripts/generate-translations.js` uses a system prompt for UI translation rules (placeholders, capitalization, tone, short strings), user message for data only, and assistant prefill `[` to force clean JSON array output without preamble or markdown.
- **Changed**: `scripts/generate-translations.js` tracks prompt/completion tokens and total cost from OpenRouter responses and prints usage and cost at the end; supports `usage.total_cost` or top-level `total_cost` when usage accounting is enabled.
- **Changed**: `scripts/generate-translations.js` accepts `--force` (or `-f`) to retranslate all strings and `--model` (or `-m`) to set the OpenRouter model (e.g. `pnpm run i18n:translate -- --force -m openai/gpt-4o`). (Previously `--retranslate`/`-r`.)
- **Changed**: Extended i18n: replaced hardcoded English strings with `t()` in Settings (Models, Languages, Transform prompts tabs), workspaces (Translate/Rewrite/Transform panels), TextPanel, TransformPromptSelector, TransformPromptEditor, TransformTestPanel, MainContent, App (loading and confirm modals), ModelSelector, and Dashboard (filters, tabs, KPI labels, table headers, confirm modal). Run `pnpm run i18n:extract` then `pnpm run i18n:translate` to refresh locale files.
- **Added**: Script `scripts/update-version.js` and pnpm script `update-version` to sync version from `package.json` into README and other files (e.g. badge).
- **Added**: Multi-language UI (i18n): interface language selector in Settings > General (Application section); key-as-default pattern with react-i18next (no en.json); optional pt-BR, de, fr, es via `pnpm run i18n:extract` and `pnpm run i18n:translate` (OpenRouter); `ui_locale` in config.
- **Changed**: Renamed `config/custom-prompts.json` to `config/transform-prompts.json`; Settings > Transform tab labels and export filenames now use "transform prompts" for coherence.
- **Changed**: Dashboard > All Calls: horizontal scroll is now a floating bar at the bottom of the visible area; table header row is sticky; vertical scrollbar is transparent and overlays the last column (no gap between table border and content).
- **Added**: Settings > General: sample of cost fraction digits style next to the dropdown so users can preview the format without changing tabs.
- **Fixed**: Settings > General: cost fraction sample now uses same formatting as Cost tracking tab (formatCost + success green styling) so subscript/muted/superscript/small are visible.
- **Changed**: Dashboard Summary tab: responsive layout (clamp for gaps, fonts, padding) to avoid scrollbar; KPI cards equal-sized with text confined (overflow/ellipsis); Usage pie chart larger (reduced margins, outerRadius 85%); Cost by model Y-axis width increased so model names show without truncation.
- **Changed**: Dashboard: new "By Usage" tab with Translation target, Rewrite style, and Transform prompt charts; Summary tab reformatted to 2×2 grid (KPI + Usage Split row 1, Cost over time + Cost by model row 2).
- **Changed**: Settings > Transform: on successful import, clear the export message so only the import result line is shown (one action log line).
- **Changed**: Transform run-mode target language: single option "Auto" (styled in blue like "Detect Language"); when selected or default, target language is not sent to the model.
- **Changed**: Dev documentation (SYSTEM-OVERVIEW.md, Development.md) updated: Transform in architecture diagram and folder structure; app DB (api_calls, custom_prompts) and appDb.js; hooks and config/custom-prompts.json; test note for Transform.
- **Fixed**: After saving a new transform prompt in edit mode, the new prompt was not selected because the settings-sync effect overwrote the selection with stale settings; effect now skips overwriting when the current selection matches a prompt whose name differs from settings (just-saved case).
- **Fixed**: App.js "Cannot access 'inputTextTransform' before initialization" by moving `activeModel` useMemo and `useTransformPrompts()` above the derived `inputText`/`outputText`/setters so transform state is defined before use.
- **Changed**: Renamed `src/main/costDb.js` to `appDb.js` and IPC channels from `costDb:*` to `appDb:*`; export `registerAppDbHandlers`. The module is the app’s SQLite DB (api_calls + custom_prompts), not only cost logging.
- **Changed**: Webpack performance limits raised (maxAssetSize / maxEntrypointSize to 2.5 MiB) so production build no longer warns on current vendors and entrypoint size.
- **Changed**: App.js refactored into maintainable modules: `setError` exposed from AppContext; styles moved to `useAppStyles.js`; format/stat helpers to `formatUtils.js`; `isWeb` and `REWRITE_STYLES` to `constants.js`; processing logic (translate/rewrite/transform run, timer, cost) to `useProcessing.js`; transform prompts CRUD and test panel to `useTransformPrompts.js`; workspace panels split by mode into `workspace/TranslateWorkspace.js`, `workspace/RewriteWorkspace.js`, `workspace/TransformWorkspace.js`; API key modal to `ApiKeyModal.js`. App.js is now a thin orchestrator.
- **Changed**: Transform page uses same behaviour as Translate: Enter key behaviour (Shift+Enter or Enter from Settings > General), auto-run on paste when "Auto-translate on paste" is on, auto-copy result when "Auto-result to clipboard" is on; Escape clears input. Real-time translation remains translate-only (no real-time transform).
- **Fixed**: Transform runs now update total cost in Settings > Cost tracking the same way as translate and rewrite; applyCostToResult is awaited so config is persisted before continuing.
- **Changed**: Transform page target language selector: added "(none)" option at top; when "(none)" or empty, target language is not sent to the model.
- **Changed**: Transform prompt config: "Target language (optional)" dropdown replaced with checkbox "Ask for target language". DB column `custom_prompts.target_language` is now boolean (INTEGER 0/1); migration converts existing TEXT values. Run mode shows target language selector only when flag is true; selector aligned with Output panel (same row as Translate "To:").
- **Changed**: Cost/API log now explicitly registers the selected target language for translate and transform runs (payload and logApiCall extra).
- **Changed**: Documentation (README, SYSTEM-OVERVIEW) updated with app description: translate, rewrite, and transform text using AI.
- **Fixed**: CSV import (Settings > Transform): correctly decode escaped quotes (`""` → `"`) so fields containing quotation marks round-trip; export was already RFC 4180–compliant.
- **Added**: Cancel button in prompt edit form (between Save and Delete) that returns to execution mode (same as Back to Run).
- **Added**: Optional "Prompt instructions" field (single line) in prompt edit form; stored in DB as `prompt_instructions`; when set, shown in Transform Input panel header row (right-aligned, muted).
- **Changed**: Transform toolbar icon buttons (Edit, Add, Duplicate, Import/Export): default slate-400; hover white + soft pill background with transition; Edit shows blue when editing is active; Import/Export default slate-500.
- **Changed**: Transform prompt row: Edit button before Add (closer to dropdown); dropdown appearance set to "underline".
- **Changed**: Load sample prompts uses same duplicate-name resolution as file import ("Name (1)", "Name (2)", etc.); shared `resolveDuplicateNames` in `promptUtils.js`.
- **Changed**: "Load sample prompts" button: light green background and BookOpenText icon.
- **Changed**: Delete confirmation modals: confirm button uses red background and white text; cancel button unchanged.
- **Added**: Trash icon in custom prompts table (Settings > Transform) on the prompt name cell; click shows confirmation modal and deletes the prompt on confirm.
- **Changed**: Settings > Transform import: prompts with a name that already exists are added as copies with "Name (1)", "Name (2)", etc., instead of overwriting.
- **Added**: "Load sample prompts" button (right-aligned on Transform prompt row) with confirmation modal; imports prompts from `config/custom-prompts.json` in merge mode.
- **Added: Export/Import button on Transform page (prompt selection row) that opens Settings > Transform tab.



