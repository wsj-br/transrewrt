services:
  t-r-web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - t-r-data:/app/data
    environment:
      - PORT=5000
      - CONFIG_PATH=/app/data/config.json

volumes:
  t-r-data:
