---
title: Налаштування
description: >-
  Короткий довідник для Загальних налаштувань, Моделей, Мов, Глосарію, Вартість,
  Трансформація, Користувачі, API та Про програму.
translation_last_updated: '2026-07-17T14:59:04.090Z'
source_file_mtime: '2026-07-17T14:37:21.849Z'
source_file_hash: 492e5fd37f4a6b282502282d4f2728047a0d09ae8c30334b3c8388a5ce6e9f68
translation_language: uk
source_file_path: src/content/docs/docs/settings.md
translation_models:
  - google/gemini-2.5-flash
  - meta-llama/llama-3.3-70b-instruct
---



Відкрийте **Налаштування** на бічній панелі, щоб налаштувати поведінку програми.

| Вкладка | Десктоп | Веб (адміністратор) | Веб (користувач) | Примітки |
| --- | :---: | :---: | :---: | --- |
| Загальні налаштування | так | так | так | Включає **Досвід ШІ** (Простий / Розширений) |
| Моделі | так | так | так | Лише коли **Досвід ШІ** є **Розширеним** |
| Мови | так | так | так | |
| Відстеження витрат | так | так | — | |
| Трансформація | так | так | так | Масовий імпорт/експорт підказок |
| Глосарій | так | так | так | Пари термінів для перекладу |
| Користувачі | — | так | — | |
| Конфігурація API | так | так | — | |
| Про програму | так | так | так | |

У **Простому** режимі виберіть ШІ за допомогою попередніх налаштувань на панелі інструментів та **Провайдера** у Загальних налаштуваннях; вкладка **Моделі** прихована.

:::note
У веб-версії кожен користувач має власну конфігурацію (досвід ШІ, провайдер, моделі/пресети, мови, опції, підказки). Зміни не впливають на інших користувачів.
:::

## Загальні налаштування

**Досвід ШІ**

- **Простий** (за замовчуванням): виберіть **Провайдера**. Хмарні провайдери використовують попередньо встановлені налаштування панелі інструментів (**Безкоштовно (OpenRouter)**, **Стандартний**, **Розширений**, **Технічний**). **Локальний LLM** натомість відображає встановлені локальні моделі. **Оновити каталог попередніх налаштувань** завантажує останній список попередніх налаштувань з репозиторію проекту.
- **Розширений**: виберіть моделі на панелі інструментів; керуйте списком у розділі [Моделі](#models).

**Вигляд** — Тема; **Показувати інформацію про вартість у діях**; **Кількість знаків після коми для вартості**; поле лише для веб-версії навколо програми; **Сімейство шрифтів** та **Розмір**.

**Поведінка** — **Поведінка для ENTER**; **Автоматичне виконання при вставці**; **Автоматичне копіювання результату в буфер обміну**; **Переклад у реальному часі під час набору тексту**; **Таймаут (мс)**.

**Історія**

- **Зберігати історію виконання** — зберігати вхідні/вихідні дані для перегляду [Історії](/docs/history/). Вимкнення запитує підтвердження та може видалити збережений текст. Якщо позначено як *вимкнено адміністратором*, встановлено `HISTORY_DISABLED` — див. [Конфігурація](/docs/configuration/#privacy-mode).
- **Видалити дані історії** — видалити збережений текст за віком або очистити все. **Не** видаляє загальні суми витрат (для цього використовуйте відстеження витрат).

**Резервне копіювання конфігурації** (адміністратори настільних і веб-версій)

- Додатково **Включити дані про використання в резервну копію**
- **Конфігурація резервного копіювання** — ZIP-архів з конфігурацією, станом, користувачами, налаштуваннями, підказками та додатковими даними про використання
- **Відновити з резервної копії** — діалогове вікно підтвердження з опціями відновлення та/або очищення даних про використання

Резервні копії можуть переміщуватися між настільною та веб-версіями; відновлення резервної копії настільної версії у веб-версії застосовує дані до користувача-адміністратора.

## Моделі

Доступно лише в режимі **Розширений**.

![Вкладка «Налаштування моделей»](/images/screenshots/uk/settings-general.png)

- **Доступні моделі** (ліворуч) та **Вибрані моделі** (праворуч)
- Пошук, мітки **Провайдера**, **Тільки безкоштовні**, **Оновити**, Розгорнути/Згорнути все
- Ідентифікатори моделей використовують префікс провайдера (`openrouter/…`, `openai/…`, `local/…`, …)

:::caution
Не використовуйте OpenRouter **Body Builder** (`openrouter/bodybuilder`) для перекладу, переписування або трансформації — він повертає корисні дані запиту JSON, а не готовий текст.
:::

Додати за допомогою **Додати**; видалити за допомогою **X**. **Скасувати вибір усіх** залишає необхідну безкоштовну модель.

## Мови

- **Найпопулярніші мови** — закріплені біля верхньої частини списків мов у Перекладі та Трансформації
- **Власна мова** — додати мову, якої немає у вбудованому списку

## Відстеження витрат

- **Загальна вартість**, **Копіювати значення**, **Скинути вартість**
- **Синхронізувати з використанням ключа API** — узгодити з використанням облікового запису OpenRouter (тільки OpenRouter)
- **Використання ключа API** — деталі OpenRouter, якщо доступні
- **Видалити дані про витрати** — усі дані або записи, старші за певну дату

OpenRouter показує фактичну виставлену вартість, якщо це можливо; інші провайдери використовують оцінки з ціноутворення OpenRouter. Оцінки не є рахунками-фактурами.

:::caution
Видалення даних про витрати не можна скасувати. Експортуйте їх через Історію або Панель інструментів → Усі виклики, якщо вам потрібна резервна копія. Пов’язана історія вхідних/вихідних даних для цих викликів API також видаляється.
:::

## Трансформація

Масове керування підказками: перегляд, видалення, імпорт, експорт та завантаження зразків підказок.

## Глосарій

Керуйте парами термінів, що застосовуються під час [перекладу](/docs/translate/#use-the-glossary). Кожен термін має мову джерела/цілі та текст джерела/цілі.

- Додати через нижній рядок і **+**
- Фільтрувати за мовами або текстом
- Імпортувати/експортувати CSV або XLSX; завантажити порожні шаблони

Десктоп зберігає глосарій локально; веб-зберігає його для кожного користувача.

## Користувачі

Тільки для веб-версії (адміністратори): додати користувачів, оновити дані, скинути паролі, видалити облікові записи.

## Конфігурація API

Налаштуйте лише тих провайдерів, яких ви використовуєте: OpenRouter, OpenAI, Anthropic, Google Gemini, DeepSeek, Groq, Mistral, xAI, Cerebras, NVIDIA, Alibaba Cloud, apikey.fun, **Локальний LLM** (базова URL-адреса для Ollama, LM Studio, llama.cpp або подібних) та необов'язковий власний сумісний з OpenAI провайдер.

**Веб (адміністратор):** ключі походять з змінних середовища — ця сторінка показує, які з них встановлені, і дозволяє вам **Перевірити**. Перезапустіть після зміни змінних середовища. Див. [Конфігурація](/docs/configuration/).

**Настільний комп'ютер:** введіть ключі (або URL-адресу локального LLM) та **Зберегти** / **Редагувати** / **Тестувати**. Ключі зберігаються в зашифрованому вигляді; ви не можете переглянути поточне значення, лише замінити його.

:::tip
Для початку не потрібен платний ключ: використовуйте безкоштовні моделі OpenRouter, інших провайдерів безкоштовного рівня або локальний сервер, сумісний з OpenAI, такий як [Ollama](https://ollama.com), LM Studio або llama.cpp (наприклад, `translategemma:4b`).
:::

## Про додаток

Назва додатка, версія, дата збірки, ліцензія, повідомлення про третіх осіб та посилання на репозиторій.
