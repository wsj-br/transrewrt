---
title: الإعدادات
description: >-
  مرجع موجز للإعدادات العامة، والنماذج، واللغات، والمسرد، والتكلفة، والتحويل،
  والمستخدمين، وواجهة برمجة التطبيقات (API)، وحول التطبيق.
translation_last_updated: '2026-07-17T21:14:44.527Z'
source_file_mtime: '2026-07-17T14:37:21.849Z'
source_file_hash: 492e5fd37f4a6b282502282d4f2728047a0d09ae8c30334b3c8388a5ce6e9f68
translation_language: ar
source_file_path: src/content/docs/docs/settings.md
translation_models:
  - google/gemini-2.5-flash
  - meta-llama/llama-3.3-70b-instruct
---



افتح **الإعدادات** من الشريط الجانبي لتخصيص سلوك التطبيق.

| علامة التبويب | سطح المكتب | الويب (المسؤول) | الويب (المستخدم) | ملاحظات |
| --- | :---: | :---: | :---: | --- |
| الإعدادات العامة | نعم | نعم | نعم | تتضمن **تجربة الذكاء الاصطناعي** (سهلة / متقدمة) |
| النماذج | نعم | نعم | نعم | فقط عندما تكون **تجربة الذكاء الاصطناعي** **متقدمة** |
| اللغات | نعم | نعم | نعم | |
| تتبع التكلفة | نعم | نعم | — | |
| تحويل | نعم | نعم | نعم | استيراد/تصدير جماعي للمطالبات |
| المسرد | نعم | نعم | نعم | أزواج المصطلحات للترجمة |
| المستخدمون | — | نعم | — | |
| تكوين واجهة برمجة التطبيقات (API) | نعم | نعم | — | |
| حول | نعم | نعم | نعم | |

في وضع **السهل**، اختر الذكاء الاصطناعي عبر الإعدادات المسبقة في شريط الأدوات و**الموفر** في الإعدادات العامة؛ يتم إخفاء علامة التبويب **النماذج**.

:::note
في إصدار الويب، لكل مستخدم تكوينه الخاص (تجربة الذكاء الاصطناعي، الموفر، النماذج/الإعدادات المسبقة، اللغات، الخيارات، المطالبات). لا تؤثر التغييرات على المستخدمين الآخرين.
:::

## الإعدادات العامة

**تجربة الذكاء الاصطناعي**

- **بسيط** (افتراضي): اختر **مزود**. مزودي السحابة يستخدمون إعدادات شريط الأدوات (**مجانًا (OpenRouter)**، **معياري**، **متقدم**، **تقني**). **النموذج المحلي LLM** يقوم بlisting النماذج المحلية المثبتة بدلاً من ذلك. **تحديث كتالوج الإعدادات المسبقة** يحصل على قائمة الإعدادات المسبقة أحدث من مستودع المشروع.
- **متقدم**: اختر النماذج في شريط الأدوات؛ إدارة القائمة تحت [النماذج](#models).

**المظهر** — السمة؛ **إظهار معلومات التكلفة على الإجراءات**؛ **أرقام كسور التكلفة**؛ الهامش حول التطبيق المخصص للويب فقط؛ **عائلة الخط** و**الحجم**.

**السلوك** — **سلوك مفتاح ENTER**؛ **التنفيذ التلقائي عند اللصق**؛ **النسخ التلقائي للنتيجة إلى الحافظة**؛ **الترجمة في الوقت الفعلي أثناء الكتابة**؛ **المهلة (مللي ثانية)**.

**السجل**

- **الاحتفاظ بسجل التنفيذ** — تخزين المدخلات/المخرجات لعرض [السجل](/docs/history/). يؤدي إيقاف التشغيل إلى طلب تأكيد ويمكن أن يزيل النص المخزن. إذا تم تصنيفها على أنها *معطلة بواسطة المسؤول*، فسيتم تعيين `HISTORY_DISABLED` — راجع [التكوين](/docs/configuration/#privacy-mode).
- **حذف بيانات السجل** — إزالة النص المخزن حسب العمر أو مسح الكل. لا **يحذف** إجماليات التكلفة (استخدم تتبع التكلفة لذلك).

**النسخ الاحتياطي للتكوين** (مسؤولو سطح المكتب والويب)

- اختياري **تضمين بيانات الاستخدام في النسخ الاحتياطي**
- **تكوين النسخ الاحتياطي** — ملف ZIP يحتوي على التكوين والحالة والمستخدمين والتفضيلات والمطالبات وبيانات الاستخدام الاختيارية
- **الاستعادة من النسخ الاحتياطي** — مربع حوار تأكيد مع خيارات للاستعادة و/أو مسح بيانات الاستخدام

يمكن نقل النسخ الاحتياطية بين سطح المكتب والويب؛ يؤدي استعادة نسخة احتياطية لسطح المكتب على الويب إلى تطبيق البيانات على المستخدم المسؤول.

## النماذج

متاح فقط في وضع **المتقدم**.

![علامة تبويب نماذج الإعدادات](/images/screenshots/ar/settings-general.png)

- **النماذج المتاحة** (اليسار) و **النماذج المحددة** (اليمين)
- بحث، **شريحة المزود**، **مجانًا فقط**، **تحديث**، توسيع/تقليل الكل
- معرّفات النماذج تستخدم بادئة مزود (`openrouter/…`، `openai/…`، `local/…`، …)

:::caution
لا تستخدم OpenRouter **Body Builder** (`openrouter/bodybuilder`) للترجمة أو إعادة الكتابة أو التحويل — فهو يُرجع حمولات طلب JSON، وليس نصًا مكتملًا.
:::

أضف باستخدام **إضافة**؛ أزل باستخدام **X**. **إلغاء تحديد الكل** يحافظ على النموذج المجاني المطلوب.

## اللغات

- **أهم اللغات** — مثبتة بالقرب من أعلى قوائم اللغات في الترجمة والتحويل
- **لغة مخصصة** — أضف لغة مفقودة من القائمة المضمنة

## تتبع التكلفة

- **التكلفة الإجمالية**، **نسخ القيمة**، **إعادة تعيين التكلفة**
- **المزامنة مع استخدام مفتاح API** — المواءمة مع استخدام حساب OpenRouter (OpenRouter فقط)
- **استخدام مفتاح API** — تفاصيل OpenRouter عند توفرها
- **حذف بيانات التكلفة** — جميع البيانات أو الإدخالات الأقدم من تاريخ معين

يعرض OpenRouter التكلفة الفعلية المفوترة عند الاقتضاء؛ يستخدم الموفرون الآخرون تقديرات من تسعير OpenRouter. التقديرات ليست فواتير.

:::caution
لا يمكن التراجع عن حذف بيانات التكلفة. قم بالتصدير عبر السجل أو لوحة التحكم ← جميع المكالمات أولاً إذا كنت بحاجة إلى نسخة احتياطية. يتم أيضًا إزالة سجل المدخلات/المخرجات ذات الصلة لتلك المكالمات API.
:::

## تحويل

إدارة المطالبات بكميات كبيرة: مراجعة، حذف، استيراد، تصدير، وتحميل مطالبات نموذجية.

## مسرد المصطلحات

إدارة أزواج المصطلحات المطبقة أثناء [الترجمة](/docs/translate/#use-the-glossary). يحتوي كل مصطلح على لغة المصدر/الهدف ونص المصدر/الهدف.

- أضف عبر الصف السفلي و **+**
- افلتر حسب اللغات أو النص
- استيراد/تصدير CSV أو XLSX؛ تحميل قوالب فارغة

يخزن سطح المكتب المسرد محليًا؛ بينما يخزنه الويب لكل مستخدم.

## المستخدمون

الويب فقط (المسؤولون): إضافة مستخدمين، تحديث التفاصيل، إعادة تعيين كلمات المرور، حذف الحسابات.

## تكوين واجهة برمجة التطبيقات (API)

قم بتهيئة المزودين فقط الذين تستخدمهم: OpenRouter، OpenAI، Anthropic، Google Gemini، DeepSeek، Groq، Mistral، xAI، Cerebras، NVIDIA، Alibaba Cloud، apikey.fun، **النموذج المحلي LLM** (URL أساسي لـ Ollama، LM Studio، llama.cpp، أو مشابه)، ومزود مخصص متوافق مع OpenAI اختياري.

**الويب (المسؤول):** تأتي المفاتيح من متغيرات البيئة — تعرض هذه الصفحة المفاتيح التي تم تعيينها وتتيح لك **الاختبار**. أعد التشغيل بعد تغيير متغيرات البيئة. راجع [التكوين](/docs/configuration/).

**أجهزة المكتب:** أدخل المفاتيح (أو URL النموذج المحلي LLM) و **حفظ** / **تحرير** / **اختبار**. يتم تخزين المفاتيح مشفرة؛ لا يمكنك عرض القيمة الحالية، فقط استبدالها.

:::tip
لا يلزم مفتاح مدفوع لبدء التشغيل: استخدم نماذج OpenRouter المجانية، أو مزودي المستوى المجاني الآخرين، أو خادم محلي متوافق مع OpenAI مثل [Ollama](https://ollama.com)، LM Studio، أو llama.cpp (على سبيل المثال `translategemma:4b`).
:::

## حول

اسم التطبيق، الإصدار، تاريخ الإنشاء، الترخيص، إشعارات الجهات الخارجية، ورابط المستودع.
