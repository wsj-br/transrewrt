---
title: 설정
description: 일반, 모델, 언어, 용어집, 비용, 변환, 사용자, API, 정보에 대한 간결한 참조입니다.
---



사이드바에서 **설정**을 열어 앱의 동작 방식을 사용자 정의하세요.

| 탭 | 데스크톱 | 웹 (관리자) | 웹 (사용자) | 비고 |
| --- | :---: | :---: | :---: | --- |
| 일반 설정 | 예 | 예 | 예 | **AI 경험** (쉬움 / 고급) 포함 |
| 모델 | 예 | 예 | 예 | **AI 경험**이 **고급**일 때만 |
| 언어 | 예 | 예 | 예 | |
| 비용 추적 | 예 | 예 | — | |
| 변환 | 예 | 예 | 예 | 프롬프트 일괄 가져오기/내보내기 |
| 용어집 | 예 | 예 | 예 | 번역용 용어 쌍 |
| 사용자 | — | 예 | — | |
| API 설정 | 예 | 예 | — | |
| 정보 | 예 | 예 | 예 | |

**쉬움** 모드에서는 도구 모음의 프리셋과 일반 설정의 **제공자**를 통해 AI를 선택합니다. **모델** 탭은 숨겨집니다.

:::note
웹 버전에서는 각 사용자가 자체 구성(AI 경험, 제공자, 모델/프리셋, 언어, 옵션, 프롬프트)을 가집니다. 변경 사항은 다른 사용자에게 영향을 주지 않습니다.
:::

## 일반 설정

![설정 일반 설정 탭](/images/screenshots/ko/settings-general.png)

**AI 경험**

- **쉬움** (기본값): **제공자**를 선택합니다. 클라우드 제공자는 도구 모음의 프리셋을 사용합니다. **로컬 LLM**은 설치된 로컬 모델을 대신 표시합니다. **프리셋 카탈로그 새로고침**은 프로젝트 저장소에서 최신 프리셋 목록을 가져옵니다.
  - **무료 (OpenRouter)** — 사용 가능한 무료 모델로 라우팅되는 무료 옵션. 품질과 가용성은 달라질 수 있습니다
  - **표준** — 가볍고 비용 효율적. 짧은 텍스트, 빠른 초안, 대량 사용에 적합
  - **고급** — 복잡하거나 미묘한 콘텐츠를 위한 고정확도 모델, 더 높은 비용
  - **기술** — 코드, API, 개발자 문서, 구조화된 콘텐츠에 최적화. 서식 및 용어 유지
- **고급**: 도구 모음에서 모델을 선택합니다. [모델](#models)에서 목록을 관리합니다.

도구 모음의 프리셋/모델 메뉴(**쉬움/고급 모드로 전환**, 설정 열기 위)에서도 쉬움 ↔ 고급을 전환할 수 있습니다.

**모양** — 테마; **작업에 비용 정보 표시**; **비용 소수점 자릿수**; 웹 전용 앱 여백; **글꼴 패밀리** 및 **크기**.

**동작** — **ENTER 키 동작**; **붙여넣을 때 자동 실행**; **결과를 클립보드에 자동 복사**; **입력 중 실시간 번역**; **시간 초과 (ms)**.

**기록**

- **실행 기록 보관** — [기록](/docs/history/) 보기를 위한 입력/출력 저장. 끄면 확인을 요청하며 저장된 텍스트를 제거할 수 있습니다. *관리자가 비활성화함*으로 표시된 경우 `HISTORY_DISABLED`이(가) 설정되어 있습니다 — [구성](/docs/configuration/#privacy-mode)을 참조하세요.
- **기록 데이터 삭제** — 기간별로 저장된 텍스트를 제거하거나 전체 삭제. 비용 합계는 삭제하지 **않습니다** (비용 추적을 사용하세요).

**구성 백업** (데스크톱 및 웹 관리자)

- 선택 사항 **백업에 사용량 데이터 포함**
- **구성 백업** — 구성, 상태, 사용자, 환경설정, 프롬프트 및 선택적 사용량 데이터가 포함된 ZIP
- **백업에서 복원** — 사용량 데이터 복원 및/또는 삭제 옵션이 있는 확인 대화상자

백업은 데스크톱과 웹 간에 이동할 수 있습니다. 웹에서 데스크톱 백업을 복원하면 데이터가 관리자 사용자에게 적용됩니다.

## 모델

**고급** 모드에서만 사용 가능합니다.

- **사용 가능한 모델** (왼쪽) 및 **선택된 모델** (오른쪽)
- 검색, **제공자** 칩, **무료만**, **새로고침**, 모두 펼치기/접기
- 모델 ID는 제공자 접두사를 사용합니다 (`openrouter/…`, `openai/…`, `local/…`, …)

:::tip
Translate, Rewrite 또는 Transform에는 OpenRouter **Body Builder** (`openrouter/bodybuilder`)를 사용하지 마세요. 완성된 텍스트가 아닌 JSON 요청 페이로드를 반환합니다.
:::

**추가**로 추가; **X**로 제거. OpenRouter 무료 모델은 선택 사항입니다 — 선택된 모델이 비어 있을 수 있습니다. 도구 모음에서 마지막 모델을 제거하면 **설정 → 모델**이 열립니다. 현재 모델을 사용할 수 없게 되면, 앱은 무료 모델을 강제 적용하는 대신 목록의 다음 모델을 선택합니다.

## 언어

- **상위 언어** — 번역 및 변환의 언어 목록 상단에 고정
- **사용자 지정 언어** — 내장 목록에 없는 언어 추가

## 비용 추적

- **총 비용**, **값 복사**, **비용 초기화**
- **API 키 사용량과 동기화** — OpenRouter 계정 사용량과 맞춤 (OpenRouter만 해당)
- **API 키 사용량** — 가능한 경우 OpenRouter 세부 정보
- **비용 데이터 삭제** — 모든 데이터 또는 특정 날짜 이전 항목

OpenRouter는 해당하는 경우 실제 청구 비용을 표시합니다. 다른 제공자는 OpenRouter 가격 책정을 기반으로 한 예상치를 사용합니다. 예상치는 청구서가 아닙니다.

:::caution
비용 데이터 삭제는 되돌릴 수 없습니다. 백업이 필요한 경우 먼저 기록 또는 대시보드 → 전체 호출을 통해 내보내세요. 해당 API 호출에 대한 관련 입력/출력 기록도 함께 제거됩니다.
:::

## 변환

프롬프트 일괄 관리: 검토, 삭제, 가져오기, 내보내기 및 샘플 프롬프트 불러오기.

## 용어집

[번역](/docs/translate/#use-the-glossary) 중에 적용되는 용어 쌍을 관리합니다. 각 용어는 원어/대상어 및 원어/대상어 텍스트를 가집니다.

- 하단 행과 **+** 를 통해 추가
- 언어 또는 텍스트로 필터링
- CSV 또는 XLSX 가져오기/내보내기; 빈 템플릿 다운로드

데스크톱은 용어집을 로컬에 저장하고, 웹은 사용자별로 저장합니다.

## 사용자

웹 전용(관리자):

- 사용자 추가, 세부 정보 업데이트, 비밀번호 재설정, 계정 삭제
- **Session Timeout** — 로그인이 유지되는 시간(1시간~7일); 변경 사항은 새 로그인에만 적용됩니다
- **Revoke sessions** — 모든 기기에서 사용자를 즉시 로그아웃

로그인한 모든 사용자(비관리자 포함)는 사이드바 하단의 사용자 메뉴에서 자신의 비밀번호를 변경하거나 로그아웃할 수 있습니다.

## API 설정

사용하는 제공자만 구성합니다: OpenRouter, OpenAI, Anthropic, Google Gemini, DeepSeek, Groq, Mistral, xAI, Cerebras, NVIDIA, Alibaba Cloud, apikey.fun, **Local LLM** (Ollama, LM Studio, llama.cpp 등의 기본 URL), 그리고 선택적 사용자 지정 OpenAI 호환 제공자.

**Web (admin):** 키는 환경 변수에서 가져옵니다 — 이 페이지는 설정된 항목을 보여주며 **Test**할 수 있게 합니다. 환경 변수를 변경한 후 다시 시작하십시오. [Configuration](/docs/configuration/)을 참조하십시오.

**Desktop:** 키(또는 Local LLM URL)를 입력하고 **Save** / **Edit** / **Test**하십시오. 키는 암호화되어 저장됩니다; 현재 값을 볼 수는 없고 교체만 할 수 있습니다.

:::tip
시작하는 데 유료 키가 필요하지 않습니다: 무료 OpenRouter 모델, 다른 무료 등급 제공자, 또는 [Ollama](https://ollama.com), LM Studio, llama.cpp(예: `translategemma:4b`)와 같은 로컬 OpenAI 호환 서버를 사용하십시오.
:::

## 정보

앱 이름, 버전, 빌드 날짜, 라이선스, 타사 고지 사항 및 리포지토리 링크.
