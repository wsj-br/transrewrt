---
title: 設定
description: 一般、模型、語言、詞彙表、成本、轉換、使用者、API 與關於的精簡參考。
---



從側邊欄開啟 **設定** 以自訂應用程式的行為。

| 分頁 | 桌面版 | 網頁版（管理員） | 網頁版（使用者） | 備註 |
| --- | :---: | :---: | :---: | --- |
| 一般設定 | 是 | 是 | 是 | 包含 **AI 體驗**（簡易／進階） |
| 模型 | 是 | 是 | 是 | 僅當 **AI 體驗**為 **進階** 時顯示 |
| 語言 | 是 | 是 | 是 | |
| 成本追蹤 | 是 | 是 | — | |
| 轉換 | 是 | 是 | 是 | 提示詞的批量匯入／匯出 |
| 詞彙表 | 是 | 是 | 是 | 翻譯用的詞彙對 |
| 使用者 | — | 是 | — | |
| API 設定 | 是 | 是 | — | |
| 關於 | 是 | 是 | 是 | |

在 **簡易** 模式下，透過工具列的預設集選擇 AI，並在一般設定中選擇 **供應商**；**模型** 分頁會隱藏。

:::note
在網頁版本中，每位使用者都有自己的設定（AI 體驗、供應商、模型／預設集、語言、選項、提示詞）。變更不會影響其他使用者。
:::

## 一般設定

![設定 一般設定 分頁](/images/screenshots/zh-Hant/settings-general.png)

**AI 體驗**

- **簡易**（預設）：選擇一個 **供應商**。雲端供應商使用工具列預設集。**本地 LLM** 則列出已安裝的本地模型。**重新整理預設集目錄** 會從專案儲存庫擷取最新的預設集清單。
  - **免費 (OpenRouter)** — 零成本選項，路由至可用的免費模型；品質與可用性可能有所差異
  - **標準** — 輕量且具成本效益；適合短文、快速草稿及大量使用
  - **進階** — 高準確度模型，適用於複雜或細微的內容，成本較高
  - **技術** — 針對程式碼、API、開發者文件及結構化內容調校；保留格式與術語
- **進階**：在工具列中選擇模型；在[模型](#models)下方管理清單。

您也可以從工具列的預設集／模型選單切換簡易 ↔ 進階（**切換至簡易／進階模式**，位於「開啟設定」上方）。

**外觀** — 主題；**在動作上顯示成本資訊**；**成本小數位數**；僅限網頁版的應用程式邊界；**字型家族**與**大小**。

**行為** — **ENTER 鍵行為**；**貼上時自動執行**；**自動複製結果到剪貼簿**；**輸入時即時翻譯**；**逾時 (毫秒)**。

**歷史**

- **保留執行歷史** — 為[歷史](/docs/history/)檢視儲存輸入/輸出。關閉時會要求確認，並可能移除已儲存的文字。如果標示為*disabled by the administrator*，表示已設定 `HISTORY_DISABLED` — 請參閱[設定](/docs/configuration/#privacy-mode)。
- **刪除歷史資料** — 依存在時間移除已儲存的文字或清除全部。**不會**刪除成本總計（請使用成本追蹤來執行此操作）。

**設定備份**（桌面版與網頁版管理員）

- 可選的 **在備份中包含使用資料**
- **備份設定** — 包含設定、狀態、使用者、偏好設定、提示詞與可選使用資料的 ZIP 檔案
- **從備份還原** — 帶有還原及/或清除使用資料選項的確認對話框

備份可以在桌面版與網頁版之間移動；在網頁版還原桌面版備份會將資料套用至管理員使用者。

## 模型

僅在**進階**模式下可用。

- **可用模型**（左側）與**已選模型**（右側）
- 搜尋、**供應商**晶片、**僅限免費**、**重新整理**、全部展開/摺疊
- 模型 ID 使用供應商前綴（`openrouter/…`、`openai/…`、`local/…`、…）

:::tip
請勿使用 OpenRouter **Body Builder** (`openrouter/bodybuilder`) 進行翻譯、改寫或轉換 — 它會傳回 JSON 請求負載，而非完成的文字。
:::

使用**新增**來新增；使用**X**來移除。OpenRouter 免費模型是選用的 — 已選模型可以為空。從工具列移除最後一個模型會開啟**設定 → 模型**。如果目前模型變得不可用，應用程式會選擇清單中的下一個模型，而不是強制使用免費模型。

## 語言

- **頂端語言** — 固定在翻譯與轉換的語言清單頂端附近
- **自訂語言** — 新增內建清單中缺少的語言

## 成本追蹤

- **總成本**、**複製數值**、**重設成本**
- **與 API 金鑰使用量同步** — 與 OpenRouter 帳戶使用量對齊（僅限 OpenRouter）
- **API 金鑰使用量** — 可用時的 OpenRouter 詳細資訊
- **刪除成本資料** — 所有資料或早於特定日期的項目

OpenRouter 會在適用時顯示實際計費成本；其他供應商則使用來自 OpenRouter 定價的預估值。預估值並非發票。

:::caution
成本資料刪除無法復原。如果您需要備份，請先透過歷史或儀表板 → 所有呼叫匯出。這些 API 呼叫的相關輸入/輸出歷史也會被移除。
:::

## 轉換

批次管理提示詞：檢閱、刪除、匯入、匯出與載入範例提示詞。

## 詞彙表

管理在[翻譯](/docs/translate/#use-the-glossary)期間套用的詞彙配對。每個詞彙都具有來源/目標語言以及來源/目標文字。

- 透過底部的列和 **+** 新增
- 按語言或文字篩選
- 匯入/匯出 CSV 或 XLSX；下載空白範本

桌面版會將詞彙表儲存在本機；網頁版會為每個使用者儲存。

## 使用者

僅限網頁版（管理員）：

- 新增使用者、更新詳細資料、重設密碼、刪除帳戶
- **工作階段逾時** — 登入持續時間（1 小時至 7 天）；變更僅適用於新的登入
- **撤銷工作階段** — 立即將使用者從所有裝置登出

每個已登入的使用者（包括非管理員）都可以從側邊欄底部的使用者選單變更自己的密碼或登出。

## API 配置

僅設定您使用的提供者：OpenRouter、OpenAI、Anthropic、Google Gemini、DeepSeek、Groq、Mistral、xAI、Cerebras、NVIDIA、阿里巴巴雲、apikey.fun、**本機 LLM**（Ollama、LM Studio、llama.cpp 或類似工具的基本 URL），以及一個可選的自訂 OpenAI 相容提供者。

**網頁版（管理員）：** 金鑰來自環境變數 — 此頁面會顯示已設定的金鑰，並讓您 **測試**。變更環境變數後請重新啟動。請參閱[設定](/docs/configuration/)。

**桌面版：** 輸入金鑰（或本機 LLM URL）並 **儲存** / **編輯** / **測試**。金鑰會以加密方式儲存；您無法檢視目前的值，只能取代它。

:::tip
開始時不需要付費金鑰：使用免費的 OpenRouter 模型、其他免費層級提供者，或本機 OpenAI 相容伺服器，例如 [Ollama](https://ollama.com)、LM Studio 或 llama.cpp（例如 `translategemma:4b`）。
:::

## 關於

應用程式名稱、版本、建置日期、授權、第三方通知和存放庫連結。
