---
title: Rewrite text
description: Improve wording in the same language — clarity, tone, length, grammar, and more.
---

Use **Rewrite** to improve wording without changing the main meaning. Text stays in the same language.

![Rewrite workspace](/images/screenshots/en-GB/rewrite.png)

Modes include:

- **Check Spelling & Grammar**
- **Improve Clarity**
- **Alternative versions** (several reformulations in one run)
- **Make Formal** / **Make Informal**
- **Shorten** / **Expand**
- **Make Technical**

## Rewrite text

1. Open **Rewrite**.
2. Choose a **Mode**.
3. Optionally set **From** to the language of your text (or leave **Detect Language**).
4. Type or paste text into **Input**.
5. Click **Rewrite**.
6. Read the result in **Output**.

The [layout toggle and keyboard shortcuts](/docs/translate/#layout-and-keyboard) work the same as in Translate.

:::tip
After a rewrite, a **Show changes** (**Changes**) switch appears next to **Copy** for **every** rewrite mode. Toggle it to show or hide a diff between input and output.
:::

:::note
**Alternative versions** returns several reformulations in a **single** run, separated by `----`. That differs from **Rephrase…**, which builds a version history over time.
:::

## Refine a rewrite

After a successful run, **Rephrase…** and the version dropdown appear on the output side (same idea as [Translate](/docs/translate/#refine-a-translation), but the text stays in the same language and keeps the current **Mode**):

1. **Rephrase…** (no selection) — another full rewrite with different wording. Up to five versions. Click **Stop Rewrite** to cancel.
2. **Word alternatives** — select text, then right-click or **Rephrase…**.
3. Each request may add usage cost.

## Next steps

- [Translate text](/docs/translate/)
- [Transform with prompts](/docs/transform/)
- [Common issues](/docs/common-issues/)
