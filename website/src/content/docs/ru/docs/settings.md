---
title: Настройки
description: >-
  Краткий справочник по разделам: Общие, Модели, Языки, Глоссарий, Стоимость,
  Преобразование, Пользователи, API и О программе.
---



Откройте **Настройки** на боковой панели, чтобы настроить поведение приложения.

| Вкладка | Десктоп | Веб (администратор) | Веб (пользователь) | Примечания |
| --- | :---: | :---: | :---: | --- |
| Общие настройки | да | да | да | Включает **Опыт ИИ** (Простой / Расширенный) |
| Модели | да | да | да | Только когда **Опыт ИИ** — **Расширенный** |
| Языки | да | да | да | |
| Отслеживание стоимости | да | да | — | |
| Преобразование | да | да | да | Массовый импорт/экспорт промптов |
| Глоссарий | да | да | да | Пары терминов для перевода |
| Пользователи | — | да | — | |
| Конфигурация API | да | да | — | |
| О программе | да | да | да | |

В **Простом** режиме выберите ИИ через предустановки на панели инструментов и **Провайдера** в Общих настройках; вкладка **Модели** скрыта.

:::note
В веб-версии у каждого пользователя своя конфигурация (опыт ИИ, провайдер, модели/предустановки, языки, опции, промпты). Изменения не влияют на других пользователей.
:::

## Общие настройки

**Опыт ИИ**

- **Простой** (по умолчанию): выберите **Провайдера**. Облачные провайдеры используют предустановки панели инструментов (**Бесплатно (OpenRouter)**, **Стандартный**, **Расширенный**, **Технический**). **Локальный LLM** вместо этого перечисляет установленные локальные модели. **Обновить каталог предустановок** извлекает последний список предустановок из репозитория проекта.
- **Расширенный**: выбирайте модели на панели инструментов; управляйте списком в разделе [Модели](#models).

**Внешний вид** — Тема; **Показывать информацию о стоимости в действиях**; **Количество знаков после запятой для стоимости**; отступ вокруг приложения только для веб-версии; **Семейство шрифтов** и **Размер**.

**Поведение** — **Поведение для ENTER**; **Автоматическое выполнение при вставке**; **Автоматическое копирование результата в буфер обмена**; **Перевод в реальном времени при наборе текста**; **Тайм-аут (мс)**.

**История**

- **Сохранять историю выполнения** — хранить ввод/вывод для представления [История](/docs/history/). Выключение запрашивает подтверждение и может удалить хранимый текст. Если помечено *отключено администратором*, `HISTORY_DISABLED` установлен — см. [Конфигурация](/docs/configuration/#privacy-mode).
- **Удалить данные истории** — удалить хранимый текст по возрасту или очистить все. **Не** удаляет общие затраты (для этого используйте Отслеживание затрат).

**Резервное копирование конфигурации** (администраторы настольных и веб-приложений)

- Необязательно **Включить данные использования в резервную копию**
- **Резервное копирование конфигурации** — ZIP с конфигурацией, состоянием, пользователями, предпочтениями, подсказками и необязательными данными использования
- **Восстановить из резервной копии** — диалог подтверждения с вариантами восстановления и/или очистки данных использования

Резервные копии можно передавать между настольными и веб-приложениями; восстановление резервной копии настольного приложения на веб-приложении применяет данные к пользователю администратора.

## Модели

Доступно только в **Продвинутом** режиме.

![Вкладка Модели настроек](/images/screenshots/ru/settings-general.png)

- **Доступные модели** (слева) и **Выбранные модели** (справа)
- Поиск, чипы **Провайдер**, **Только бесплатные**, **Обновить**, Развернуть/Свернуть все
- Идентификаторы моделей используют префикс провайдера (`openrouter/…`, `openai/…`, `local/…`, …)

:::caution
Не используйте OpenRouter **Body Builder** (`openrouter/bodybuilder`) для перевода, переписывания или преобразования — он возвращает JSON-запросы, а не готовый текст.
:::

Добавить с помощью **Добавить**; удалить с помощью **X**. **Отменить все** сохраняет необходимую бесплатную модель.

## Языки

- **Верхние языки** — закреплены gần верха списка языков в Переводе и Преобразовании
- **Пользовательский язык** — добавить язык, отсутствующий в списке по умолчанию

## Отслеживание затрат

- **Общая стоимость**, **Копировать значение**, **Сбросить стоимость**
- **Синхронизировать с использованием API-ключа** — согласовать с использованием учетной записи OpenRouter (только OpenRouter)
- **Использование API-ключа** — детали OpenRouter, когда доступно
- **Удалить данные затрат** — все данные или записи старше определенной даты

OpenRouter показывает фактическую оплаченную стоимость, когда применимо; другие поставщики используют оценки из цен OpenRouter. Оценки не являются счетами.

:::caution
Удаление данных затрат нельзя отменить. Сначала экспортируйте через История или Панель управления → Все вызовы, если вам нужна резервная копия. Связанная история ввода/вывода для этих вызовов API также удаляется.
:::

## Преобразование

Массовое управление подсказками: просмотр, удаление, импорт, экспорт и загрузка образцов подсказок.

## Глоссарий

Управление парами терминов, применяемых во время [перевода](/docs/translate/#use-the-glossary). Каждый термин имеет язык источника/цели и текст источника/цели.

- Добавление с помощью нижней строки и **+**
- Фильтрация по языкам или тексту
- Импорт/экспорт CSV или XLSX; загрузка пустых шаблонов

Настольная версия хранит глоссарий локально; веб-версия хранит его для каждого пользователя.

## Пользователи

Только веб-версия (администраторы): добавление пользователей, обновление данных, сброс паролей, удаление учетных записей.

## Конфигурация API

Настройте только те провайдеры, которые вы используете: OpenRouter, OpenAI, Anthropic, Google Gemini, DeepSeek, Groq, Mistral, xAI, Cerebras, NVIDIA, Alibaba Cloud, apikey.fun, **Локальный LLM** (базовый URL для Ollama, LM Studio, llama.cpp или аналогичных) и необязательный пользовательский провайдер, совместимый с OpenAI.

**Веб-версия (администратор):** ключи берутся из переменных среды — на этой странице показано, какие из них установлены, и позволяет **Проверить**. Перезапустите после изменения переменных среды. См. [Конфигурация](/docs/configuration/).

**Рабочий стол:** введите ключи (или URL локального LLM) и **Сохранить** / **Изменить** / **Проверить**. Ключи хранятся в зашифрованном виде; вы не можете просмотреть текущее значение, только заменить его.

:::tip
Для начала работы не требуется платный ключ: используйте бесплатные модели OpenRouter, других провайдеров с бесплатным уровнем или локальный сервер, совместимый с OpenAI, такой как [Ollama](https://ollama.com), LM Studio или llama.cpp (например, `translategemma:4b`).
:::

## О программе

Название приложения, версия, дата сборки, лицензия, уведомления третьих сторон и ссылка на репозиторий.
