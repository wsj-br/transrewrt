---
title: تنظیمات
description: >-
  مرجع فشرده برای عمومی، مدل‌ها، زبان‌ها، واژه‌نامه، هزینه، تبدیل، کاربران، API
  و درباره.
---



برای سفارشی‌سازی نحوه عملکرد برنامه، **تنظیمات** را از نوار کناری باز کنید.

| تب | دسکتاپ | وب (مدیر) | وب (کاربر) | یادداشت‌ها |
| --- | :---: | :---: | :---: | --- |
| تنظیمات عمومی | بله | بله | بله | شامل **تجربه هوش مصنوعی** (آسان / پیشرفته) |
| مدل‌ها | بله | بله | بله | فقط زمانی که **تجربه هوش مصنوعی** **پیشرفته** باشد |
| زبان‌ها | بله | بله | بله | |
| ردیابی هزینه | بله | بله | — | |
| تبدیل | بله | بله | بله | واردات/صادرات انبوه پرامپت‌ها |
| واژه‌نامه | بله | بله | بله | جفت‌های اصطلاح برای ترجمه |
| کاربران | — | بله | — | |
| پیکربندی API | بله | بله | — | |
| درباره | بله | بله | بله | |

در حالت **آسان**، هوش مصنوعی را از طریق پیش‌تنظیمات در نوار ابزار و **ارائه‌دهنده** در تنظیمات عمومی انتخاب کنید؛ تب **مدل‌ها** پنهان است.

:::note
در نسخه وب، هر کاربر پیکربندی خاص خود را دارد (تجربه هوش مصنوعی، ارائه‌دهنده، مدل‌ها/پیش‌تنظیمات، زبان‌ها، گزینه‌ها، پرامپت‌ها). تغییرات بر سایر کاربران تأثیری نمی‌گذارد.
:::

## تنظیمات عمومی

![تب تنظیمات عمومی](/images/screenshots/fa/settings-general.png)

**تجربه هوش مصنوعی**

- **آسان** (پیش‌فرض): یک **ارائه‌دهنده** انتخاب کنید. ارائه‌دهندگان ابری از پیش‌تنظیمات نوار ابزار استفاده می‌کنند. **LLM محلی** به جای آن مدل‌های محلی نصب شده را فهرست می‌کند. **به‌روزرسانی کاتالوگ پیش‌تنظیمات** جدیدترین لیست پیش‌تنظیمات را از مخزن پروژه واکشی می‌کند.
  - **رایگان (OpenRouter)** — گزینه بدون هزینه که به مدل‌های رایگان موجود هدایت می‌شود؛ کیفیت و در دسترس بودن ممکن است متفاوت باشد
  - **استاندارد** — سبک و مقرون‌به‌صرفه؛ بهترین برای متون کوتاه، پیش‌نویس‌های سریع و استفاده با حجم بالا
  - **پیشرفته** — مدل با دقت بالا برای محتوای پیچیده یا ظریف، با هزینه بالاتر
  - **فنی** — تنظیم شده برای کد، APIها، مستندات توسعه‌دهنده و محتوای ساختاریافته؛ قالب‌بندی و اصطلاحات را حفظ می‌کند
- **پیشرفته**: مدل‌ها را در نوار ابزار انتخاب کنید؛ لیست را در [مدل‌ها](#models) مدیریت کنید.

همچنین می‌توانید حالت آسان ↔ پیشرفته را از منوی پیش‌تنظیم/مدل نوار ابزار (**تغییر به حالت آسان/پیشرفته**، بالای باز کردن تنظیمات) تغییر دهید.

**ظاهر** — پوسته؛ **نمایش اطلاعات هزینه روی اقدامات**؛ **ارقام اعشار هزینه**؛ حاشیه فقط وب در اطراف برنامه؛ **خانواده فونت** و **اندازه**.

**رفتار** — **رفتار برای ENTER**؛ **اجرای خودکار هنگام چسباندن**؛ **کپی خودکار نتیجه در کلیپ‌بورد**؛ **ترجمه بلادرنگ هنگام تایپ**؛ **مهلت زمانی (میلی‌ثانیه)**.

**تاریخچه**

- **نگهداری تاریخچه اجرا** — ورودی/خروجی را برای نمای [تاریخچه](/docs/history/) ذخیره می‌کند. خاموش کردن آن درخواست تأیید می‌کند و می‌تواند متن ذخیره‌شده را حذف کند. اگر *توسط مدیر غیرفعال شده* برچسب‌گذاری شده باشد، `HISTORY_DISABLED` تنظیم شده است — به [پیکربندی](/docs/configuration/#privacy-mode) مراجعه کنید.
- **حذف داده‌های تاریخچه** — متن ذخیره‌شده را بر اساس سن حذف می‌کند یا همه را پاک می‌کند. مجموع هزینه‌ها را **حذف نمی‌کند** (برای این کار از ردیابی هزینه استفاده کنید).

**پشتیبان‌گیری پیکربندی** (مدیران دسکتاپ و وب)

- **شامل داده‌های استفاده در پشتیبان‌گیری** اختیاری
- **پیکربندی پشتیبان‌گیری** — ZIP با پیکربندی، وضعیت، کاربران، تنظیمات برگزیده، اعلان‌ها و داده‌های استفاده اختیاری
- **بازیابی از پشتیبان‌گیری** — گفتگوی تأیید با گزینه‌هایی برای بازیابی و/یا پاک کردن داده‌های استفاده

پشتیبان‌گیری‌ها می‌توانند بین دسکتاپ و وب جابجا شوند؛ بازیابی یک پشتیبان‌گیری دسکتاپ در وب، داده‌ها را برای کاربر مدیر اعمال می‌کند.

## مدل‌ها

فقط در حالت **پیشرفته** موجود است.

- **مدل‌های موجود** (چپ) و **مدل‌های انتخاب‌شده** (راست)
- جستجو، تراشه‌های **ارائه‌دهنده**، **فقط رایگان**، **تازه‌سازی**، گسترش/جمع کردن همه
- شناسه‌های مدل از پیشوند ارائه‌دهنده استفاده می‌کنند (`openrouter/…`، `openai/…`، `local/…`، …)

:::caution
از **Body Builder** (`openrouter/bodybuilder`) OpenRouter برای ترجمه، بازنویسی یا تبدیل استفاده نکنید — این ابزار بار داده‌های درخواست JSON را برمی‌گرداند، نه متن نهایی.
:::

با **افزودن** اضافه کنید؛ با **X** حذف کنید. مدل رایگان OpenRouter اختیاری است — مدل‌های انتخاب‌شده ممکن است خالی باشند. حذف آخرین مدل از نوار ابزار، **تنظیمات ← مدل‌ها** را باز می‌کند. اگر مدل فعلی در دسترس نباشد، برنامه مدل بعدی را در لیست انتخاب می‌کند به جای اینکه مدل رایگان را اجباری کند.

## زبان‌ها

- **زبان‌های برتر** — در بالای لیست زبان‌ها در ترجمه و تبدیل پین شده‌اند
- **زبان سفارشی** — زبانی را که در لیست داخلی موجود نیست اضافه کنید

## ردیابی هزینه

- **هزینه کل**، **کپی مقدار**، **بازنشانی هزینه**
- **همگام‌سازی با استفاده از کلید API** — همگام‌سازی با استفاده از حساب OpenRouter (فقط OpenRouter)
- **استفاده از کلید API** — جزئیات OpenRouter در صورت موجود بودن
- **حذف داده‌های هزینه** — همه داده‌ها یا ورودی‌های قدیمی‌تر از یک تاریخ

OpenRouter هزینه واقعی صورت‌حساب شده را در صورت لزوم نشان می‌دهد؛ سایر ارائه‌دهندگان از تخمین‌های قیمت‌گذاری OpenRouter استفاده می‌کنند. تخمین‌ها صورت‌حساب نیستند.

:::caution
حذف داده‌های هزینه قابل بازگشت نیست. اگر به پشتیبان‌گیری نیاز دارید، ابتدا از طریق تاریخچه یا داشبورد ← همه تماس‌ها خروجی بگیرید. تاریخچه ورودی/خروجی مرتبط برای آن تماس‌های API نیز حذف می‌شود.
:::

## تبدیل

مدیریت انبوه اعلان‌ها: بررسی، حذف، وارد کردن، صادر کردن و بارگذاری اعلان‌های نمونه.

## واژه‌نامه

جفت‌های اصطلاحاتی را که در طول [ترجمه](/docs/translate/#use-the-glossary) اعمال می‌شوند، مدیریت کنید. هر اصطلاح دارای زبان مبدأ/مقصد و متن مبدأ/مقصد است.

- از طریق ردیف پایین و **+** اضافه کنید
- بر اساس زبان‌ها یا متن فیلتر کنید
- CSV یا XLSX را وارد/صادر کنید؛ الگوهای خالی را دانلود کنید

دسکتاپ واژه‌نامه را به صورت محلی ذخیره می‌کند؛ وب آن را برای هر کاربر ذخیره می‌کند.

## کاربران

فقط وب (مدیران):

- کاربران را اضافه کنید، جزئیات را به‌روزرسانی کنید، رمز عبور را بازنشانی کنید، حساب‌ها را حذف کنید
- **مدت زمان نشست** — مدت زمان اعتبار ورود (۱ ساعت تا ۷ روز)؛ تغییرات فقط برای ورودهای جدید اعمال می‌شود
- **لغو نشست‌ها** — بلافاصله کاربر را از همه دستگاه‌ها خارج کنید

هر کاربر وارد شده (از جمله غیرمدیران) می‌تواند رمز عبور خود را تغییر دهد یا از منوی کاربر در پایین نوار کناری خارج شود.

## پیکربندی API

فقط ارائه‌دهندگانی را که استفاده می‌کنید پیکربندی کنید: OpenRouter، OpenAI، Anthropic، Google Gemini، DeepSeek، Groq، Mistral، xAI، Cerebras، NVIDIA، Alibaba Cloud، apikey.fun، **LLM محلی** (URL پایه برای Ollama، LM Studio، llama.cpp، یا مشابه)، و یک ارائه‌دهنده اختیاری سازگار با OpenAI سفارشی.

**وب (مدیر):** کلیدها از متغیرهای محیطی می‌آیند — این صفحه نشان می‌دهد کدام یک تنظیم شده‌اند و به شما امکان **تست** را می‌دهد. پس از تغییر متغیرهای محیطی، راه‌اندازی مجدد کنید. به [پیکربندی](/docs/configuration/) مراجعه کنید.

**دسکتاپ:** کلیدها (یا URL LLM محلی) را وارد کنید و **ذخیره** / **ویرایش** / **تست** را انجام دهید. کلیدها به صورت رمزگذاری شده ذخیره می‌شوند؛ نمی‌توانید مقدار فعلی را مشاهده کنید، فقط آن را جایگزین کنید.

:::tip
برای شروع نیازی به کلید پولی نیست: از مدل‌های رایگان OpenRouter، سایر ارائه‌دهندگان رایگان، یا یک سرور محلی سازگار با OpenAI مانند [Ollama](https://ollama.com)، LM Studio، یا llama.cpp (مثلاً `translategemma:4b`) استفاده کنید.
:::

## درباره

نام برنامه، نسخه، تاریخ ساخت، مجوز، اطلاعیه‌های شخص ثالث، و پیوند مخزن.
